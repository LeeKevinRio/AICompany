"""Signal engine: technical indicators and risk statistics over price bars.

Measurement only -- numbers and explicit states, no recommendations or target
prices (that is the later M3 phase). See ``models.py`` for the shared result
shapes and ``.claude/skills/backtest-protocol`` for the point-in-time rules the
backtest package enforces on top of these signals.
"""
