# ===== AI 虛擬公司環境變數範本 =====
# 用法：複製這個檔成 .env，填入真實值。
#   cp .env.example .env   （Windows PowerShell: Copy-Item .env.example .env）
# 注意：.env 已被 .gitignore，真 key 只放 .env，絕不放這個 .env.example，也絕不 commit。

# --- OpenAI Codex（跨廠商 code review 用）---
# 若用 API key 認證 Codex CLI，填這個；若改用 ChatGPT 帳號登入（codex login），可留空。
OPENAI_API_KEY=sk-your-openai-api-key-here

# --- 選填：image 模型（art-lead 實際出圖時）---
# TODO：依實際採用的 image 服務補上對應 key。
# IMAGE_API_KEY=your-image-model-api-key-here

# --- 選填：其他第三方服務 ---
# TODO：之後依專案需求新增。
