# Stock Desk C5 Kelly — 揭露/說明文案起草（creative-lead，2026-08-22）

- **狀態**：草案，逐字待 risk-compliance-officer CONFIRMED。K4 風險層與前端 UI 落地皆等本批。
- **分支**：product/stock-desk
- **依據**：
  - `work/stock-desk-C5-Kelly-架構評估.md` 約束 19/20（追加送審句 (a)-(e)）、約束 30（K 句顯示規則）、約束 36（`ci_includes_no_edge` 由 api 算好傳入，`limits.py` 只得分支）
  - `work/stock-desk-C5-Kelly-D9-量化意見.md` §3.3（揭露七要點，技術要點來源）、§3.1（選擇偏誤 MVP 只能定性的限制）
  - `docs/adr/0006-stock-desk-kelly-輸入來源與模組邊界.md` D-3（422 常態路徑）、D-6（`KellyInputs`／`ci_includes_no_edge` 欄位邊界）、Consequences（「樣本外」定位、跨界排除為已知限制不得包裝成優點）
  - 風格對齊：`apps/stock-desk/frontend/app/lib/adviceWording.ts`、`format.ts`；`app/settings/net_worth.py`（自報數字先例）；`work/stock-desk-C5-Kelly-PRD.md` FR-3/FR-5/FR-6 草案語意
  - `apps/stock-desk/backend/app/kelly/sample_gate.py`（現行六段 422 訊息）
  - K1 補審紀錄 `work/reviews/2026-08-19-C5-K1-qa補審.md`（缺 `oos_end_date` 時 `age_days=None`、`anchored_at=None`，一律判 `expired`，不得偽造天數）

## 0. 一件事要先跟風控挑明：既有「勝率」用詞的張力

`limits.py:837`（既有已上線、屬送審範圍 19、因輸入解封須重審）與本 PRD FR-5/FR-6 草案，都把 `win_rate` 顯示為「勝率」。但 `apps/stock-desk/frontend/app/lib/adviceWording.ts` 的 `FRONTEND_FORBIDDEN_TERMS`（機率洗白類）把「勝率」列為禁詞——雖然該清單的逐字守門測試目前只掃描 `adviceWording.ts` 自身的字面常數，不會擋到 `sample_gate.py` 或本批新句，但兩者出自同一份風控措辭原則，語意上是同一件事：**把歷史頻率讀成未來機率**。

本批的處理方式是**不迴避「勝率」這個詞**（它是 `win_rate` 在本產品既有、已核可的中文名，PRD/ADR/`limits.py` 全部沿用，臨時改名反而會製造第二套詞彙），而是用 (e) 句正面拆解「勝率≠下一筆機率」，並在每一處新句引用「勝率」時盡量伴隨限定語境（樣本、回合、歷史）。**請風控在審這批時一併確認：這個處理方式是否解除機率洗白疑慮，或是否要求本批連同 `limits.py:829/837` 一起改用其他詞（例如「歷史獲利回合比例」）。** 這是本次唯一一個我無法自行拍板、需要風控明示接受或改名的分歧點，其餘句子皆可視為逐字定稿候選。

另一個小發現：`sample_gate.py` 的 `PB_NONE_MESSAGE` 用「賠率」，但全產品其餘位置（`limits.py:829/837`、PRD FR-5/FR-6）一律用「盈虧比」指同一個 `payoff_ratio` 欄位。建議統一改「賠率」為「盈虧比」（純詞彙替換，不動 `reason_code`、不動數字結構），見 §4 (d)。

---

## (a) f* 區間顯示句（`ci_includes_no_edge = true` 時）

兩個顯示位置各有一版：**帶數字**版給設定頁／個股頁的 Kelly 輸入明細（該處直接讀 `kelly_inputs` 列，`f_star_ci_low`/`f_star_ci_high` 可得）；**旗標版**給 `LimitsCheckList` 第 5 條上限說明（該處走 `limits.py`，依約束 36 只能拿到布林 `ci_includes_no_edge`，不得自己算統計量）。

### (a-1) 帶數字版（設定頁／個股頁 Kelly 明細）— 主案

> 此次估計的 f\*（Kelly 建議比例，尚未套用上限）在 95% 信心水準下的區間為 {f_star_ci_low_pct} 至 {f_star_ci_high_pct}。此區間涵蓋『沒有優勢』的可能——以目前的樣本量，這個策略是否真的有優勢仍無法確定，即使點估計本身是正值。

**逐條自檢**
- 語族：使用「不代表」家族的變體「仍無法確定」，未直接用「本系統無法」——可接受，但若要嚴格對齊語族，可改為「本系統無法確定這個策略是否真的有優勢」。備案已改用該版本。
- 否定層數：「涵蓋『沒有優勢』」1 層 ＋「仍無法確定」1 層，共 2 層，分屬不同子句、非疊套反轉，符合 ≤2 層。
- 因果詞：無「因此/導致」等裸因果連接詞；「即使點估計本身是正值」是讓步子句，非因果宣稱。
- 措辭＝行為依據：區間確實是系統落地的 `f_star_ci_low/high`（約束 27），`ci_includes_no_edge` 的定義正是「`f_star_ci_low <= 0`」（D-6），故「涵蓋『沒有優勢』的可能」與「點估計為正不代表已排除」皆是對落地欄位的如實轉述，非額外宣稱。

### (a-1) 備案

> 本次 f\* 估計值的 95% 區間為 {f_star_ci_low_pct} 至 {f_star_ci_high_pct}，本系統無法確定這個策略在此區間內是否真的有優勢——區間跨過了『沒有優勢』（f\*≤0）那一側，點估計是正值不代表這個可能已被排除。

**逐條自檢**：語族含「本系統無法」；否定層數 2（「無法確定」「不代表…已排除」為 2 個獨立子句各 1 層）；無裸因果；措辭＝行為依據同主案。

### (a-2) 旗標版（`LimitsCheckList` 第 5 條上限說明，僅布林、無數字）— 主案

> 本次估計值的信賴區間涵蓋『沒有優勢』的可能：以目前樣本量，本系統無法確定這個策略是否真的有優勢。完整區間數字可至設定頁查看。

**逐條自檢**：語族含「本系統無法」；否定層數 1（「涵蓋『沒有優勢』」不算否定句式，是名詞化引用；「無法確定」1 層）；無裸因果；措辭＝行為依據——`limits.py` 只做 `if ctx.kelly.ci_includes_no_edge:` 這類分支後接固定句，不計算任何統計量，符合約束 36「只得分支」。

### (a-2) 備案

> 這個估計值的區間跨過了『沒有優勢』的那一側，本系統無法排除這個策略目前沒有優勢；區間的實際數字在設定頁可查看。

**逐條自檢**：語族含「本系統無法」；否定層數——「無法排除…沒有優勢」為疊套（無法+沒有），2 層但屬同一子句疊套，語意仍清楚（"cannot rule out no-edge" 是英文直譯感的中文句型，比主案的「無法確定是否真的有優勢」略繞口）；**建議採主案，備案僅供風控挑選語感**。

---

## (b) K_observed 選擇偏誤句 — 完整版（K≥2）與短版（K=1）

依約束 30：K_observed 含 rejected、含所有策略、不設時間窗；同時落地 K_distinct_specs；揭露須寫明兩者定義與偏誤方向。依架構評估限制：**MVP 只能定性，不得引用 D9 §3.1 模擬表的具體數字**（下列句子沒有任何一處引用「87.7%」「7.7% 額度」等模擬數字，僅用 K_observed / K_distinct_specs 兩個真實落地整數）。

### (b-完整版，K≥2) 主案

> 此標的已記錄 {k_observed} 次回測帶入嘗試（K_observed）——這個數字含被系統拒絕（422）的嘗試、含你試過的所有策略、不限時間範圍，只要你對這個標的送出過帶入請求就會被計入；另有 {k_distinct_specs} 組不同設定被嘗試過（K_distinct_specs，僅計相異參數組合）。這兩個數字都可能失真：K_observed 可能因重複送出同一組設定而偏高；K_distinct_specs 只算相異設定，即使只做過細微調整也會被算成新的一組。兩者也都無法涵蓋你在系統外或腦中比較過的設定，因此都可能低估你實際檢視過的可能性。已知的方向是：嘗試越多組設定、只把其中最好看的一次帶入，結果越容易被系統性地高估——問題出在『事後只挑最好的那一次』這個動作本身，不是任何一組設定本身有誤。本系統無法得知你實際嘗試過幾次，也不會、也無法事後校正這個高估；這裡呈現的只是系統看得到的下界。

**逐條自檢**
- 語族：「本系統無法得知」符合語族要求。
- 否定層數：逐句拆開——「都可能失真」非否定；「無法涵蓋」1 層；「不是任何一組設定本身有誤」1 層（獨立子句，說明歸因對象，非疊套）；「無法得知」1 層；「不會、也無法事後校正」1 層。共 4 個否定，分佈在 4 個不同子句、各自陳述獨立事實（K_observed 的定義局限／K_distinct_specs 的定義局限／偏誤成因歸屬／系統不校正的事實），**非同一命題的鏈式疊套**，與風控先前退修的「未顯示…不代表…並未過舊」三重反轉（同一命題被連續三次否定、造成印象反轉）不同質。仍請風控確認「≤2 層」是否為逐句硬性上限或僅適用鏈式疊套情況；若要求逐句嚴格 ≤2，需拆成兩段顯示（定義段＋偏誤段），備案已提供拆分版本供選用。
- 因果詞：無「因此/導致」等裸接續；「嘗試越多組…結果越容易被高估」是量化定性的比例關係陳述（quant D9 已定案的方向性結論），非未經支持的因果宣稱；「問題出在…不是…」明確界定歸因範圍，避免讀者誤讀成「某組設定有問題」。
- 措辭＝行為依據：K_observed／K_distinct_specs 的定義逐字對應約束 30 的 SQL 定義；「系統無法得知」對應 D9 §3.2「系統在原理上無法知道真正的 K」；「不會事後校正」對應 M2 說明（系統只顯示計數，不做任何統計校正）；未引用任何模擬數字，符合 MVP 限制。

### (b-完整版，K≥2) 備案（拆兩段，逐句否定層數更低，供風控挑選）

> 【定義】K_observed = {k_observed} 次：含被系統拒絕的嘗試、含所有策略、不限時間範圍，是你對這個標的送出過『帶入』的總次數。另記錄 K_distinct_specs = {k_distinct_specs} 組相異設定。兩者都可能低估你實際比較過的設定數（系統看不到你在畫面外或腦中做的比較），K_observed 也可能因重複送出同一設定而偏高。
> 【風險方向】嘗試越多組設定、只帶入其中最好看的一次，結果越容易被高估；這是『事後挑最好的那次』造成的，跟任何一組設定本身是否正確無關。本系統無法得知你實際比較過幾次，這裡顯示的次數只是系統看得到的下界，不會被拿來自動調整這條上限。

**逐條自檢**：語族含「本系統無法」；否定層數每句 ≤2；因果詞同主案判斷（定性比例關係，非裸因果）；措辭＝行為依據同主案，額外補一句「不會被拿來自動調整這條上限」對應約束 34（區間/樣本結構只供揭露，不進 clamp）。

### (b-短版，K=1) 主案

> 此標的目前記錄到 1 次回測帶入嘗試（K_observed，含被拒絕的嘗試、含所有策略、不限時間範圍）。這只代表你送出『帶入』的次數是 1 次，不代表你只檢視過 1 次回測結果——本系統無法得知你在畫面上比較過幾種設定、又只送出了其中一組。

**逐條自檢**：語族含「不代表」「本系統無法」；否定層數 2（「不代表」1、「無法得知」1，各自獨立子句）；無裸因果；措辭＝行為依據對應 D9 M2「K_observed==1 不代表使用者只看過一次回測，只代表他只按過一次帶入」。

### (b-短版，K=1) 備案

> 此標的的回測帶入嘗試記錄為 1 次（K_observed）。此數字只計你送出帶入的次數，不是你檢視過的設定數；你有可能已在畫面上比較過多組設定，才送出這一次。

**逐條自檢**：否定層數 1（「不是」）；語族未含「本系統無法」，較平鋪直敘；其餘同主案判斷。

---

## (c) walk-forward 定位句（使用者版）

要求：講清楚「本系統 walk-forward 不做參數擬合、out_of_sample 僅代表區段位置、不提供過擬合防護」，且不得暗示已提供保護。

### 主案

> 本系統的 walk-forward 是把資料按時間切成兩段：較早一段用來看策略在該段的表現，較晚一段（樣本外／out-of-sample）用同一組參數再看一次。三支策略的參數是系統寫死的常數，不會依任何輸入被調整，因此不存在『用樣本外資料回頭調參數』這件事。但也因為如此，『樣本外』在本系統中只代表資料的時間區段位置，對『你自己反覆嘗試多種設定後才挑一次結果』這件事沒有防護作用（詳見選擇偏誤揭露）。

**逐條自檢**
- 語族：本句未直接使用「不代表」「本系統無法」字面，用的是「不存在」「沒有防護作用」——語意等價但字面不同；備案改用語族字面版本供比較。
- 否定層數：「不會被調整」1、「不存在」1、「沒有防護作用」1，共 3 個否定，分屬 3 個獨立子句、各自陳述不同事實（參數不被調參／不存在調參這件事／樣本外標籤不提供保護），非同一命題鏈式疊套。與 (b) 完整版同理由，請風控一併確認 3 個獨立否定分佈於不同子句是否可接受，或要求拆句。
- 因果詞：「因此不存在」是有明確前提支持的因果（前提：參數是寫死常數、不受輸入影響 → 結論：無法「回頭調參」），非裸因果；「但也因為如此」承接的是同一組事實的另一面（樣本外只是位置），非另起爐灶的假因果。
- 措辭＝行為依據：逐字對應 D9 §3.1「`build_strategy(name)` 不接受 request 參數，三支策略的參數全部是模組常數」與「out_of_sample 這個標籤在本系統中只代表『區段位置』，不代表『未被擬合污染』」；ADR-0006 Consequences 亦已將此列為強制顯示事實。

### 備案（改用語族字面版）

> 本系統的三支策略參數是寫死的常數，不會依你的任何輸入調整，樣本外資料因此不曾被用來回頭調參數。但『樣本外（out-of-sample）』在本系統裡只代表資料的時間區段位置，本系統無法用這個標籤保證你沒有自己反覆試過多種設定、事後才挑一次結果（詳見選擇偏誤揭露）。

**逐條自檢**：語族含「本系統無法」；否定層數：「不會依…調整」1、「不曾被用來」1、「無法用這個標籤保證」1，仍 3 個獨立否定，判斷同主案；因果詞判斷同主案；措辭＝行為依據同主案。

---

## (d) 422 樣本不足說明句

### 我的判斷（先說結論）

**六段既有訊息（`LOW_ROUND_TRIPS_MESSAGE`／`LOW_WIN_TRIPS_MESSAGE`／`LOW_LOSS_TRIPS_MESSAGE`／`PB_NONE_MESSAGE`／`SYMBOL_MISMATCH_MESSAGE`／`INSUFFICIENT_DATA_MESSAGE`）可以原樣送審，只建議一處純詞彙置換（`PB_NONE_MESSAGE` 的「賠率」→「盈虧比」，理由見上文 §0），不建議改寫其餘結構或語氣。** 理由：
1. K1 已通過 qa-reviewer 審查，紀錄明載「sample_gate 訊息為中性事實敘述，無建議/揭露語句」——結構（哪個閘門、實際數字、`本次不寫入`）已經是乾淨的事實陳述，符合模組 docstring 自訂的標準（no recommendation, no action verb）。
2. 這六句本身就已經是「陳述量測結果＋陳述後果」的最小事實句，沒有語氣問題（沒有道歉語、沒有錯誤字眼、沒有行動指令），重寫反而有引入新風險的成本。
3. D9 §1.4 要求的「把 422 當常態路徑設計、清楚說明差距」——六句已經做到「說明差距」（`{count}` vs `{threshold}`），但沒有做到「讓使用者一望而知這是常態、不是系統壞掉」。這件事**不建議塞進六句本身**（會讓每句變長、且每個 reason_code 各自重複同一句免責語沒有必要），改用下面的**獨立框架句**，包在 API 回應或前端錯誤呈現的外層，六句作為「原因細節」子句接在後面。

### (d-1) 框架句（包住六段既有訊息的外層說明，非改寫既有訊息）— 主案

> 多數標的、多數期間的完整進出次數都達不到本系統設定的門檻；未達門檻時，系統會拒絕寫入這筆 Kelly 輸入，這是常見情況，不是系統出錯，也不代表這支策略或這個標的『不好』。這次沒有寫入的原因與實際數字如下：

（後接該次觸發的 reason_code 對應訊息，例如 `LOW_ROUND_TRIPS_MESSAGE`）

**逐條自檢**
- 語族：未直接用「不代表」/「本系統無法」字面，用「不是系統出錯」「不代表…『不好』」——語意上是同一家族的否定澄清句型。
- 否定層數：「不是系統出錯」1、「不代表…『不好』」1，共 2 層，分屬不同子句，符合 ≤2。
- 因果詞：無裸因果；「這是常見情況，不是系統出錯」是並列澄清，非因果推導。
- 措辭＝行為依據：對應 D9 §1.4「即使門檻只訂 20，`rsi_reversal` 需要約 14 年…UI 與文案必須把 422 當成常態路徑設計」與 D-3「422 是常態路徑而非錯誤」；沒有引用任何具體年限或機率數字（避免踩到 quant §1.4 表格的具體數字，雖然該表不在「選擇偏誤句」限制範圍內，但基於一致的保守原則，本句仍只做定性陳述）。

### (d-1) 備案（更短，適合按鈕旁常駐提示，不隨錯誤才出現）

> 回測帶入需要該策略在你指定的期間內有足夠多次完整進出；未達門檻會被拒絕寫入，這是常見情況、不是系統錯誤。

**逐條自檢**：否定層數 1（「不是系統錯誤」）；語族較弱（未用「不代表/本系統無法」，判斷此為常駐提示、篇幅優先精簡，語族要求可放寬）；因果詞：「需要…才能」是條件陳述非裸因果；措辭＝行為依據同主案。

### (d-2) 既有六句的建議微調（僅一處，且不動 `reason_code`／數字結構）

現行：
```
PB_NONE_MESSAGE = (
    "本次回測的樣本外勝率或賠率為 null（勝率 {win_rate}、賠率 {payoff_ratio}），"
    "沒有可寫入的數值。"
)
```
建議：把「賠率」全部換成「盈虧比」（與 `limits.py:829/837`、PRD FR-5/FR-6 用詞一致，同一個 `payoff_ratio` 欄位不該有兩個中文名）：
```
本次回測的樣本外勝率或盈虧比為 null（勝率 {win_rate}、盈虧比 {payoff_ratio}），沒有可寫入的數值。
```
其餘五句（`LOW_ROUND_TRIPS_MESSAGE`／`LOW_WIN_TRIPS_MESSAGE`／`LOW_LOSS_TRIPS_MESSAGE`／`SYMBOL_MISMATCH_MESSAGE`／`INSUFFICIENT_DATA_MESSAGE`）建議**原樣送審，不改一字**。

---

## (e) 「歷史頻率不等於下一筆交易的機率」正面處理句

要求：正面處理，不能只靠「歷史不代表未來」這句通用免責帶過（D9 §3.3 明文禁止的行為）；須說明「Kelly 的 p 是重複同分布下注的機率，等同於這一筆交易的勝率是偷換概念」。

### 主案

> 此處的『勝率』，是這段歷史樣本中獲利回合佔全部回合的比例——是對已經發生過的事做的統計整理，不代表你下一筆交易獲利的機率。歷史頻率是回顧過去，機率是對未來單一事件的陳述，本系統無法把兩者畫上等號。

**逐條自檢**
- 語族：含「不代表」「本系統無法」字面，完整對齊語族要求。
- 否定層數：「不代表」1、「無法把兩者畫上等號」1，共 2 層，分屬不同子句。
- 因果詞：無裸因果；本句是定義區分（回顧 vs 未來陳述），非因果推導，不會被誤讀成「因為 A 所以 B」。
- 措辭＝行為依據：對應 D9 §3.3 明文要求「Kelly 的 p 在數學上是『重複同分布下注的機率』，把它等同於『這一筆交易的勝率』是本功能最根本的偷換，揭露必須正面處理」；本句直接點出「歷史頻率」與「機率」是兩個不同的數學物件，而非只重複「僅供參考」的免責語。

### 備案

> 『勝率』是過去樣本中獲利回合的比例，只描述已經發生過的結果，不代表你下一筆交易獲利的機率；本系統無法、也不會把歷史頻率直接當成未來的機率使用。

**逐條自檢**：語族同主案；否定層數：「不代表」1、「無法、也不會」算同一子句內的並列否定（可視為 1 層的並列強化，或 2 個緊鄰否定詞），建議風控留意「無法、也不會」是否讀來比主案更重；因果詞、措辭＝行為依據同主案，額外多一句「也不會…使用」對應「系統不做任何統計校正」的一貫立場（與 (b) 呼應）。

---

## (f) 手動輸入來源句（manual 來源 p/b）

要求：使用者自報估計、系統未驗證，比照淨值自報先例語意（`net_worth.py`「系統不會自行調整這個數字」）。

### 主案

> 此勝率與盈虧比是你自行輸入的估計值，本系統不會查核其正確性，也不會自行調整你輸入的數字；若與實際交易狀況不符，不代表系統已為你確認過這組數字合理，本條上限的判定將隨之失真。

**逐條自檢**
- 語族：含「不代表」；未直接用「本系統無法」字面（此處是「不會」而非「無法」——語意上是『系統選擇不做』而非『系統做不到』，更準確：查核勝率/盈虧比正確性在技術上並非不可能，只是產品刻意不做，用「無法」會失真，故刻意保留「不會」而非套用「無法」語族，見措辭＝行為依據）。
- 否定層數：「不會查核」1、「不會自行調整」1（同子句並列，可視為 1 個否定動作的兩個對象，或 2 個獨立否定）、「不代表」1。若嚴格逐一計數為 3，分屬「查核」「調整」「確認合理」三個不同的系統行為承諾，非鏈式疊套。
- 因果詞：「若與實際交易狀況不符…判定將隨之失真」是條件句（若 P 則 Q），非裸因果宣稱，且與 `net_worth.py` 既有核可句式（「若此數字有誤，第 3 條上限將失去意義」）同構。
- 措辭＝行為依據：「不會查核」對應 ADR-0006/PRD 明文「手動輸入不設任何合理性防呆」；「不會自行調整」對應約束 6「全程式禁 min/max/round clamp」與既有慣例「系統不自行調整使用者輸入」；用「不會」而非「無法」是刻意選擇，避免暗示技術上做不到（措辭＝行為的另一面：不誇大限制、也不迴避這是產品選擇）。

### 備案（短版，適合欄位旁 tooltip）

> 此數字是你自行輸入的估計值，系統未驗證正確性，也不會自動調整；不代表系統已為你確認過這組數字合理。

**逐條自檢**：語族含「不代表」；否定層數 3（「未驗證」「不會自動調整」「不代表」），分屬三件事,同主案判斷；因果詞無;措辭＝行為依據同主案。

---

## (g) 過期／從未輸入 not_evaluable 理由句

依 PRD FR-3、K1 補審紀錄，需區分四種情境：①從未輸入（來源無關）；②manual 過期（錨 `updated_at`，有明確天數）；③backtest 過期（錨 `oos_end_date`，有明確天數）；④backtest 缺 `oos_end_date`（K1 修法後：`age_days=None`、`anchored_at=None`，一律視為過期，**不得**編造天數）。

### 我的判斷：④是否需要獨立句——**需要，且是必要而非可省**

理由：③的句子結構依賴 `{age_days}` 這個真實數字（「結束於 {age_days} 天前」）；K1 退修的結論明確是「缺錨一律 `expired`，`age_days=None`、`anchored_at=None`，不產生可誤讀成新鮮的數字」——若沿用③的模板去套一個 `None`，模板會產生「結束於 None 天前」或被迫留白（讀者會誤以為系統有算但漏顯示），兩者都違反「絕不捏造」的模組首要慣例，也違反 K1 已 CONFIRMED 的退修結論本身。④必須是一句**不含天數、明說『無法判定新鮮度、一律視為過期』**的獨立句。

### (g-1) 從未輸入 — 主案

> 此標的尚未輸入 Kelly 所需的勝率與盈虧比（可透過手動輸入或回測帶入取得），本條上限目前無法評估。

**逐條自檢**：語族「無法」；否定層數 1；無因果詞；措辭＝行為依據——對應現行 `_check_kelly_fraction` 的 `not_evaluable` 慣例（「輸入缺失 → not_evaluable，絕不捏造」），且明確與②③④的「已過期」文案不同句（PRD FR-3 明文要求）。

### (g-1) 備案

> 此標的目前沒有 Kelly 輸入，本條上限暫時無法評估。

**逐條自檢**：更短，語族「無法」；否定層數 1；其餘同主案。

### (g-2) manual 過期（錨 `updated_at`，有天數）— 主案

> 此標的的 Kelly 輸入（來源：手動輸入）已過期——上次更新於 {age_days} 天前，超過 30 天的新鮮期，本條上限暫不評估；請重新確認數字後更新。

**逐條自檢**：語族未用「不代表/本系統無法」（此句是狀態陳述＋行動指引，非否定澄清句型，語族要求在此類句子上不強制適用）；否定層數 0（全句無否定詞，符合『措辭=行為』的另一面：能用肯定句陳述事實時不刻意加否定）；無因果詞；措辭＝行為依據——`age_days` 直接對應 `book.py` builder 算出的欄位，`30 天` 對應 D-4 沿用淨值先例的常數，「暫不評估」對應 `not_evaluable` 的實際回傳值，「請重新確認數字後更新」對應手動輸入路徑允許使用者直接覆寫、不涉及回測重跑。

### (g-2) 備案

> 此標的的手動 Kelly 輸入已過期（{age_days} 天前更新），需重新確認後才能評估本條上限。

**逐條自檢**：更接近 PRD FR-3 草案原句；否定層數 0；其餘同主案。

### (g-3) backtest 過期（錨 `oos_end_date`，有天數）— 主案

> 此標的的 Kelly 輸入（來源：回測帶入）已過期——樣本外區段結束於 {age_days} 天前，超過 30 天的新鮮期，本條上限暫不評估；請重新執行回測並確認後更新。

**逐條自檢**：否定層數 0；措辭＝行為依據——「請重新執行回測並確認後更新」對應 D-3「帶入必須是使用者主動點擊」（不會自動重跑）；`oos_end_date` 用詞對應 D-4「backtest 系以 OOS 區段結束日計齡，不以執行時間計齡」，刻意不寫「回測執行於 {age_days} 天前」以避免讀者誤解成執行時間。

### (g-3) 備案

> 此標的的回測 Kelly 輸入已過期（樣本外區段結束於 {age_days} 天前），需重新確認後才能評估本條上限。

**逐條自檢**：同主案判斷，句式更貼近 PRD FR-3 草案原句。

### (g-4) backtest 缺 `oos_end_date`（一律視為過期，無天數可顯示）— 主案，**新增，K1 退修後新狀態**

> 此標的的 Kelly 輸入（來源：回測帶入）缺少樣本外區段結束日，本系統無法判定其新鮮度，一律視為已過期，本條上限暫不評估；請重新執行回測並確認後更新。

**逐條自檢**
- 語族：含「本系統無法」。
- 否定層數：「無法判定」1 層。
- 因果詞：無裸因果；「缺少…，本系統無法判定…，一律視為已過期」是清楚的條件鏈（缺 X → 無法判定 Y → 保守判定 Z），每一步都有明確依據，非跳躍式因果。
- 措辭＝行為依據：逐字對應 K1 補審紀錄的裁定——「移除 fallback，缺 anchor 一律保守判 `expired`、不回報偽造 age」；本句**刻意不出現任何天數佔位符**，因為 `age_days` 在此情境下就是 `None`，任何把它塞進句子的寫法都會產生假新鮮度或空白衝突。

### (g-4) 備案

> 此標的的回測 Kelly 輸入缺少可判斷新鮮度的日期，本系統一律視為已過期，需重新確認後才能評估本條上限。

**逐條自檢**：語族含「本系統」（弱化「無法」為「一律視為」，更直接陳述系統的保守判定行為而非強調限制）；否定層數 0（改用「一律視為已過期」的肯定判定句取代「無法判定」的否定句，兩種寫法互為對照，供風控選擇語氣輕重）；措辭＝行為依據同主案。

---

## 總覽：待風控裁定的公開分歧點

1. **「勝率」用詞張力**（§0）：沿用既有詞＋(e) 正面拆解，或連同 `limits.py:829/837` 改名——我建議前者（改名成本高、且會製造新舊詞並存的窗口期），但需風控明示接受。
2. **否定層數計算方式**：(b)/(c)/(f) 部分句子在「同一句內、分屬不同子句」的情況下有 3 個獨立否定，我的判斷是這與被退修的「三重疊套反轉」不同質，但每句都已備妥拆句/降層版本供風控直接選用。
3. **(d) 判斷**：六段既有 422 訊息**建議原樣送審**，僅 `PB_NONE_MESSAGE` 一處「賠率→盈虧比」的詞彙置換；新增獨立框架句 (d-1) 包在外層，不改動六句本身的結構與 `reason_code`。

## 交接

- 逐字定稿後 → risk-compliance-officer CONFIRMED/退修。
- CONFIRMED 後：dev-lead 落地 `sample_gate.py`（僅詞彙置換）、`app/api/kelly.py`（(a)/(b)/(g) 的欄位組裝與 `ci_includes_no_edge` 分支）、`KellyInputsSection.tsx`/`LimitsCheckList.tsx`（前端顯示，禁止自行改寫後端句，比照 NetWorthSection 三約束）。
- qa-reviewer：逐字守門測試（比照 `AS_OF_CALENDAR_UNCONFIRMED_STATEMENT` 等既有先例，字面含標點不得漂移）。

---

# 第二輪（2026-08-19）

依 `work/reviews/2026-08-19-C5-Kelly-文案批審.md` 五、退件對象逐條重擬。四項本輪任務中，(e) 全句重擬與 (h) 跨界回合排除新句屬「既有方案被 VETO／全新揭露事實」的卡關重審，依 `.claude/skills/creative-masters/SKILL.md` 走 Braintrust＋Rubin＋宮本茂三視角（見各節「發想過程」）；(f) 標點切開與 (d-1) 觸發範圍限縮是裁決已指名的精確結構修正（「一字不增」「精確綁定 reason_code」），屬機械執行裁決而非發想，不另跑五步流程，但仍附逐條自檢。

查證依據（本輪新讀）：`apps/stock-desk/backend/app/advice/limits.py:595-615`（`kelly_fraction`／`kelly_allowed_weight` 把 `ctx.win_rate` 直接代入 Kelly 公式的 `w`）、`apps/stock-desk/backend/app/kelly/attempts.py:108-148`（`append`／`k_observed` docstring：「every attempt that got past request validation」）、`apps/stock-desk/backend/app/api/kelly.py:416-591`（`import_kelly_input_from_backtest`：六種 `reason_code`〔`symbol_mismatch`／`insufficient_data`／`low_round_trips`／`low_win_trips`／`low_loss_trips`／`pb_none`〕的 `refuse()` 分支皆先 `_append_attempt` 才丟 422，六者無一例外會計入 `K_observed`）、`apps/stock-desk/backend/app/kelly/sample_gate.py:58-82`（六段既有訊息原文）、`apps/stock-desk/backend/app/kelly/models.py:50-55`（`KellyGateReasonCode` 六碼定義）、`apps/stock-desk/backend/app/api/common.py:19`（`PayloadStatus = Literal["ok", "insufficient_data"]`，即 `INSUFFICIENT_DATA_MESSAGE` 的 `{status}` 落地只會是 `"insufficient_data"` 這個英文字面）、`docs/adr/0006-…D.md` Consequences（跨界回合排除為強制揭露、不得包裝成保守作法）。

---

## 1.（e）歷史頻率≠下一筆機率句 — 全句重擬（前輪雙案 VETO）

### 發想過程（Braintrust＋Rubin＋宮本茂）

**定錨**
- 目標：讓使用者看懂「勝率」是歷史樣本頻率、不是下一筆交易的機率，並且正面承認——不是否認——本系統的 Kelly 計算把這個歷史頻率直接當成公式要的 p 使用；這是已知限制，不是已解決的問題。
- 受眾：正在看 Kelly 部位上限的一般投資人，無統計背景，但需要看懂「這個數字為什麼可能不準」。
- 限制：不能用「系統不會／無法把歷史頻率當機率用」這種與程式行為相反的陳述（兩案皆因此被 VETO）；不能用「信心水準／信賴區間／建議比例」；分母須指涉「納入計算的回合」而非「全部回合」；否定層數依新口徑（同命題疊套 ≤2 且結束在保守側；跨子句獨立否定 ≤4 且受詞具名）。

**三視角發想**
- **視角一：Braintrust（拆台既有方案）**——核心提問：「這句話讀完後，使用者會相信系統做了什麼？」前兩案讀完後，使用者會相信「系統很聰明地把歷史頻率和機率分開了」，但 `limits.py:611` 的事實是系統把兩者直接畫上等號去算上限。點子：句子的重心必須從「系統沒有做 X」搬到「系統事實上做了 Y，這是它的限制」。
- **視角二：Rubin（減法／誠實）**——核心提問：「拿掉修辭之後，剩下的事實夠不夠嚇人、夠不夠誠實？」不需要「無法畫上等號」這種把數學不等同包裝成能力限制的修辭，直接講「系統把這個位置放的就是歷史頻率」這件事實本身就足夠有力，不用再修飾。
- **視角三：宮本茂（把規則講成可操作的因果鏈）**——核心提問：「使用者下一步該把這個數字當什麼用？」規則要一步步交代：勝率是什麼（歷史統計）→ Kelly 的 p 理論上該是什麼（重複下注的機率）→ 系統實際放的是什麼（歷史頻率本身）→ 這代表什麼（限制，非已解決）。四步缺一步，使用者就會誤讀成「系統已經處理過這個落差」。

**Braintrust 互評**
- 「不代表你下一筆交易獲利的機率」（視角一提案）← 視角二：這句本身夠了，但如果只停在這裡，使用者仍不知道「那系統到底拿它做什麼用」，等於還是在講「不是什麼」而非「是什麼」。← 視角三：需要接一句明確講「系統拿它去做什麼」，否則因果鏈斷在中間。
- 「本系統無法把兩者畫上等號」（沿用前案殘影）← 視角一：這正是被 VETO 的句型，寫死不能再用。← 視角二：拿掉這句，換成直接陳述系統做了什麼，資訊量不減反增。

**最終提案（減法後）**
1. **穩健牌**：三件事按「勝率是什麼→Kelly 的 p 理論上是什麼→系統實際放的是什麼→這是限制不是已解決」的順序，用一個长句＋破折號承接，語氣是陳述事實而非道歉或辯護。
2. **險牌**（更直白，捨棄「Kelly 公式」的學術包裝，直接講「系統拿歷史數字當機率用」）：見備案。

**被捨棄的與原因**：「無法畫上等號」「無法確定是否真的等同」——凡是把「系統這樣做」講成「系統無法／不會這樣做」的句型，一律捨棄，因為這正是兩次 VETO 的根因。

### 主案

> 此處的『勝率』，是這段歷史樣本中——納入計算的回合裡，獲利回合所佔的比例；這是對已經發生過的事做的統計整理，不是你下一筆交易會不會獲利的機率。Kelly 公式所要求的 p，在數學上是『重複同一分布下注時獲勝的機率』；本系統在計算這條上限時，放進這個位置的正是前面這個歷史頻率，而不是這個機率——這是本功能目前的已知限制，不是已經被系統處理掉的問題。

**逐條自檢**
- 三件事全部成立且順序即因果鏈：①勝率＝歷史樣本頻率、非下一筆機率（「是對已經發生過的事做的統計整理，不是你下一筆交易會不會獲利的機率」）；②Kelly 的 p 是重複同分布下注的機率、本系統在該位置放的就是這個歷史頻率——**以肯定句陳述系統實際行為**（「本系統…放進這個位置的正是前面這個歷史頻率，而不是這個機率」），非「系統無法／不會」的能力語氣；③明說是已知限制、非已處理（「這是本功能目前的已知限制，不是已經被系統處理掉的問題」）。
- 分母：改「納入計算的回合裡，獲利回合所佔的比例」，不寫「全部回合」——對應 `attribute_round_trips` 完全包含歸屬、跨界與期末未平倉回合已被排除出這個分母（見下 (h) 句）。
- 語族：含「不是」（等價於「不代表」家族）；核心第②點刻意不用「無法／不會」字面，因為此處要陳述的正是系統**做了**這件事，套用否定能力語族反而會製造「系統沒做」的錯誤印象——與整批「措辭＝行為」原則一致。
- 否定層數：「不是你下一筆交易會不會獲利的機率」1（子句 A：勝率的定義）；「而不是這個機率」1（子句 B：p 與歷史頻率的對比，與 A 不同受詞——一個是「這個統計數字本身是什麼」，一個是「系統拿它跟公式的 p 對比」）；「不是已經被系統處理掉的問題」1（子句 C：限制的狀態）。共 3 個獨立否定，分屬 3 個不同子句、各自受詞具名（機率陳述本身／p 與頻率的對應關係／已處理與否的狀態），非同一命題鏈式疊套，符合「跨子句獨立否定 ≤4 且受詞具名」；子句內皆恰 1 層，符合「單一子句 ≤2」。
- 因果詞：無「因此／導致」等裸接續詞；四件事以分號與破折號並列陳述，非因果推論鏈。
- 措辭＝行為依據：`limits.py:604` `kelly_fraction(win_rate, payoff_ratio)` 回傳 `win_rate - (1-win_rate)/payoff_ratio`，`kelly_allowed_weight`（`limits.py:611`）直接把 `ctx.win_rate`（即回合層歷史頻率）代入這個 `w`；「w 是 Kelly 公式要的 p」是該公式的標準寫法，代入的正是歷史頻率而非任何機率估計——句子如實描述此事，未誇大也未淡化。

### 備案（更直白版，捨棄「Kelly 公式」學術包裝）

> 此處的『勝率』，是這段歷史樣本中，納入計算的回合裡獲利回合所佔的比例——這是回顧過去發生的事，不是預測你下一筆交易會不會賺錢。Kelly 公式的計算需要的是『重複同一種下注反覆進行時獲勝的機率』；本系統目前的做法，是把剛才這個歷史頻率直接當成這個機率代入計算——這件事本身是本功能已知、尚未解決的限制。

**逐條自檢**
- 三件事同主案，順序相同；②同樣以肯定句陳述系統實際做法（「本系統目前的做法，是把剛才這個歷史頻率直接當成這個機率代入計算」），未使用能力否定語氣。
- 分母同主案處理。
- 語族：「不是」1 次（子句 A）；③句改用「已知、尚未解決的限制」——「尚未」屬弱否定但與②句「代入計算」非同一命題疊套，是獨立陳述系統狀態，判斷同主案的 ≤2 層規則不違反。
- 否定層數：「不是預測…」1（子句 A）；「尚未解決」1（子句 C，若計入「未」）。共 2 個獨立否定，比主案更保守，供風控在「用詞直白程度」上與主案比較選用。
- 因果詞：無裸因果。
- 措辭＝行為依據同主案；「直接當成這個機率代入計算」用詞比主案「放進這個位置的正是」更口語，风控可依語感偏好選擇。

**建議**：主案在學術準確度（區分「Kelly 公式的 p」與「歷史頻率」兩個物件）上更精確，且與既有定稿（(a)(b)(c) 皆用「Kelly」「f\*」等術語不迴避技術詞）語族一致，優先推薦主案；備案供風控挑選語感直白版本。

---

## 2.（f）手動輸入來源句 — 主案標點切開（三層邏輯分離，內容一字不增）

裁決指名缺陷：「系統未確認過這組數字」是無條件事實，卻寫在「若與實際狀況不符」的條件子句之下（作用域歧義）。修法方式僅調整標點與子句順序，**不增刪任何字元**；「不會」沿用（風控已背書，`net_worth.py:41` 先例）。

### 原文（前輪主案，逐字列出以便比對）

> 此勝率與盈虧比是你自行輸入的估計值，本系統不會查核其正確性，也不會自行調整你輸入的數字；若與實際交易狀況不符，不代表系統已為你確認過這組數字合理，本條上限的判定將隨之失真。

### 主案（第二輪，標點切開三層）

> 此勝率與盈虧比是你自行輸入的估計值，本系統不會查核其正確性，也不會自行調整你輸入的數字；不代表系統已為你確認過這組數字合理；若與實際交易狀況不符，本條上限的判定將隨之失真。

**逐條自檢**
- 一字不增查證：逐字比對前後兩版，字元集合完全相同（「若與實際交易狀況不符」與「不代表系統已為你確認過這組數字合理」互換順序，原本用逗號連接「若…不符」與「不代表…合理」的地方改為分號分隔），僅語序與標點（一個逗號→分號）變動，**無新增或刪除任何字**。
- 三層邏輯分離：①自報事實（「此勝率與盈虧比是你自行輸入的估計值，本系統不會查核其正確性，也不會自行調整你輸入的數字」）；②系統未確認（無條件——「不代表系統已為你確認過這組數字合理」，現在以分號獨立成句，不再落在「若」的作用域內，無論數字是否與實際狀況相符，這句話永遠成立）；③若不符則失真（條件——「若與實際交易狀況不符，本條上限的判定將隨之失真」，只有這句仍帶條件）。
- 「不會」保留：兩處「不會」（查核／調整）字面未動，對應風控背書理由（查核技術上可行、產品刻意不做，「無法」會誇大限制）。
- 否定層數：「不會查核」「不會自行調整」（同子句，2 個動作各 1 個「不會」，受詞分別是「查核正確性」與「調整輸入」，各自具名）＋「不代表」（獨立子句，受詞是「系統已確認合理」）＝ 3 個跨子句獨立否定、受詞皆具名，符合 ≤4；內容與否定詞本身較前輪未變動，僅語序調整，故沿用前輪風控「(f) 3 通過」的既有判斷。
- 因果詞：「若…不符，…將隨之失真」是條件句（若 P 則 Q），非裸因果，且僅此條件句保留條件關係，與②的無條件事實已用分號隔開，不再共享同一因果／條件範圍。

### 備案（破折號版，供風控挑選視覺層次）

> 此勝率與盈虧比是你自行輸入的估計值，本系統不會查核其正確性，也不會自行調整你輸入的數字——不代表系統已為你確認過這組數字合理；若與實際交易狀況不符，本條上限的判定將隨之失真。

**逐條自檢**：與主案字元集合相同（僅第一個分號改破折號），三層邏輯分離、否定層數、因果詞判斷均同主案；破折號版在視覺上更強調②是獨立於③的無條件轉折，主案分號版三層更平權並列——差異僅在標點語氣強弱，內容無差異，供風控依畫面排版偏好選用。

（備案短版 tooltip 句已於前輪 CONFIRMED，本輪不重審，原樣沿用：「此數字是你自行輸入的估計值，系統未驗證正確性，也不會自動調整；不代表系統已為你確認過這組數字合理。」）

---

## 3.（d-1）422 框架句 — 觸發範圍限縮＋補「已計入 K_observed」告知句

裁決指名兩個缺陷：①「達不到門檻是常見情況」的安撫語不得套用到 `symbol_mismatch`（前端程式錯誤）與 `insufficient_data`（回測未產出可用結果），只能綁定三個樣本量 `reason_code`；②422 畫面須告知「這次被拒的嘗試已被記錄、並計入 K_observed」。

查證：`kelly.py:457-482` 的 `refuse()` 對六種 `reason_code`（`symbol_mismatch`／`insufficient_data`／`low_round_trips`／`low_win_trips`／`low_loss_trips`／`pb_none`）一視同仁，全部先 `_append_attempt` 記錄才丟 422——**六碼全部會計入 K_observed，不是只有三個樣本量碼**。因此本輪把 (d-1) 拆成兩個獨立元件：

- **元件 A（樣本量框架句）**：只在 `reason_code ∈ {low_round_trips, low_win_trips, low_loss_trips}` 時顯示，內容沿用前輪文字（裁決未指出這段文字本身有誤，只指出觸發範圍過寬）。
- **元件 B（K_observed 告知句）**：**六種 `reason_code` 皆顯示**，因為六者皆會被記錄計入 K_observed，這是措辭＝行為的直接要求——若只在三個樣本量碼顯示，`symbol_mismatch`／`insufficient_data`／`pb_none` 被拒的嘗試一樣會推高使用者的 K_observed，卻不會被告知，反而製造新的資訊落差。

### 元件 A：樣本量框架句（僅三個樣本量 reason_code 顯示）— 主案（沿用前輪文字，未改一字）

> 多數標的、多數期間的完整進出次數都達不到本系統設定的門檻；未達門檻時，系統會拒絕寫入這筆 Kelly 輸入，這是常見情況，不是系統出錯，也不代表這支策略或這個標的『不好』。這次沒有寫入的原因與實際數字如下：

（後接該次觸發的 `reason_code` 對應訊息，例如 `LOW_ROUND_TRIPS_MESSAGE`）

**顯示條件（required，非文案本身，供 dev-lead／tech-architect 落地）**：`reason_code ∈ {"low_round_trips", "low_win_trips", "low_loss_trips"}` 時才顯示元件 A；`reason_code ∈ {"symbol_mismatch", "insufficient_data", "pb_none"}` 時**不得**顯示元件 A，畫面只顯示該 `reason_code` 對應訊息本身＋元件 B。

**逐條自檢**
- 觸發範圍：文字本身未改動（裁決未認定文字有誤），僅追加顯示條件限定於三個樣本量碼，`symbol_mismatch`／`insufficient_data`／`pb_none` 不再套用此安撫語——解除「以無關安撫語粉飾異常」的問題。
- 語族：「不是系統出錯」「不代表…『不好』」，語意上是「不代表」家族的否定澄清句型。
- 否定層數：「不是系統出錯」1、「不代表…『不好』」1，共 2 層，分屬不同子句，符合 ≤2；與前輪判斷一致，內容未變動故不重審否定內容本身，僅重審觸發範圍。
- 因果詞：無裸因果，「這是常見情況，不是系統出錯」為並列澄清。
- 措辭＝行為依據：`sample_gate.py` docstring「A refusal is a **normal path**, not a fault」對應三個樣本量門檻（`MIN_OOS_ROUND_TRIPS`／`MIN_OOS_WIN_TRIPS`／`MIN_OOS_LOSS_TRIPS`），這段安撫語的事實基礎僅止於樣本量不足這件事，套到 `symbol_mismatch`（前端傳錯標的）或 `insufficient_data`（回測本身沒跑出結果）並不成立，故限縮顯示範圍後才與事實相符。

### 元件 B：K_observed 告知句（六種 reason_code 皆顯示）— 主案

> 這次被拒絕寫入的嘗試，已經被系統記錄下來，並計入 K_observed（詳見選擇偏誤揭露）。

**逐條自檢**
- 內容：對應裁決要求的措辭「這次被拒的嘗試已被記錄、並計入 K_observed」，一字不差地保留核心事實敘述，僅調整語序使其成為完整句。
- 語族：無否定詞，全句為肯定的事實陳述——符合「措辭＝行為」原則中「能用肯定句陳述事實時不刻意加否定」的一面（比照 (g-2)(g-3) 全句無否定詞的先例）。
- 否定層數：0。
- 因果詞：無。
- 措辭＝行為依據：`kelly.py:457-482` `refuse()` 對六種 `reason_code` 一律先呼叫 `_append_attempt`（`kelly.py:368-413`）才丟出 422，`_append_attempt` 呼叫 `attempts.append(record)`（`attempts.py:108-134`，docstring 明載「Called for every attempt that got past request validation, whatever the gate then decided」），而 `k_observed()`（`attempts.py:136-148`）的 `COUNT(*)` 對這張表的每一列（含 `outcome="rejected"` 的六種 `reason_code`）一視同仁——句子對六碼皆成立，無需為不同 `reason_code` 寫不同版本。
- 顯示範圍：六種 `reason_code` 皆顯示，緊接在該碼對應的既有訊息之後（元件 A 只在三碼時額外出現在元件 B 之前）。

### 元件 B 備案

> 系統已經記錄這次被拒絕的匯入嘗試，這筆記錄會被算入 K_observed（詳見選擇偏誤揭露）。

**逐條自檢**：內容同主案，「已經記錄」與「會被算入」語序調整；否定層數 0；其餘同主案。主案「已經被系統記錄下來，並計入」語氣更貼近既有 (b) 句「本系統」開頭語族，優先推薦主案。

### 六種 reason_code 畫面組裝示意（供 dev-lead／tech-architect 落地參考，非逐字定稿範圍）

| `reason_code` | 元件 A（安撫語） | 既有訊息 | 元件 B（K_observed 告知） |
|---|---|---|---|
| `low_round_trips` | 顯示 | `LOW_ROUND_TRIPS_MESSAGE` | 顯示 |
| `low_win_trips` | 顯示 | `LOW_WIN_TRIPS_MESSAGE` | 顯示 |
| `low_loss_trips` | 顯示 | `LOW_LOSS_TRIPS_MESSAGE` | 顯示 |
| `symbol_mismatch` | **不顯示** | `SYMBOL_MISMATCH_MESSAGE`（第 5 節重擬） | 顯示 |
| `insufficient_data` | **不顯示** | `INSUFFICIENT_DATA_MESSAGE`（第 5 節重擬） | 顯示 |
| `pb_none` | **不顯示** | `PB_NONE_MESSAGE`（第 5 節重擬） | 顯示 |

---

## 4.（h）跨界回合排除揭露句 — 新擬（C5 上線閘門，整批缺漏 1）

### 發想過程（Braintrust＋Rubin＋宮本茂，因屬全新揭露事實、無既有草案可修）

**定錨**
- 目標：把 ADR-0006 Consequences 強制項「排除跨界回合會優先移除存續較長的回合」照實寫成使用者看得懂的句子，且不得包裝成保守作法——quant 實測樣本僅 n=10，檢定力極低，不支持任何方向性結論。
- 受眾：同 (e)。
- 限制：**禁寫「排除是保守的」**或任何暗示排除方向對使用者有利／穩健的字眼；不得引用 D9 §3.3 第 6 點量級具體數字（該點 MVP 降為定性，另案列管待模擬表固化）；可用真實落地欄位 `oos_excluded_boundary_trips`／`oos_open_trip_at_end`。

**三視角發想**
- **視角一：Braintrust**——核心提問：「如果使用者只看標題不看內文，會不會誤以為『排除掉的都是可疑的』『排除是在幫我把資料變乾淨』？」點子：句子順序要先講「排除的機制」，再講「機制對統計的影響方向未知」，避免讀者在看到「排除」兩字時已經腦補成正面動作。
- **視角二：Rubin**——核心提問：「拿掉『保守』『穩健』這類修飾詞後，剩下的機制描述夠不夠讓人明白這是有代價的？」不需要替排除機制辯護，直接陳述「這個排除方式系統性地偏向拿掉存續較長的回合」這個事實就夠了，讀者自己能判斷有沒有影響。
- **視角三：宮本茂**——核心提問：「使用者需要知道『規則是什麼』還是『規則的後果是什麼』？」兩者都要：規則（哪些回合被排除、怎麼算）＋後果（此排除有方向性但方向未知）——只給規則不給後果，讀者無法評估風險；只給後果不給規則，讀者無法驗證。

**Braintrust 互評**
- 「這個排除方式相對保守」（初稿殘影，來自舊有直覺想安撫使用者）← 視角一：這正是裁決明文禁止的寫法，quant n=10 檢定力極低，講「保守」等於捏造一個站不住的正面結論。← 視角二：拿掉這半句，換成「目前的證據不支持任何方向的判斷」，資訊更誠實。
- 「跨界與期末未平倉回合會被排除」單獨成句、不提「優先移除存續較長的回合」← 視角三：漏掉這個機制細節，使用者無法理解為什麼這個排除「不是隨機的」，等於藏起 ADR 明文要求的強制揭露事實。

**最終提案**：機制（完全包含歸屬＋跨界／期末未平倉排除＋另計數）→ 系統性偏向（優先移除存續較長回合）→ 方向性結論（證據不支持任何方向）三段式，順序不可調換（先講規則、再講偏向、最後才講「我們不知道這偏向的後果」，避免讀者在看到「偏向」兩字時就自行腦補方向）。

**被捨棄的與原因**：任何形式的「這樣做是為了…」「這樣比較…」——只要暗示排除機制有正面意圖或正面效果，一律捨棄，因為這正是裁決要防的「包裝成保守作法」。

### 主案（定性版，不含數量占位符）

> 本系統計算樣本外（OOS）回合數時，只計入完整包含在樣本外區段內的回合；跨越樣本內、外邊界的回合，以及在樣本外區段結束時仍未平倉的回合，會被排除在外，並另行計數。這個排除方式，會優先排除掉存續時間較長的回合；這對統計結果會造成什麼方向的影響，目前的證據不足以支持任何方向的判斷。

**逐條自檢**
- 內容三件事全部成立：①「OOS 只納入完整包含於樣本外區段的回合」＝「只計入完整包含在樣本外區段內的回合」；②「跨越邊界與期末未平倉的回合被排除並另行計數」＝「跨越樣本內、外邊界的回合，以及在樣本外區段結束時仍未平倉的回合，會被排除在外，並另行計數」；③「此排除會優先移除存續較長的回合，對統計的影響方向,目前的證據不支持任何方向的判斷」＝「這個排除方式，會優先排除掉存續時間較長的回合；這對統計結果會造成什麼方向的影響，目前的證據不足以支持任何方向的判斷」。
- **未寫「排除是保守的」**或任何同義替換（「穩健」「安全」「有利」皆未出現）；「優先排除掉存續時間較長的回合」是中性機制陳述，不帶價值判斷字眼。
- 語族：句尾「目前的證據不足以支持任何方向的判斷」對齊「本系統無法確定」家族的精神（無法對方向下判斷），但因主詞是「證據」而非「本系統」，字面上未套用「本系統無法」——如需嚴格對齊語族字面，備案已改寫。
- 否定層數：全句僅「不足以支持」1 個否定，受詞具名（「任何方向的判斷」），遠低於 ≤4 上限，是本輪四句中否定密度最低的一句。
- 因果詞：無「因此／導致」等裸接續；三段以分號並列陳述機制與結論，不構成因果推論鏈（刻意不寫「因為優先排除較長回合，所以…」這種會暗示已知方向的因果句）。
- 措辭＝行為依據：`oos_excluded_boundary_trips`／`oos_open_trip_at_end`（`kelly.py:360-361`、`models.py:176-177`）為程式落地欄位，「完全包含歸屬」對應 `attribute_round_trips`／ADR-0006 D-6(b)「OOS 採完全包含歸屬、以 index 判定，跨界回合與期末未平倉回合排除且分別計數」；「優先排除較長回合」「證據不支持任何方向」逐字對應 ADR Consequences 修訂項與 D9 quant n=10 檢定力極低的註記，**未引用**該量級的具體倍數數字（1.73 倍），對齊審查三-3 已接受的「MVP 降為定性」條件。

### 備案（含數量占位符版，供 FR-5／tech-architect 補齊顯示欄位後採用；語族字面對齊「本系統」）

> 本系統計算樣本外（OOS）回合數時，只計入完整包含在樣本外區段內的回合；跨越邊界的回合（{oos_excluded_boundary_trips} 筆）與樣本外區段結束時仍未平倉的回合（{oos_open_trip_at_end} 筆），會被排除在外，另行計數。這個排除方式會優先排除掉存續時間較長的回合；本系統無法判斷這對統計結果的影響方向，目前的證據不支持任何方向的結論。

**逐條自檢**
- 三件事同主案；新增兩個占位符逐字對應 `oos_excluded_boundary_trips`／`oos_open_trip_at_end`（`models.py:176-177`）。
- **未寫「排除是保守的」**，同主案。
- 語族：句尾改「本系統無法判斷…方向」，字面對齊「本系統無法」家族。
- 否定層數：「無法判斷」1、「不支持」1，共 2 個獨立否定，分屬同一句子的前後兩個子句，受詞不同（「影響方向的判斷能力」／「結論的證據支持度」），符合 ≤4；因語意高度重疊（都在講「不知道方向」），也符合「同命題疊套 ≤2 且結束在保守側」（結束在「不支持任何方向的結論」，即保守側）。
- 因果詞：無裸因果。
- 措辭＝行為依據同主案，另加：本備案的數量占位符**尚未獲 FR-5 核准顯示**（審查「三、整批缺漏」第 2 點：D9 §3.3 第 3/4/7 點顯示欄位待 PM／tech-architect 補齊後隨 FR-5 句送審）——本備案僅供**該欄位核准顯示後**直接採用，若 FR-5 尚未核准逐檔顯示這兩個數字，本輪應採主案（定性版），不得先行把占位符數字塞進畫面。

**建議**：本輪送審主案（定性版），備案列管待 FR-5 欄位到位後另行送審，避免搶跑於 PM／tech-architect 尚未完成的欄位盤點。

---

## 5.（加碼）dev 退修訊息使用者可讀版 — 為 D5 落地鋪路

三則訊息由 dev-lead 落地（None／null 渲染、`{status}` 裸列舉、URL/body 行話為工程缺陷，非文案本身問題），本節僅提供**方向文案**，結構維持「reason_code＋事實敘述」，不加建議、不加行動指令（比照 sample_gate.py docstring「no recommendation, no action verb」）。

### 5-1 INSUFFICIENT_DATA_MESSAGE

現行：「回測結果狀態為 {status}，不是 ok，本次不寫入 Kelly 輸入。」——問題：`{status}` 落地只會是英文字面 `insufficient_data`（`PayloadStatus = Literal["ok", "insufficient_data"]`，`api/common.py:19`），把裸英文列舉值嵌進中文句子的主要子句，且讀者無法從「狀態為 insufficient_data，不是 ok」直接理解「這次回測到底發生了什麼」。

**主案**

> 這次回測沒有產出可用的樣本外結果，本次不寫入 Kelly 輸入（狀態代碼：{status}）。

**逐條自檢**
- 事實句主體不含裸英文列舉值——`{status}` 移到句尾括號，作為技術代碼附註，非句子的語法主詞或受詞。
- 否定層數：「沒有產出」1，受詞具名（「可用的樣本外結果」）。
- 因果詞：無。
- 無建議、無行動指令：只陳述「沒有產出結果」與「本次不寫入」兩個事實，不寫「請調整期間」等建議語（避免踩到 sample_gate.py 模組慣例與風控紅線）。
- 措辭＝行為依據：`review_result_status`（`sample_gate.py:106-117`）在 `status != "ok"` 時才觸發，即回測本身未能產出 `"ok"` 狀態的結果；「沒有產出可用的樣本外結果」如實描述這件事，未過度簡化或誇大。
- 備註：此分支目前 `{status}` 落地必為 `"insufficient_data"`（`PayloadStatus` 只有兩個值），故括號代碼實質上恒定；若未來 `PayloadStatus` 擴充其他值，此句結構仍成立，不需重寫。

**備案**

> 本次回測未能產出可用的樣本外（OOS）結果，因此本次不寫入 Kelly 輸入（狀態代碼：{status}）。

**逐條自檢**：內容同主案，插入「因此」連接兩個已確定為因果關係的事實（回測未產出結果 → 不寫入），此處「因此」有明確依據支撐（非裸因果），供風控挑選是否要顯式因果詞；否定層數 1（「未能」）；其餘同主案。

### 5-2 SYMBOL_MISMATCH_MESSAGE

現行：「路徑指定的標的為 {path_symbol}（{path_market}），回測請求的標的為 {body_symbol}（{body_market}），兩者不一致，本次不寫入。」——問題：「路徑指定的」是 URL path 參數的工程行話，一般使用者看不懂「路徑」在這裡指什麼。

**主案**

> 你目前操作的標的是 {path_symbol}（{path_market}），但這次回測請求裡指定的標的是 {body_symbol}（{body_market}）——兩者不一致，本次不寫入。

**逐條自檢**
- 去 URL/body 行話：「路徑指定的」→「你目前操作的」（對應該端點 `/{symbol}/import-backtest` 的路徑參數，即使用者當前所在頁面／操作的標的，語意對應但用使用者能懂的說法）；「回測請求的標的」保留「回測請求」四字（此為使用者能理解的操作概念——「這次送出的回測」——非工程術語，予以保留）。
- 否定層數：「不一致」可視為 1 個否定性陳述，受詞具名（「你操作的標的」與「回測請求標的」兩者的關係）。
- 因果詞：無。
- 無建議：僅陳述兩個標的不同，不寫「請確認你選的標的」等指令句。
- 措辭＝行為依據：`review_symbol_match`（`sample_gate.py:120-148`）比對的正是 URL path 的 `symbol`/`market` 與請求 body 的 `symbol`/`market`；path 參數在使用者操作情境下就是「使用者當前在看／在操作的標的」（前端組裝這個路徑時取的是使用者選定的標的），故「你目前操作的標的」是對 `path_symbol` 語意的忠實白話轉譯，非臆測。

**備案**（更貼近裁決原句方向：「你操作的標的與回測請求中的標的不一致」）

> 你操作的標的（{path_symbol}，{path_market}）與這次回測請求中的標的（{body_symbol}，{body_market}）不一致，本次不寫入。

**逐條自檢**：與主案語意相同，句型更緊湊（先並列兩個標的，再陳述不一致與後果），否定層數 1（「不一致」），其餘同主案；此版更貼近協調人加碼指示的原文方向，優先推薦此版。

### 5-3 PB_NONE_MESSAGE

現行：「本次回測的樣本外勝率或賠率為 null（勝率 {win_rate}、賠率 {payoff_ratio}），沒有可寫入的數值。」——問題：`{win_rate}`/`{payoff_ratio}` 其中之一為 Python `None` 時，`.format()` 會把它渲染成字面 `"None"`，與句中既有的 `"null"` 字面同框、皆為工程師術語；且 `review_estimates`（`sample_gate.py:151-164`）在「其中一項缺漏」與「兩項皆缺漏」時共用同一句，句子需涵蓋兩種情況而不指名哪一項缺漏（除非 dev-lead 另外決定拆成逐項顯示，屬程式邏輯決定，非本節文案範圍）。「賠率」→「盈虧比」置換方向照准。

**主案**（不嵌入可能為 None 的數值，改用「缺值」描述）

> 本次回測的樣本外勝率與盈虧比，其中至少一項沒有算出數值，沒有可寫入的完整配對，本次不寫入 Kelly 輸入。

**逐條自檢**
- 不含 `None`／`null` 字面：全句未出現任何英文字面或直接嵌入 `{win_rate}`/`{payoff_ratio}`，從根源避免 `.format()` 渲染 `None` 的風險——無論哪一項缺漏、或兩項皆缺漏，這句話都成立，不需要 dev-lead 為「只缺一項」與「兩項皆缺」寫兩套字串。
- 「賠率」→「盈虧比」：已置換，與 `limits.py:829/837`、PRD FR-5/FR-6 用詞一致。
- 否定層數：「沒有算出數值」1、「沒有可寫入的完整配對」1，共 2，同一命題（缺值→無法寫入）疊套但結束在保守側（「不寫入」），符合「同命題疊套 ≤2 且結束在保守側」。
- 因果詞：無裸因果，「其中至少一項沒有算出數值，沒有可寫入的完整配對」為並列陳述，非「因為…所以…」。
- 無建議：不寫「請重新執行回測」等行動指令（此點與既有六句「無行動指令」的模組慣例一致，且與 (d-1) 框架句衝突的風險——框架句已在外層處理「常見情況」的安撫語，六句本身維持純事實陳述）。
- 措辭＝行為依據：`review_estimates`（`sample_gate.py:159`）在 `win_rate is None or payoff_ratio is None` 時觸發，即「至少一項沒有算出數值」；「沒有可寫入的完整配對」對應 `KellyInputRecord` 需要 p 與 b 成對寫入的約束（ADR-0006「Kelly 輸入是配對，缺一不是較小的輸入，是沒有」）。

**備案**（保留「盈虧比／勝率」欄位名稱、仍不嵌入可能為 None 的原始值）

> 這次回測沒有算出完整的樣本外勝率與盈虧比配對（其中至少一項缺值），因此沒有可寫入 Kelly 輸入的數值。

**逐條自檢**：內容同主案，「缺值」置於括號補充說明哪個層次缺（配對層級，非欄位層級）；否定層數 2（「沒有算出」「沒有可寫入」），同命題疊套、結束保守側，同主案判斷；「因此」為有明確依據支撐的因果（缺值→無法寫入），非裸因果。

**致 dev-lead 的落地備註（非文案本身，供落地時參考）**：若日後產品需求要求逐項顯示「勝率：{有值/缺值}、盈虧比：{有值/缺值}」，這屬於訊息結構改變（從一句話變成條列），需要拆成新的訊息模板並另外送審——本節主案／備案皆假設維持現行「單句涵蓋兩種缺值情況」的結構，不預先假設 dev-lead 會如何重構程式邏輯。

---

## 第二輪待風控裁定事項彙總

1. **(e)**：主案（保留「Kelly 公式」學術用詞）與備案（更直白，捨棄公式包裝）二選一；兩者三件事皆成立、分母皆已改為「納入計算的回合」，差異僅在用詞直白程度。
2. **(f)**：分號版（主案）與破折號版（備案）二選一，內容完全相同，僅標點視覺層次不同。
3. **(d-1)**：本輪拆成元件 A（三碼限定）＋元件 B（六碼皆顯示），需風控確認「元件 B 六碼皆顯示」是否為正確理解（依 `kelly.py` 程式行為六碼皆計入 K_observed，故判斷六碼皆需告知）；若風控認為六碼皆顯示的資訊量對 `symbol_mismatch` 等情境是干擾而非必要揭露，需另行指示限縮範圍。
4. **(h)**：本輪送審**主案（定性版，不含數量占位符）**；備案（含 `oos_excluded_boundary_trips`／`oos_open_trip_at_end` 占位符）列管，待 PM／tech-architect 依審查「三、整批缺漏」第 2 點補齊 FR-5 顯示欄位後才可能採用，本輪不建議直接核可備案。
5. **5-1/5-2/5-3**：三則皆為「文案方向」，最終渲染安全性（避免 `None`/`null` 字面、`{status}` 是否仍用裸列舉、逐項缺值顯示與否）取決於 dev-lead 的程式落地方式；若 dev-lead 的實作與本節假設的訊息結構不同（例如改成逐欄位顯示），需回送本節重審對應句子。

## 交接（第二輪）

- 逐字定稿後 → risk-compliance-officer 第二輪 CONFIRMED／退修。
- (h) 句 CONFIRMED 前，C5 不得上線（審查「三、整批缺漏」第 1 點）。
- (d-1) 元件 A／B 的顯示條件（reason_code 分支）與 5-1/5-2/5-3 的落地方式 → dev-lead／tech-architect 依本節「措辭＝行為依據」與「致 dev-lead 的落地備註」實作，若程式邏輯與本節文案假設的訊息結構不同，回送 creative-lead 重審對應句子。
- qa-reviewer：本輪四項＋加碼三則的逐字守門測試比照第一輪已 CONFIRMED 八句的先例辦理，並新增「元件 A 顯示條件」「元件 B 六碼皆出現」的分支測試。

---

# 第三輪（2026-08-19）

依 `work/reviews/2026-08-19-C5-Kelly-文案批審.md` 第二輪批審逐條 1-B、3-B 重擬，小批共兩句，非全新揭露事實的自由發想，而是裁決已指名三要件／內容邊界的精確結構補件，屬機械執行裁決，不另跑 creative-masters 五步流程，但兩句皆附逐條自檢（含否定口徑、措辭＝行為依據）。

查證依據（本輪新讀）：`apps/stock-desk/backend/app/advice/limits.py:526-527`（`PortfolioContext.win_rate` 單一欄位、無論來源為 manual／backtest／backtest_overridden 皆走同一個欄位，`kelly_allowed_weight` 讀這個欄位時不再分辨來源）、`limits.py:607-615`（`kelly_allowed_weight` 把 `ctx.win_rate` 直接代入 `kelly_fraction` 的 `w`，即 Kelly 公式的 p）、`apps/stock-desk/backend/app/kelly/models.py`（`KellySource = Literal["manual", "backtest", "backtest_overridden"]` 定義；`KellyInputRecord.overriding()` classmethod docstring：`backtest_overridden` = 「``previous`` with a hand-typed pair on top of it」，即覆寫後的**有效值是使用者手鍵輸入**，`backtest_win_rate`/`backtest_payoff_ratio` 才是原始回測數字、且不受覆寫影響）、`apps/stock-desk/backend/app/api/kelly.py:536-569`（`fraction` 非有限區間時，先以 `reason_code=None` 呼叫 `_append_attempt` 記錄一筆 `outcome="ok"` 的嘗試，才拋出 500；`KellyInputRow` 從未被寫入 store）、`models.py` `KellyAttemptRecord._reason_code_matches_outcome`（`outcome` 為 `"ok"` 時不得帶 `reason_code`，故此路徑的 attempts 列 `outcome="ok"`，不是 `"rejected"`）。

## 1.（e-manual）manual／backtest_overridden 來源的勝率／盈虧比揭露句

### 主案

> 此處的『勝率』與『盈虧比』，是你自行輸入（或以回測帶入後自行覆寫）的估計值，不是本系統量測所得的數字。Kelly 公式所要求的 p，在數學上是『重複同一分布下注時獲勝的機率』；本系統在計算這條上限時，放進這個位置的正是你剛才自己輸入（或覆寫）的這個數字——這是本功能目前的已知限制，不是已經被系統處理掉的問題。

**逐條自檢**
- **三要件全部成立**：
  ①自報事實——「是你自行輸入（或以回測帶入後自行覆寫）的估計值，不是本系統量測所得的數字」：涵蓋 manual（從頭手鍵）與 backtest_overridden（先帶入後覆寫）兩種來源，措辭對稱「自行輸入」／「自行覆寫」，未把 backtest_overridden 誤寫成單純的 backtest。
  ②p 位置放的正是自報數字——「Kelly 公式所要求的 p，在數學上是『重複同一分布下注時獲勝的機率』；本系統在計算這條上限時，放進這個位置的正是你剛才自己輸入（或覆寫）的這個數字」：**以肯定句陳述系統實際行為**（「放進…正是…」），全句無「無法」「不會」等能力語氣字面，符合裁決「不得寫成無法/不會能力語氣」的要求。
  ③已知限制非已處理——「這是本功能目前的已知限制，不是已經被系統處理掉的問題」：與 (e) 定稿收尾逐字相同（此收尾句本身是通用陳述、非「歷史樣本／歷史頻率」等只對 backtest 為真的措辭，故沿用不算逐字複製受限部分）。
- **否定口徑**：「不是本系統量測所得的數字」1（子句 A：自報事實的定義）、「不是已經被系統處理掉的問題」1（子句 C：限制的狀態）。共 2 個獨立否定，分屬 2 個不同子句，各自受詞具名（量測來源／處理狀態），符合「跨子句獨立否定 ≤4」；核心第②句刻意**完全不用否定詞**，避免把「系統做了這件事」誤讀成「系統沒做什麼」，是本句否定口徑的關鍵設計（否定只用在定義與狀態上，行為描述本身全程肯定句）。
- **與 (e) 語族對齊、不逐字複製 backtest 專屬措辭**：句式結構（自報/量測定義 → p 的數學定義 → 系統實際放入的內容 → 已知限制非已處理）與 (e) 定稿平行；但第一句刻意不寫「歷史樣本」「歷史頻率」「回合」等只對 `source == "backtest"` 為真的詞（manual 輸入沒有回合、沒有樣本），改用「你自行輸入（或以回測帶入後自行覆寫）的估計值」精確描述 manual／backtest_overridden 的共同性質。
- **與 (f) 分工、不重複**：(f) 講的是「系統未查核這組數字的正確性、不會自行調整」（資料驗證面）；本句完全不提查核／調整，只講「Kelly 的 p 這個位置放的是什麼」（公式代入面）——兩句分別回答「這數字系統有沒有把關」與「這數字在計算裡被當成什麼用」，無內容重疊，可同畫面並存而不冗餘。
- **措辭＝行為依據**：`limits.py:526-527` `PortfolioContext.win_rate` 是單一欄位，`kelly_allowed_weight`（`limits.py:607-615`）讀取時不分辨 `source`，故無論 manual 或 backtest_overridden，`ctx.win_rate` 進入 `kelly_fraction` 的 `w`（即 Kelly 的 p）時都是使用者自己敲進去的數字，非系統量測；`models.py` `KellyInputRecord.overriding()` 的 docstring 明載 `backtest_overridden` 的有效 `win_rate`/`payoff_ratio` 是「hand-typed pair on top of」原始回測列，原始回測數字保留在 `backtest_win_rate`/`backtest_payoff_ratio`、不進入計算，如實支持「即使來源曾是回測，覆寫後放進 p 位置的仍是自報數字」這一陳述。

### 備案

> 這裡用來計算上限的『勝率』與『盈虧比』，是你自己輸入的估計值——如果這組數字原本來自回測帶入、後來又被你手動覆寫過，也是同一種情況；它不是本系統實測得到的數字。Kelly 公式的 p，在數學上指的是『重複同一分布下注時獲勝的機率』；本系統計算這條上限時，實際放進這個位置的，就是你剛剛自己輸入（或覆寫）的那個數字——這是本功能目前尚未解決的已知限制，並非系統已經處理過的狀況。

**逐條自檢**
- 三要件同主案：①「是你自己輸入的估計值……它不是本系統實測得到的數字」，並用破折號子句把 backtest_overridden 的情境單獨點名（「如果這組數字原本來自回測帶入、後來又被你手動覆寫過，也是同一種情況」），比主案的括號注記更明白地把兩個來源的等同關係講出來；②「本系統計算這條上限時，實際放進這個位置的，就是你剛剛自己輸入（或覆寫）的那個數字」——同樣全句肯定句，無「無法/不會」；③「這是本功能目前尚未解決的已知限制，並非系統已經處理過的狀況」，用「尚未解決」替換主案「不是已經被系統處理掉的問題」，語意相同、字面不同。
- 否定口徑：「它不是本系統實測得到的數字」1、「並非系統已經處理過的狀況」1，共 2，分屬子句 A／C，判斷同主案。
- 與 (e) 語族對齊、不複製 backtest 專屬詞：同主案判斷，備案額外把 backtest_overridden 的情境從括號拉出成獨立子句，語意更顯白但字數略增。
- 與 (f) 分工：同主案，全句不涉查核／調整。
- 措辭＝行為依據：同主案。

**建議**：主案較精簡、括號注記已足夠涵蓋 backtest_overridden，優先推薦；備案供風控在「backtest_overridden 情境是否需要獨立子句點名」上比較選用。

---

## 2. 3-B 非有限 f\* 區間 500 路徑告知句

### 主案

> 這次嘗試已經被系統記錄下來，並計入 K_observed（詳見選擇偏誤揭露）；這筆輸入本身，這次沒有被寫入。

**逐條自檢**
- 內容對應裁決要求的兩件事：①「這次嘗試已被系統記錄並計入 K_observed」——「這次嘗試已經被系統記錄下來，並計入 K_observed」；②「雖然這筆輸入沒有被寫入」——「這筆輸入本身，這次沒有被寫入」。
- **否定口徑**：「沒有被寫入」僅 1 個否定，受詞具名（「這筆輸入本身」），遠低於上限；不寫「被拒絕」「失敗」等字眼——本句全程不使用「拒絕」二字。
- **outcome 口徑查證**：`kelly.py:545-554` 對此路徑呼叫 `_append_attempt` 時傳入 `reason_code=None`；`models.py` `KellyAttemptRecord._reason_code_matches_outcome` 規定 `reason_code is None` 時 `outcome` 必為 `"ok"`（若寫 `"rejected"` 會被 model validator 擋下），故這一列 attempts 的 `outcome` 是 `"ok"`——**表示樣本量／估計配對閘門本身都已通過**，只是算出來的 f\* 區間非有限值，觸發的是寫入前的防禦性斷言（`math.isfinite` 檢查），不是任何一個 `KellyGateReasonCode` 閘門的拒絕；因此本句刻意不用「被拒絕」，只用中性的「沒有被寫入」描述結果，如實對應 `outcome="ok"` 但未落地成 `KellyInputRow` 的實際狀態。
- **與元件 B 措辭區分**：元件 B（422 路徑）定稿「這次被拒絕寫入的嘗試，已經被系統記錄下來，並計入 K_observed（詳見選擇偏誤揭露）」，主詞直接是「這次被拒絕寫入的嘗試」，把「被拒絕」放進主詞里；本句主詞是「這次嘗試」（不帶「被拒絕」修飾），並拆成兩個獨立子句分別陳述「已記錄且計入 K」與「這筆輸入沒有被寫入」，字面上與元件 B 無重疊子句，避免讀者把兩種路徑混為一談（一個是閘門擋下、一個是閘門通過後的防禦性中止）。
- 因果詞：無「因此/導致」等裸接續；分號並列兩件獨立事實。
- 措辭＝行為依據：`kelly.py:536-569`——`fraction.low`/`fraction.high` 非有限時，先呼叫 `_append_attempt`（`reason_code=None`）把這次嘗試寫進 attempts 表（`attempts.k_observed()` 之後的計數會含這一列），再拋出 `500 KELLY_NON_FINITE_INTERVAL_MESSAGE`；程式在此之後直接 `raise`，從未呼叫任何寫入 `KellyInputRow`／`store.put` 的路徑，故「這筆輸入本身，這次沒有被寫入」如實描述結果，且未使用「被拒絕」（該詞在本模組語意上專指 422 的六個具名閘門，與此路徑的 `outcome="ok"` 不符）。

### 備案

> 這次嘗試同樣已經被系統記錄，並計入 K_observed（詳見選擇偏誤揭露）；只是這一次，你想寫入的這筆勝率與盈虧比，並未被寫入。

**逐條自檢**
- 內容同主案：①②兩件事皆涵蓋；「並未被寫入」取代主案「沒有被寫入」，語意相同、字面不同。
- 否定口徑：「並未被寫入」1，判斷同主案；同樣全句不寫「拒絕」。
- 與元件 B 措辭區分：主詞「這次嘗試」＋「你想寫入的這筆勝率與盈虧比」，比主案更具體點出「被寫入的對象是勝率與盈虧比這組數字」，與元件 B「這次被拒絕寫入的嘗試」的主詞結構仍不同，判斷同主案。
- 措辭＝行為依據：同主案；「你想寫入的這筆勝率與盈虧比」對應 `_measured_columns` 算出的 `win_rate`/`payoff_ratio` 從未被組裝進 `KellyInputRow` 並呼叫 store 寫入這一事實。

**建議**：主案句式更精簡、與元件 B 的主詞結構差異更清楚，優先推薦；備案供風控在「是否需要具名『勝率與盈虧比』」上比較選用。

---

## 第三輪待風控裁定事項彙總

1. **(e-manual)**：主案（括號注記 backtest_overridden）與備案（獨立子句點名 backtest_overridden）二選一，三要件、否定口徑、與 (f) 分工判斷皆相同，差異僅在 backtest_overridden 情境的顯白程度。
2. **3-B 告知句**：主案（「這筆輸入本身，這次沒有被寫入」）與備案（「你想寫入的這筆勝率與盈虧比，並未被寫入」）二選一，內容與 outcome 口徑判斷相同，差異僅在受詞具體程度；顯示位置需 dev-lead／tech-architect 確認是否緊接在 `KELLY_NON_FINITE_INTERVAL_MESSAGE`（技術性 500 訊息本身，非本節文案範圍）之後同屏顯示。

## 交接（第三輪）

- 逐字定稿後 → risk-compliance-officer 第三輪 CONFIRMED／退修。
- (e-manual) CONFIRMED 前，`limits.py:837` 於 manual／backtest_overridden 來源不得出貨（第二輪 1-B 閘門延續）。
- 3-B CONFIRMED 後，C5 上線閘門「3-B 結案」視為解除（第二輪「C5 文案面整體狀態」項目②）。
- dev-lead：確認 (e-manual) 顯示條件綁定 `source in {"manual", "backtest_overridden"}`、與 (e) 的 `source == "backtest"` 互斥覆蓋全部三來源；3-B 句與 `KELLY_NON_FINITE_INTERVAL_MESSAGE` 的同屏排版。
- qa-reviewer：新增 (e) / (e-manual) 三來源分支測試（backtest／manual／backtest_overridden 各一）；3-B 句的 `outcome="ok"` 斷言測試（不得誤植為 `"rejected"` 分支）。

---

# 第四輪(2026-08-19)

依 `work/reviews/2026-08-19-C5-Kelly-文案批審.md` 第三輪批審「3. 本輪查證新發現」重擬 `KELLY_NON_FINITE_INTERVAL_MESSAGE`——原句從未經風控審查即已上線，帶三缺陷：①禁用字面「信賴區間」；②`{low}`/`{high}` 必渲染 `inf`/`-inf`/`nan` 英文字面；③顯示 f\* 端點卻無生效上限同屏。此句是**全新重擬**（非既有草案的機械修正），走 `.claude/skills/creative-masters/SKILL.md` 五步流程。

查證依據（本輪新讀）：`apps/stock-desk/backend/app/api/kelly.py:134-136`（原句常數，`.format(low=…, high=…)` 於 `kelly.py:566-568` 呼叫，會把 `math.inf`/`float("nan")` 渲染成 Python 字面）、`kelly.py:536-569`（此路徑先 `review_sample`／`review_estimates` 兩道閘門皆通過，`bootstrap_fraction_ci` 算出的 `fraction.low`/`fraction.high` 才在 `math.isfinite` 檢查失敗時觸發——即「閘門全過、寫入被防禦性斷言擋下」，且 `_append_attempt` 以 `reason_code=None` 記錄，之後才 `raise HTTPException(500, …)`，`logger.error` 已把 `low`/`high` 原始值寫進 log，`detail` 不需要再帶這兩個數字）、`kelly.py:328-334`（`_bootstrap_seed(spec_hash)` docstring：種子由 `spec_hash` 決定，「same spec **and** same bars」才會重現同一結果，同一次請求若立即重試、市場資料未變，會重現同一組非有限端點——這是判斷「是否要寫重試指引」的關鍵事實依據）。

## 發想過程（Braintrust＋Rubin＋宮本茂）

**定錨**
- 目標：把違規三缺陷全部移除，重擬為一句從根源不嵌數值、不用「信賴區間／信心水準」、不用「拒絕／失敗」、以「沒有被寫入」語族陳述的事實句；這是系統側罕見狀況（閘門全過、被防禦性斷言擋下），不安撫、不道歉、不建議；且此句會與已定稿的 3-B 句（「這次嘗試已經被系統記錄下來，並計入 K_observed（詳見選擇偏誤揭露）；這筆輸入，這次沒有被寫入。」）同屏，須規劃兩句的閱讀順序、避免逐字重複「沒有被寫入」的資訊。
- 受眾：同 (e)/(h)，但情境更陌生——這不是使用者輸入或操作造成的結果（三個樣本量閘門、配對閘門全部通過），措辭必須避免讓使用者誤以為「是我哪裡沒做對」。
- 限制：逐字比照落地條件 7（「信賴區間」不得與 f\* 區間同框）；比照 PB_NONE 定稿做法（數值從根源移除、改述而非嵌值，`.format()` 呼叫應與其引數一併移除，比照 5-3A 對 `sample_gate.py:163` 的處置）；比照第三輪查證發現(iii)——移除端點數值後，「顯示 f\* 端點卻無生效上限同屏」這個問題不再成立，但需在自檢中交代清楚；是否寫重試／回報指引，依紅線判斷、不得暗示重試必然成功。

**三視角發想**
- **視角一：Braintrust（拆台）**——核心提問：「使用者讀完這句話，會不會誤以為是自己的輸入有問題？」既有六段 422 訊息（樣本量不足等）的語感都圍繞「你的資料／設定沒有達到門檻」，但這個 500 路徑完全不是這回事——樣本量與估計配對兩道閘門都已通過，是系統內部算出的區間本身不是有限數。若沿用 422 句群的語感（例如暗示「常見情況」「未達門檻」），會誤導使用者往錯的方向理解成因。句子必須清楚只陳述「這次算出的區間不是有限數、超出可寫入範圍」，不影射任何使用者側的原因。
- **視角二：Rubin（減法／誠實）**——核心提問：「拿掉修飾之後，剩下的事實夠不夠誠實、夠不夠精簡？」不需要解釋「為什麼」會出現非有限值（那是 code 註解與 log 該管的技術細節，不是使用者需要的資訊，log 已經有 `low`/`high` 原始值），也不需要「這種情況很罕見」之類的安撫語（違反「不安撫」紅線）；只需要誠實地說「這次算出的區間，不是有限的數字，超出可寫入的範圍」這一件事實。
- **視角三：宮本茂（可操作的因果鏈）**——核心提問：「使用者讀完這句話，該對『這條上限』抱持什麼期待？」規則要交代「發生了什麼」（區間不是有限數、超出可寫入範圍）；「這代表什麼後果」（這次沒有數值被寫入）已經由即將同屏的 3-B 句涵蓋——若本句再完整交代一次後果，兩句合起來會顯得囉唆且資訊冗餘；本句只負責「發生了什麼」，把「後果」的收尾讓給 3-B，兩句合起來才是完整的因果鏈，不必每句各自完整。

**Braintrust 互評**
- 「f\* 信賴區間出現非有限值（下界 {low}、上界 {high}），本次不寫入 Kelly 輸入」（原句殘影）← 視角一：套用禁詞「信賴區間」，且把端點數字當成核心資訊呈現，正是被指名的缺陷，整句框架捨棄。← 視角二：`{low}`/`{high}` 一律非有限值，寫進句子除了讓 `.format()` 渲染出英文字面，對使用者沒有任何可操作的資訊量（inf/-inf/nan 不是使用者能理解或核對的東西），從根源不嵌是唯一選項。
- 「……本次不寫入 Kelly 輸入」（比照既有 422 六句的收尾慣例）← 視角三：這個收尾與 3-B 定稿句「這筆輸入，這次沒有被寫入」高度重疊，兩句同屏會讓「沒有被寫入」這件事被講兩次；應該把「這件事沒被寫入」的最終陳述讓給已定稿、無法再改的 3-B 句，本句只負責描述現象本身。← Rubin：同意，這是減法——不必讓每句話各自完整，讓兩句合起來完整、不重複，讀起來更乾淨。
- 「這種情況非常少見／系統會自動記錄以供排查」等安撫語（曾被列入候選）← 視角一／視角二一致否決：紅線明寫「不安撫不道歉不建議」，且「非常少見」屬於系統側自我保證的語氣，與整批禁止「保證性、誇大」語氣的紅線牴觸，捨棄。

**最終提案（減法後）**：只保留「這次算出的 f\* 區間，其上下界都不是有限的數字，超出本系統可寫入的範圍」這一個事實句，**不再自行重複「沒有被寫入」的收尾**（讓給同屏的 3-B 句），不含任何數值、不含「拒絕／失敗」、不含「信賴區間／信心水準」；是否加重試／回報指引另立一個備案供風控選用，主案不建議加入（理由見下方自檢的 `_bootstrap_seed` 依據）。

**被捨棄的與原因**：任何形式的「這種情況很少見」「系統已自動處理」——屬於安撫語，違反紅線；「請重新執行回測」——若無條件寫這句，等同暗示重試能解決問題，但 `_bootstrap_seed(spec_hash)` 是確定性種子，同一 spec、同一批市場資料（bars 未變）重試會重現同一組非有限端點，無條件建議重試會誤導使用者以為換一次就可能不同。

## 主案（推薦）

> 本次計算出的 f\* 區間，其上界與下界都不是有限的數字，超出本系統可寫入的範圍。

**逐條自檢**
- **從根源不嵌數值**：全句無任何佔位符，`{low}`/`{high}` 完全移除；`fraction.low`/`fraction.high` 的原始值仍由 `logger.error`（`kelly.py:555-563`）寫入 log，不會遺失可排查資訊——比照 PB_NONE 定稿「數值移 log、訊息不帶佔位符」的做法。**落地備註（非文案本身，供 dev-lead 參考）**：既然常數不再需要插值，`kelly.py:566-568` 的 `KELLY_NON_FINITE_INTERVAL_MESSAGE.format(low=fraction.low, high=fraction.high)` 呼叫應同步移除、直接 `detail=KELLY_NON_FINITE_INTERVAL_MESSAGE`，比照第一輪落地條件 15／5-3A 對 `sample_gate.py:163` `.format()` 呼叫與引數同步移除的處置。
- **禁用字面**：未出現「信賴區間」「信心水準」；「f\* 區間」是既有已核可的中性名稱（(a-1)/(a-2) 定稿先例，僅「95% 」需搭配名目覆蓋率語境時才需迴避，本句不涉及覆蓋率宣稱，單純的「區間」一詞可用）。
- **不寫「拒絕／失敗」**：全句無「拒絕」「失敗」「錯誤」等字眼；本句刻意不落入既有 422 六句「未達門檻」的語感（那三句 421 對應的是使用者側樣本量不足，這裡是系統內部算出的區間本身不是有限數，兩者是不同的失敗模式，措辭不得混同——見 Braintrust 視角一）。
- **不安撫不道歉不建議**：全句只有一個事實陳述句，無「這種情況很少見」「造成不便」「請聯繫客服」等語，亦不含任何行動指令；無退讓詞（「大致」「應該」等）弱化事實，也無誇大詞強化事實，符合「事實陳述即可」的要求。
- **否定層數**：0（全句無否定詞）——比照 (g-2)/(g-3)/元件 B 全句無否定詞的先例，能用肯定句陳述事實時不刻意加否定。
- **因果詞**：無「因此／導致」等裸接續；兩個分句（「都不是有限的數字」「超出本系統可寫入的範圍」）並列陳述同一件事的兩個面向（數學性質／系統後果），非因果推論鏈。
- **f\* 端點與生效上限同屏（第一輪落地條件 6）問題自動解除**：本句已不顯示任何 f\* 端點數值，落地條件 6「凡顯示未套上限 f\*／區間之處，同畫面須顯示生效上限」的觸發前提（顯示端點）不再成立，故本句不需要、也不應該額外插入生效上限（`min(f*×0.25, 0.10)`）的文字——插入反而會讓一句單純的「寫入被擋下」通知句夾帶一個與本次事件無關的數字，此點請風控在裁決時一併確認「不顯示端點即不觸發條件 6」的判斷是否成立。
- **與 3-B 銜接、不重複「沒有被寫入」**：本句**刻意不含**「沒有被寫入」「本次不寫入」等收尾子句——這件事已由同屏的 3-B 定稿句（「……這筆輸入，這次沒有被寫入。」）承載；本句只負責交代「發生了什麼」（區間不是有限數、超出可寫入範圍），兩句合起來才完整交代「發生了什麼」＋「因此沒有被寫入、且已計入 K_observed」，不逐字也不逐概念重複。**閱讀順序**：本句在前（描述發生什麼），3-B 句在後（補計數揭露＋收尾「沒有被寫入」），見下方組合預覽。
- **措辭＝行為依據**：`kelly.py:536-569`——`review_sample`／`review_estimates` 兩道閘門皆已通過（否則會在此之前就 422 refuse，不會走到這裡），`bootstrap_fraction_ci` 算出的 `fraction.low`/`fraction.high` 未通過 `math.isfinite` 檢查時才觸發本句，故「超出本系統可寫入的範圍」如實描述觸發條件，未誇大、未影射使用者側原因；`_append_attempt(…, reason_code=None, …)` 早於 `raise` 且獨立寫入，`KellyInputRow` 從未被組裝或寫入 store，如實支持（配合 3-B）「這次沒有被寫入」的最終事實。

## 備案（自我完整版——含「沒有被寫入」，措辭與 3-B 不同，供風控在「兩句是否各自需要完整」上比較選用）

> 本次計算出的 f\* 區間，其上界與下界都不是有限的數字，超出本系統可寫入的範圍，這次沒有數值被寫入。

**逐條自檢**
- 前兩分句與主案完全相同，判斷同主案（不嵌數值、無禁用字面、無拒絕/失敗字眼、否定層數 0 → 此句因新增「沒有數值被寫入」故否定層數為 1，受詞具名「數值」）。
- **與 3-B 差異化用字**：3-B 定稿收尾為「這筆輸入，這次沒有被寫入」（主詞：這筆輸入）；本句收尾為「這次沒有數值被寫入」（主詞：數值，且語序不同——先講「沒有」再講「被寫入」的對象是「數值」而非「輸入」），避免逐字重複，但語意上仍與 3-B 重疊（兩句都在講「沒有被寫入」這件事），**兩句同屏會有輕微的概念性重複**——這是本備案相對主案的取捨代價，換取的好處是本句本身自成完整陳述，即使未來顯示位置調整導致 3-B 不與本句同屏，本句仍能單獨成立。
- 措辭＝行為依據：同主案，額外補「沒有數值被寫入」對應 `KellyInputRow` 從未被寫入 store 這一事實。

**建議**：主案（不重複收尾）在「與 3-B 組合後」的閱讀體驗更精簡、無冗餘，是本輪推薦版本；備案供風控在「該不該讓每句話各自完整、不依賴同屏搭配」這個設計取捨上比較選用。若風控選備案，兩句同屏會有輕微語意重疊，需請風控明示是否可接受。

## 是否加「重試／回報」中性指引——列為獨立選項，主案／備案皆不建議採用

> （可附加於主案或備案句尾，供風控參考／裁決）可以再試一次；若重複出現這個狀況，可回報。

**逐條自檢與理由**
- 內容要件容許「可再試一次」類中性指引「可有可無」，但**不得暗示重試必然成功**——查證 `_bootstrap_seed(spec_hash)`（`kelly.py:328-334`）：種子由 `spec_hash` 決定，docstring 明載「same spec **and** same bars」才會重現同一結果；也就是說，若使用者對同一標的、同一策略、同一參數立即重試，且市場資料未變（多數情況下短時間內確實不會變），會重現**同一組**非有限端點，重試不會有幫助。加上這句等於暗示「換一次可能就好了」，但實際上很可能不會——與紅線「不得暗示重試必然成功」的精神相牴觸，即使措辭本身沒有寫「一定會成功」，語用上「可以再試一次」仍隱含「值得一試」的期待，與已知的確定性種子事實不符。
- 「回報」半句本身無此問題（回報永遠是中性、無誤導的選項），但「可以再試一次」與「若重複出現……可回報」綁在同一句時，前半句的誤導風險會連帶讓整句都需要風控更仔細審視。
- **本輪建議**：主案／備案皆**不附加**此句，維持純事實陳述（最貼合「不安撫不道歉不建議」紅線的版本）；若風控認為使用者仍需要一個明確的下一步（例如避免使用者反覆手動重試造成無謂的 K_observed 累積——重試本身若再次通過閘門到達此路徑，會再記一筆 attempts），可考慮改寫成不涉及「試不試」的中性描述，例如單獨一句「如持續發生，可回報」（不含「再試一次」），但此為額外選項，不在本輪主案／備案之內，留待風控指示是否需要。

## 與 3-B 句的組合預覽

**建議顯示順序**：本句（描述發生什麼）在前，3-B 定稿句（補 K_observed 計數揭露＋收尾「沒有被寫入」）在後。

**組合預覽一（主案＋3-B，推薦）**：

> 本次計算出的 f\* 區間，其上界與下界都不是有限的數字，超出本系統可寫入的範圍。這次嘗試已經被系統記錄下來，並計入 K_observed（詳見選擇偏誤揭露）；這筆輸入，這次沒有被寫入。

- 逐讀檢查：第一句只陳述現象（區間非有限、超出可寫入範圍），不提後果；第二句（3-B）承接「這次嘗試」的記錄與計數，並在句尾第一次、也是唯一一次交代「沒有被寫入」——兩句合起來因果鏈完整（發生了什麼 → 這次嘗試仍被記錄計入 K → 但這筆輸入沒有被寫入），且「沒有被寫入」全文只出現一次，無重複。

**組合預覽二（備案＋3-B，供比較）**：

> 本次計算出的 f\* 區間，其上界與下界都不是有限的數字，超出本系統可寫入的範圍，這次沒有數值被寫入。這次嘗試已經被系統記錄下來，並計入 K_observed（詳見選擇偏誤揭露）；這筆輸入，這次沒有被寫入。

- 逐讀檢查：「這次沒有數值被寫入」（第一句尾）與「這筆輸入，這次沒有被寫入」（第二句尾）語意重疊、字面不同，讀者會在兩句話裡各讀到一次「沒有被寫入」的陳述——內容不算逐字重複，但整段落閱讀節奏比預覽一囉唆，是備案的已知取捨代價（見備案自檢）。

## 待風控裁定事項

1. **主案 vs 備案**：是否接受「本句不重複交代『沒有被寫入』、完全依賴與 3-B 同屏搭配才完整」的設計（主案），或要求本句自身也完整（備案，但與 3-B 有輕微概念重疊）。
2. **f\* 端點與生效上限同屏（落地條件 6）是否確實解除**：本輪判斷「不顯示端點即不觸發條件 6」，請風控明示是否成立，或要求即使不顯示端點也要在此路徑補一句生效上限說明（若後者，需另擬句子、不在本輪範圍）。
3. **重試／回報中性指引**：本輪建議兩案皆不附加，僅列出附加版供參考；如風控認為使用者仍需要明確下一步，需另行指示措辭方向（例如是否採用不涉「試不試」的「如持續發生，可回報」變體）。

## 交接（第四輪）

- 逐字定稿後 → risk-compliance-officer 第四輪 CONFIRMED／退修；CONFIRMED 前，第三輪「3-B 上線閘門」不得結案（3-B 文案面已定稿，但落地畫面卡在本句未審）。
- dev-lead：`kelly.py:134-136` 常數改為本輪定稿逐字（含標點）；`kelly.py:566-568` 的 `.format(low=…, high=…)` 呼叫與其引數一併移除；本句與 3-B 句同屏排版、顯示順序（本句在前、3-B 在後）依本輪組合預覽落地；`logger.error`（`kelly.py:555-563`）保留現狀，不受本輪影響。
- qa-reviewer：新增本句逐字守門測試（含標點，比照既有先例）；新增斷言確認 `KELLY_NON_FINITE_INTERVAL_MESSAGE` 不含任何 `{`／`}` 佔位符字元、且渲染結果不含 `inf`／`nan`／`None`／`null` 等英文字面；新增本句與 3-B 句同屏／順序的顯示測試。

---

# 第五批（2026-08-19）

C5 文案面最後一批送審句：f\*≤0 專屬句、`quantity_range` 零額度句、FR-5 回測來源揭露框架（11 欄位）、FR-6 手動來源摘要句（含 `backtest_overridden` 變體）、FR-4 三態徽章、`BacktestReportView` 勝率口徑限定語。六類齊送後，連同前四批已定稿的 21 項，C5 文案面即補齊 `work/stock-desk-C5-Kelly-架構評估.md` 送審範圍 19-20 列出的全部項目。

查證依據（本批新讀）：`docs/adr/0006-…D.md` D-5（f\*≤0 的 violated＋專屬句＋`notional_caps` 排除規則）、`app/advice/limits.py:595-615`（`kelly_fraction`／`kelly_allowed_weight`，`full <= 0` 時 `fractional = max(full, 0.0) * cap = 0.0`）、`app/advice/limits.py:884-922`（`notional_caps`，`allowed is not None` 即写入 `caps["kelly_fraction"]`——**這是 D-5 要求排除、但今天程式碼尚未排除的分支，本批文案先行，留一則落地備註給 dev-lead**）、`app/advice/limits.py:960-1040`（`suggest_quantity_range` 的 `skipped_note` 現行只有「缺少可用資料」一種措辭）、`app/kelly/models.py:132-270`（`KellyInputRecord`／`overriding`：`backtest_win_rate`／`backtest_payoff_ratio` 永不覆寫）、`app/api/kelly.py:372-398`（`_measured_columns`：`oos_observations` 是 bars 數非回合數）、`app/backtest/episodes.py:117-131`（`RoundTripAttribution.excluded_boundary_trips`／`open_trip_at_end`）、`apps/stock-desk/frontend/app/backtest/BacktestReportView.tsx:27`（既有核可標籤「勝率」，fill 層，對應 `report.py` 的 `win_rate`／`_trade_stats`：「computed over **closing** (position-reducing) fills」）、`app/api/backtest.py:89-154`（除權息還原五句既有核可文案，依 `reason_code` 選句）。

---

## 1. f\*≤0 專屬句（第 5 條 violated 的專屬 detail）

### 發想過程（Braintrust＋Rubin＋宮本茂）

**定錨**
- 目標：`f* <= 0` 時第 5 條回 `violated`，需要一句專屬 detail，說明「以目前輸入計算，Kelly 公式不支持任何加碼部位」這個事實，且不得暗示應賣出／清倉／減碼——ADR-0006 Options 比較表已明寫這正是「f\*≤0 → 上限 0 + violated + 專屬句」這個選項本身的風險（「文案可能被讀成『叫我賣光』」），本句存在的目的就是防住這個誤讀。
- 受眾：正在看第 5 條上限、可能已持有部位的使用者；`violated` 這個狀態字本身（連同同一列的 `observed`／`threshold` 數字）已經會被 UI 呈現，本句的責任不是重複「已違規」這件事，而是解釋「違規」在這裡的真實意思——不是「部位太大」，是「這條上限本次算出的加碼額度是 0」。
- 限制：架構評估約束 11「專屬句禁『建議／應該／賣出／清倉／減碼至／最佳倉位』」；不得暗示系統對現有部位的處置意見（紅線）；措辭＝行為——`kelly_allowed_weight`（`limits.py:607-615`）在 `full <= 0` 時回傳 `max(full, 0.0) * cap = 0.0`，句子只能陳述「這次算出的加碼額度是 0」，不能陳述「你的部位超標」這種需要額外詮釋的說法。

**三視角發想**
- **視角一：Braintrust（拆台）**——核心提問：「使用者讀完這句話會不會覺得『系統要我把部位清空』？」既有 `_check_kelly_fraction` 的一般 `violated` 句式是「目前佔比 X% 已達或超過該上限 Y%」——這個句式套在 `Y=0%` 時會變成「目前佔比 X% 已達或超過 0%」，任何持有部位的人讀到都成立（因為任何正數都 >= 0），這句話對「有部位的人」和「還沒開始加碼的候選標的」講的是同一句，卻只有前者會把「達到 0% 上限」聯想成「應該歸零」。**這正是 ADR 選項表指名的風險**，一般句式在 `f*<=0` 這個特例下必須換掉，不能沿用。
- **視角二：Rubin（減法／誠實）**——核心提問：「拿掉『違規』的外殼，剩下的事實是什麼？」剩下的事實只有一件：Kelly 公式這次算出來的比例不是正值，所以「分數 Kelly 與硬上限取小」這個計算式的結果是 0——句子不需要用「違規」「超標」這種評價性詞彙包裝，直接講這個計算結果就是最誠實、資訊量最大的版本。
- **視角三：宮本茂（可操作的因果鏈）**——核心提問：「使用者讀完該對這條上限抱持什麼期待？」因果鏈要交代：勝率盈虧比算出的 f\* 不是正值 → 因此 Kelly 公式在這組輸入下不支持任何新增部位 → 這條上限本次可用的加碼額度是 0。但因果鏈到此為止，不能再往下延伸到「所以你應該怎麼處理手上的部位」——那是另一個問題，這條上限的職權範圍只到「能不能加碼」，管不到「該不該減碼」，句子必須明講這個職權邊界，否則讀者自己會把因果鏈接下去、接到紅線禁止的方向。

**Braintrust 互評**
- 「目前佔比 X% 已達或超過 0% 上限」（沿用一般句式套 0）← 視角一：這正是套上去會製造「叫我賣光」誤讀的版本，捨棄。← 視角二：把「已達或超過」這種比較句式用在門檻是 0 的地方，資訊量反而比直接講「額度是 0」更低、更容易誤讀，捨棄。
- 「Kelly 公式判斷這個標的沒有優勢」（曾列入候選）← 視角二：「沒有優勢」是對「真實世界」下判斷，但 `kelly_fraction` 只是對「輸入的 p、b 這兩個數字」做代數運算，句子不能宣稱系統對「優勢是否存在」下了判斷——這正是 (e)/(e-manual) 兩句才剛費了很大力氣要拆開的「歷史頻率≠機率」的同一種誇大，捨棄，改用「Kelly 公式算出的比例不是正值」這種只描述公式輸出、不對現實下判斷的說法。
- 「這條上限只限制加碼的額度，不對你目前已持有的部位提出任何處置意見」（收尾候選）← 視角三：這句話正是把因果鏈的職權邊界講清楚、擋住紅線指名的誤讀方向，保留；← 視角一：這句話同時也解釋了「為什麼即使你已經有部位，這句話也不是叫你賣」，兩個視角一致同意保留。

**最終提案（減法後）**：只保留「f\* 不是正值 → 因此 Kelly 公式在這組輸入下不支持任何加碼 → 這條上限本次的加碼額度是 0」這一條因果鏈，加一句收尾明講「只管加碼、不管既有部位」的職權邊界；捨棄任何形式的「已達／超過上限」比較句式（那是一般句式的專利，f\*≤0 分支不適用）。

### 主案（推薦）

> 以目前輸入的勝率與盈虧比計算，Kelly 公式算出的比例（f\*）不是正值：以目前輸入計算，Kelly 公式不支持任何加碼部位，分數 Kelly 與硬上限取小後，這條上限這次可用的加碼額度為 0%。本條上限只限制新增加碼的額度，不對你目前已持有的部位提出任何處置意見。

**逐條自檢**
- **必答事實**：逐字含任務指名的核心句「以目前輸入計算，Kelly 公式不支持任何加碼部位」，未改寫、未省略。
- **禁用字**：全句無「建議／應該／賣出／清倉／減碼至／最佳倉位」（架構評估約束 11 專屬禁詞），亦無「信賴區間／信心水準／即時」（通則禁詞）。
- **否定層數**：「不是正值」1（子句 A：f\* 的符號，數學事實）；「不支持」1（子句 B：這個符號對『能不能加碼』的涵義）；「不對……提出任何處置意見」1（子句 C：本條上限的職權邊界）。共 3 個獨立否定，分屬 3 個不同子句、各自受詞具名（f\* 的符號／加碼能力／處置意見的職權範圍），非同一命題鏈式疊套，符合「跨子句獨立否定 ≤4 且受詞具名」；每個子句內都恰 1 層，符合「單一子句 ≤2」。
- **因果詞**：無「因此／導致」等裸接續；「Kelly 公式算出的比例（f\*）不是正值：以目前輸入計算，Kelly 公式不支持任何加碼部位」用冒號承接同一件事的兩個說法（數學結果／對加碼能力的涵義），不是跳躍式因果推論。
- **不暗示應賣出／清倉（紅線）**：全句不出現「達到」「超過」「已違規」等比較字眼去對照使用者目前的持股，只講「這次算出的加碼額度是 0」這個計算結果本身；收尾句「不對你目前已持有的部位提出任何處置意見」正面畫出職權邊界，直接對應 ADR-0006 Options 表指名的風險（「文案可能被讀成『叫我賣光』」）。
- **措辭＝行為依據**：`app/advice/limits.py:595-604`（`kelly_fraction`）在 `full <= 0` 時回傳非正值；`kelly_allowed_weight`（`limits.py:607-615`）對此取 `max(full, 0.0) * budget.kelly_fraction_cap`，`full<=0` 恆得 `0.0`，`min(0.0, budget.kelly_position_cap)` 仍為 `0.0`——「這條上限這次可用的加碼額度為 0%」逐字對應這個計算結果；「不對……持有的部位提出任何處置意見」對應 `_check_kelly_fraction` 本身只回傳 `(status, detail, observed, threshold)` 四元組，不呼叫任何 `suggest_quantity_range` 或賣出邏輯，本條檢查函式在程式碼層級確實不涉及「處置」。
- **與既有 21 項不重複**：本句是全新的 `violated` 分支專屬 detail，一般句式（`detail_head` + 「已達或超過該上限」）與 (e)/(e-manual)/(g) 系列皆未涵蓋 `f*<=0` 這個特例，無重疊。

### 備案（短版，不含職權邊界收尾句，供風控在篇幅與是否需要額外收尾句之間取捨）

> 以目前輸入的勝率與盈虧比計算，Kelly 公式算出的比例（f\*）不是正值：以目前輸入計算，Kelly 公式不支持任何加碼部位，這條上限這次可用的加碼額度為 0%。

**逐條自檢**：前兩句與主案逐字相同，判斷同主案；否定層數降為 2（僅子句 A、B，無收尾的職權邊界句）；**風險提示**：省略收尾句後，本句本身不再正面畫出「只管加碼、不管既有部位」的邊界，若畫面上與（下方任務 2 的）`quantity_range` 零額度句不同屏，本句單獨出現時「不暗示應賣出」這條紅線的把關會變弱——建議僅在與任務 2 的完整版同屏顯示時使用備案，否則採主案。

---

## 2. `quantity_range` 零額度句（額度欄位的零值說明，與任務 1 分工）

分工：任務 1 的句子回答「為什麼這條上限這次不給加碼額度」（第 5 條 `violated` 的理由）；本句回答「這條上限的 0 額度，對『可加碼股數區間』這個欄位造成什麼影響、為什麼跟一般的『缺資料』不是同一件事」。依 D-5：`f*<=0` 時 `notional_caps` 不放入 `kelly_fraction` 鍵，理由是「0 額度作 binding cap 會推導出清倉股數」——今天的 `notional_caps`（`limits.py:918-920`）尚未做這個排除（`allowed is not None` 時就寫入，`0.0 is not None` 為真），這是本句需要搭配的一則 dev-lead 落地缺口，見批末落地備註。

排除後，`suggest_quantity_range` 現有的 `skipped_note`（`limits.py:1008-1013`：「以下上限本次缺少可用資料，未參與這個區間的計算」）用在這裡會是不實陳述——不是缺資料，是這條上限已經算出結果、結果是 0，且這個 0 被刻意排除在區間計算之外以避免推導出清倉股數。本句就是要填這個空隙的專屬措辭。

### 主案（完整版，可獨立顯示於區間說明區）

> 「分數 Kelly 部位上限」這次計算出的加碼額度是 0（原因見上方第 5 條的說明，不是因為本次缺少資料），因此不列入這個區間的計算基礎；這個 0，只代表這條上限這次不提供加碼空間，不涉及你目前部位的任何處置。

**逐條自檢**
- **與任務 1 分工不重複**：本句不重複任務 1 的因果鏈（f\* 不是正值→Kelly 不支持加碼），只處理「這個已知的 0，在區間計算裡是被排除、不是被當成缺資料」這一件事，並指向任務 1（「原因見上方第 5 條的說明」）取得完整理由，兩句合起來才完整、互不重複。
- **禁用字**：無「建議／應該／賣出／清倉／減碼／最佳倉位」（本句刻意迴避具名任何操作動詞，用「處置」這個中性詞概括，而非任務 1 收尾句沿用的「處置意見」措辭——刻意選字避免與任務 1 逐字重複，但語意一致）；無「信賴區間／信心水準／即時」。
- **否定層數**：「不是因為本次缺少資料」1（子句 A：排除原因）；「不列入」1（子句 B：對區間計算的具體處置）；「不涉及……任何處置」1（子句 C：對現有部位無涵義）。共 3 個獨立否定，分屬 3 個不同子句、各自受詞具名（排除原因／區間計算基礎／現有部位處置），符合跨子句 ≤4、單句 ≤2 的口徑。
- **因果詞**：「因此不列入」有明確前提支持（前提：這次額度是 0、且依 D-5 規則排除入 `notional_caps`），非裸因果。
- **不暗示應賣出／清倉（紅線）**：明確排除在「加碼區間」之外時，同時聲明「不涉及你目前部位的任何處置」——這正是防住「0 額度 → 系統要我清倉到 0」這條讀法的收尾句，呼應任務 1 對同一風險的處理，用詞不同但職權邊界一致。
- **措辭＝行為依據**：D-5 原文「f\*≤0 時 notional_caps 不放入 kelly_fraction 項（0 額度作 binding cap 會推導出清倉股數）；改由 suggest_quantity_range 專屬句揭露」——本句就是這個「專屬句」；「不是因為本次缺少資料」對應現行 `skipped_note`（`limits.py:1008-1013`）僅涵蓋「缺少可用資料」一種情境、與這裡「已算出結果、結果為 0、因規則排除」的情境不同，若沿用 `skipped_note` 字面會誤述原因。

### 備案（短版，僅限與任務 1 完整版同屏顯示，不得單獨出現）

> 「分數 Kelly 部位上限」這次計算出的加碼額度是 0（原因見上方第 5 條的說明，不是因為本次缺少資料），因此不列入這個區間的計算基礎。

**逐條自檢**：前半與主案逐字相同；省略收尾的「不涉及……處置」句，否定層數降為 2；**適用限制**：比照 (f) 短版 tooltip 僅在完整版同屏時核可的先例（第一輪落地條件 4），本備案僅在與任務 1 主案同屏顯示、且任務 1 主案已承載「不對持有部位提出處置意見」時可用；若本句會單獨出現在畫面上（例如區間說明區與上方第 5 條檢查列分屬不同摺疊區塊），須採主案。

### 待風控裁定（任務 1／2 共通）

1. **`notional_caps` 尚未排除 `kelly_fraction`（`f*<=0` 時）**：這是**程式落地缺口**，非文案問題——依 D-5，`f*<=0` 時 `kelly_fraction` 不該出現在 `notional_caps` 回傳的 dict 裡，但 `limits.py:918-920` 目前是 `allowed is not None` 就寫入，`0.0` 會被寫入。任務 1／2 兩句文案均以「D-5 規則已落地」為前提撰寫；**若這個排除尚未寫進 `notional_caps`，這兩句就不能出貨**（沿用「措辭＝行為」的一貫要求，句子描述的行為必須先在程式碼裡成立）。這點需要 dev-lead／tech-architect 確認排期，不是本批創意文案能自行解決的落地依賴，特此在批末列為 required 而非直接宣稱已完成。
2. **短版適用範圍**：任務 2 備案是否核准僅限同屏顯示（與 (f)/(3-B) 同型先例一致），或要求一律採主案。

---

## 3. FR-5 回測來源揭露框架句＋11 欄位標籤

依 PRD FR-5（2026-08-19 補齊版）與架構評估約束 20，須顯示 11 個欄位；本節只給「框架句＋欄位標籤」，數字由對應欄位帶入，不重複撰寫 (a-1)/(a-2)/(e)/(h) 已定稿的完整句——那四句在同一個明細區塊內原樣沿用，本節只負責把它們跟 11 個欄位放進同一個可核對的框架裡。

### 框架句 — 主案

> 以下是這筆 Kelly 輸入所依據的回測樣本明細，供你核對前面各句所指的「這段歷史樣本」「這次量測」實際是哪一次回測、哪一段期間。

**逐條自檢**：否定層數 0（純陳述用途）；措辭＝行為依據——本句的作用正是滿足第二輪批審 1-A「(e) 必須與該筆輸入的 OOS 起訖日期同屏，否則『這段歷史樣本』指涉不可查證」的要求，框架句把 (e)/(e-manual) 的「這段歷史樣本」與下面欄位 2（OOS 起訖）綁在同一個區塊，讓指涉可核對；不重複 (e)/(h) 本身的內容，只是引導語。

### 框架句 — 備案（更短）

> 以下欄位是這筆 Kelly 輸入所依據的回測樣本明細。

**逐條自檢**：同主案判斷，僅省略「供你核對……」的用途說明，供風控在篇幅上取捨。

### 11 個欄位標籤

| # | 欄位標籤（主案） | 對應資料 | 自檢重點 |
| - | - | - | - |
| 1 | 策略 | `strategy_id` | 純欄位名稱標籤，不含判斷語 |
| 2 | 樣本外（OOS）區段（起訖日期） | `oos_start_date` ～ `oos_end_date` | 與 (e)/(g-3) 已用的「樣本外」用詞一致，不另創新詞；滿足 1-A 同屏要求 |
| 3 | 完整回合數 | `oos_round_trips` | 對應 D-9 所稱 `n`；標籤本身不出現字母 `n`（面向使用者的欄位名不用內部縮寫） |
| 4 | 獲利回合數 | `oos_win_trips` | 對應 D-9 `n_win` |
| 5 | 虧損回合數 | `oos_loss_trips` | 對應 D-9 `n_loss` |
| 6 | 跨界排除回合數 | `oos_excluded_boundary_trips` | **不含任何方向性形容詞**（不寫「僅」「已排除的少量」等字眼）；標籤本身只是計數欄位名，「排除方式優先排除存續較長回合、證據不足以支持任何方向」的完整說明留給同框顯示的 (h) 句承載，標籤不重複、不搶先下結論 |
| 7 | 期末未平倉回合數 | `oos_open_trip_at_end` | 同欄位 6 判斷，同樣由 (h) 句承載機制說明 |
| 8 | 樣本觀測數（資料點數，非回合數） | `oos_observations` | 括號限定語防止讀者把這個數字誤讀成回合數——對應 `app/api/kelly.py:377-382` docstring「the number of **bars** the out-of-sample window spans, not the number of round trips」 |
| 9 | 勝率的 95% 區間（Wilson，依完整回合計） | `p_ci_low` ～ `p_ci_high` | **禁用字面查證**：不含「信賴區間」「信心水準」，比照 (a-1)/(a-2) 用「95% 區間」的既有先例；「依完整回合計」同時是任務 6 的限定語，兩處共用同一措辭，避免「勝率」在 Kelly 面出現兩種不同限定語版本 |
| 10 | 費率查證狀態 | `rates_verified` | 見下方獨立句（僅 `false` 時顯示） |
| 11 | 除權息還原狀態 | `dividend_reason_code` | 見下方，沿用既有核可句 |

### 欄位 9 連動規則（沿用 PRD 原文，未新擬句）

當 `ci_includes_no_edge` 為真時，本欄位同框連動顯示 (a-1)（設定頁／個股頁明細，帶數字版）或 (a-2)（`LimitsCheckList`，旗標版）——兩句皆已於第一／二輪定稿，本批不重擬，僅確認欄位 9 與這兩句的顯示時機綁定關係：`ci_includes_no_edge` 是 `f_star_ci_low <= 0` 的布林事實（D-6），技術上是對 f\* 區間下的判斷、非對這裡的 `p_ci_*` 本身，但 PRD FR-5 第 9 項驗收條件明文要求兩者同框連動顯示，本批依 PRD 原文執行，不在此另立新句。

### 欄位 10：費率查證狀態句 — 主案（`rates_verified=false` 時顯示）

> 此筆 Kelly 輸入所依據的回測，其手續費／交易稅／滑價率尚未對照主要來源查證，這筆勝率與盈虧比應視為待查證狀態。

**逐條自檢**
- 否定層數 1（「尚未」）。
- 措辭＝行為依據：`app/kelly/models.py:168`（`KellyInputRecord.rates_verified`）由 `app/api/kelly.py:639` 賦值 `run.response.rates_verified`，與 `BacktestReportView.tsx:121-129` 既有核可句所描述的是**同一次回測、同一個布林欄位**；差異只在受詞——既有句主詞是「報告數字」，本句主詞改為「這筆勝率與盈虧比」，因為這裡的顯示情境是 Kelly 輸入明細、不是整份回測報告。
- **與既有核可句的關係（請風控明示）**：本句不是既有 `BacktestReportView` 核可句的逐字搬移，是**同一事實在新情境（Kelly 明細）下的改寫**，依「重新情境即需重新核可」的一貫規則（比照 D6 ETF 殘餘風險句、`unsupported_market` 殘餘風險句各自獨立核可、不得互相代用的先例），本句視為**全新送審**，非自動沿用；若風控認為兩句差異過小可直接核准沿用既有句主詞置換，也請一併明示。

### 欄位 10 — 備案（逐字更貼近既有核可句，僅置換主詞與收尾受詞）

> 費率未查證：本次回測使用的手續費／交易稅／滑價率尚未對照主要來源查證（`verified_on` 為 `null`），這筆勝率與盈虧比應視為待查證狀態。

**逐條自檢**：與 `BacktestReportView.tsx:126-127` 既有核可句相比，僅結尾「報告數字」→「這筆勝率與盈虧比」一處置換，其餘字元（含「本次回測使用的手續費／交易稅／滑價率尚未對照主要來源查證（`verified_on` 為 `null`）」）逐字保留；否定層數同主案；供風控在「要不要盡量貼近既有句面」與「主案更貼合 Kelly 情境語感」之間選用。

### 欄位 11：除權息還原狀態 — 不新擬句，逐字沿用既有核可文案

`app/api/backtest.py:89-134`（`dividend_notes_by_code`）已有五句依 `reason_code`（`adjusted`／`disabled`／`never_synced`／`no_events`／`unusable_events`）分流的核可文案，且 `dividend_reason_code` 存進 `kelly_inputs` 時就是同一次回測算出的同一個代碼（`app/api/kelly.py:640`：`dividend_reason_code=str(run.response.dividend_adjustment["reason_code"])`）。**這是同一次回測、同一個事實的第二個顯示位置，不是新情境**——與欄位 10（受詞從「報告」換成「這筆輸入」而需要重新核可）不同，這裡的五句原文本身就是「本回測……」的通用敘述，未把「報告」寫進句子主體，直接搬到 Kelly 明細區塊語意不變、無需改寫一字。

**逐條自檢**：不新擬文案，本項自檢只確認「零修改沿用」的判斷是否成立——若風控認為即使字面不變，換一個顯示位置仍須重新用印，請在裁決中明示，本批將依裁決結果補送。

---

## 4. FR-6 手動來源摘要句（來源標示，與 (f) 免責分工）

分工：(f)／(f-tooltip) 是免責聲明（系統不查核、不自動調整）；本句是來源標示（這筆輸入從哪裡來、目前生效值是什麼版本），對應 FR-1「backtest_overridden 顯示上須清楚標示『回測預填，使用者已調整』而非單純標成 backtest」的要求。兩種來源各一句：

### (fr6-manual) 主案

> 此標的目前生效的 Kelly 輸入，來源為手動輸入。

**逐條自檢**：否定層數 0；措辭＝行為依據——`app/kelly/models.py:241-252`（`KellyInputRecord.manual`）確立 `source="manual"` 且不帶任何 `strategy_id`／回測欄位，句子如實對應「純手動、無回測成分」這一事實，不誇大也不隱藏。

### (fr6-manual) 備案（短版標籤）

> 來源：手動輸入。

**逐條自檢**：同主案判斷，僅供欄位旁短標籤情境（非完整句情境）使用。

### (fr6-overridden) 主案

> 此標的目前生效的 Kelly 輸入，原本由回測帶入，之後經你手動調整；調整後的數字才是目前生效值，原始回測帶入的數字仍保留、可以查看。

**逐條自檢**
- 否定層數 0（全句無否定詞——能用肯定句陳述事實時不刻意加否定，比照 (g-2)/(g-3)/元件 B 的既有判斷）。
- 因果詞：無裸因果；「原本……之後……」是時序陳述，非因果推論。
- 措辭＝行為依據：`app/kelly/models.py:254-270`（`KellyInputRecord.overriding`）——`values = {name: getattr(previous, name) for name in cls.model_fields}` 先把整列（含 `backtest_win_rate`／`backtest_payoff_ratio`）原樣複製，再只更新 `win_rate`／`payoff_ratio`／`source`，`backtest_*` 兩欄從未被寫入新值，逐字對應「原始回測帶入的數字仍保留、可以查看」；`source` 變為 `"backtest_overridden"` 對應「之後經你手動調整」。
- **與 (e-manual) 不重複**：(e-manual) 的職責是解釋「為什麼這個 backtest_overridden 情境下的『勝率』也不是機率」（數學論證）；本句的職責只是標示「這筆輸入的來源版本」（provenance 陳述），兩句可同屏並存、各自負責不同問題，不構成內容重複。

### (fr6-overridden) 備案（短版標籤）

> 來源：回測帶入，已手動調整；原始回測值仍保留可查。

**逐條自檢**：同主案判斷；短版仍保留「原始回測值仍保留可查」這個 FR-1 強制要求的事實（不可省略，否則使用者會以為調整後原始值就此消失），僅省略「之後經你手動調整」的完整敘事語氣。

### 待風控裁定

1. `(fr6-overridden)` 主案是否需要在同句內附上「（原始回測值另列於下方，見設定頁明細）」這類具體位置指引——本批未加，因為顯示位置屬前端排版而非文案應承諾的內容（若排版最終不在「下方」，句子會變成不實陳述），故刻意只講「可以查看」、不具體承諾方位，請風控確認這個保守判斷是否成立。

---

## 5. FR-4 三態徽章文字（fresh／ageing／expired）

沿用範圍：僅徽章極短標籤，`expired` 不承載完整揭露（完整句已有 (g-2)/(g-3)/(g-4)，徽章只是狀態指標）；`fresh`／`ageing` 目前無獨立完整句（FR-3 只要求 `ageing` 顯示「建議更新」提示，不影響上限評估），因此這兩態的責任範圍本來就只到「狀態標示」為止，不需另擬完整句搭配。

### 主案（逐字沿用 `NetWorthSection.tsx` 既有三態標籤，與 PRD FR-4/FR-3 驗收條件本身已引用的字面一致）

| 狀態 | 標籤 |
| - | - |
| fresh | 已更新 |
| ageing | 建議更新 |
| expired | 已過期 |

**逐條自檢**
- 一致性：與 `apps/stock-desk/frontend/app/settings/NetWorthSection.tsx:36-38`（`FRESHNESS_LABELS`）逐字相同；PRD FR-3/FR-4 驗收條件本身已直接引用「已更新」「建議更新」兩詞（`work/stock-desk-C5-Kelly-PRD.md` FR-3 時效性規則、FR-4 AC），採用相同標籤不是巧合沿用，而是與既有已核可產品慣例（淨值頁）及 PM 已寫入驗收條件的字面同步，降低「同一產品兩種新鮮度視覺卻用不同字」的認知成本。
- `expired` 徽章不單獨承載完整揭露：「已過期」三字本身不含天數、來源、下一步指引，這些事實全部由同框顯示的 (g-2)/(g-3)/(g-4) 承載（三句本身已在第一／二輪定稿，本批不重擬）；徽章與完整句的分工比照 (f) 短版 tooltip 與 (f) 完整版的既有分工模式。
- 禁用字：三個標籤均為中性狀態詞，無「建議你」「應該」等操作動詞（「建議更新」的「建議」是對「更新這個動作」的提示，不是對「加碼／減碼」等交易操作的建議，用法與 FR-8「不得暗示具體操作建議」規範的對象不同——FR-8 禁的是 Kelly 上限本身變成部位建議，不是禁止系統提示「這筆自報資料該更新了」這類資料維護性質的提醒，此點與淨值頁既有用法一致，供風控確認是否認可这個區分）。

### 備案（Kelly 專屬用詞，供風控在「是否需要與淨值頁視覺區隔」上選用）

| 狀態 | 標籤 |
| - | - |
| fresh | 新鮮 |
| ageing | 偏舊 |
| expired | 已逾期 |

**逐條自檢**：同主案判斷；差異僅在用詞本身、不影響分工與禁用字結論；`偏舊`／`已逾期` 皆為中性狀態詞，無方向性或緊急性誇大（未用「即將過期」等可能被讀成催促的字眼）。

---

## 6. `BacktestReportView` 勝率口徑限定語（綁 C8 閘門）

依五輪微批列管事項：`BacktestReportView.tsx:27` 的既有核可標籤「勝率」是 fill 層（`report.py` `_trade_stats`，「computed over **closing** (position-reducing) fills」），Kelly 面的「勝率」是回合層（`episodes.py` `round_trip_win_rate`），兩者同名不同口徑、且「帶入」動作發生在同一個畫面，需要限定語區辨，不評價哪個口徑較好。

### `BacktestReportView` 側 — 主案

> 勝率（依平倉筆數計）

**逐條自檢**：措辭＝行為依據——`app/backtest/report.py:19-20`（模組 docstring）「Win rate / profit factor are computed over **closing** (position-reducing) fills」、`_trade_stats`（`report.py:134-150`）以 `realized = [t.realized_pnl for t in trades if t.realized_pnl is not None]` 為分母，即「有已實現損益的平倉筆」，用「平倉筆數」比審查原文建議方向的「成交筆數」更精確——`num_trades`（含未平倉的開倉筆）與 `num_closing_trades`（僅平倉筆）是報表上並列的兩個不同計數，「成交筆數」一詞在使用者理解上可能被讀成 `num_trades`（含開倉），但這裡的分母其實是 `num_closing_trades`，用「平倉」二字避免這個混淆。

### `BacktestReportView` 側 — 備案（逐字採用審查原文建議方向）

> 勝率（依成交筆數計）

**逐條自檢**：逐字對應 2026-08-19 五輪微批原文所舉方向「（依成交筆數計）」；措辭精確度略遜於主案（見上），但與審查文字本身完全一致，供風控直接核准最省事；若核准本備案，建議風控知悉「成交筆數」在技術上實際對應的是 `num_closing_trades` 而非 `num_trades`，避免未來讀者望文生義。

### Kelly 面側 — 主案（同時作為任務 3 欄位 9 標籤的共用限定語）

> 勝率（依完整回合計）

**逐條自檢**：逐字對應五輪微批原文建議方向；措辭＝行為依據——`app/backtest/episodes.py` 的 `RoundTripStats`／`round_trip_win_rate` 以「完整持倉回合」（`round_trip`）為統計單位，非 fill；「完整」二字對應 D-9／約束 23 的「完全包含（entry_idx >= oos_start 且 exit_idx < oos_stop）」歸屬規則——跨界與期末未平倉回合都不算進這個分母，用「完整回合」精確描述這一點。
- **落地需求（非文案本身）**：這個標籤若要出現在 Kelly 面（例如任務 3 欄位 9、或任何顯示 `round_trip_win_rate` 數值的地方），依分歧①「前端零字面」規則，必須是**後端常數**、前端逐字渲染，且需併入 `test_kelly_wording.py` 白名單守門清單（第五輪微批 required 1-D 的同類擴充，這裡新增第六項）——不得由前端自行寫出「勝率（依完整回合計）」這幾個字。`BacktestReportView` 側則沿用既有例外（前端既有核可標籤，未追溯撤銷），兩處的落地路徑不同，請風控在核准本句時一併留意這個差異，避免 dev-lead 誤把 Kelly 面的限定語也當成前端可自行寫的既有例外。

### Kelly 面側 — 備案（更長、更精確版本）

> 勝率（依完整持倉回合計）

**逐條自檢**：同主案判斷，多「持倉」二字更貼近程式碼註解原文「complete **holding** round trips」，供風控在精確度與簡潔度間取捨；若採本備案，欄位 9 標籤（任務 3）需同步更新以維持兩處限定語逐字一致。

---

## 第五批總覽：待風控裁定事項彙總

1. **任務 1／2（f\*≤0 句與零額度句）落地前提**：`notional_caps`（`limits.py:918-920`）今天尚未依 D-5 排除 `kelly_fraction` 鍵，這兩句文案的「措辭＝行為」前提要求這個排除先落地，屬 dev-lead／tech-architect 排期事項，非文案本身可解決；請風控裁決時一併載明此為出貨前提，比照既有「(e-manual) CONFIRMED 前 `limits.py:837` 於後兩來源不得出貨」同類閘門寫法。
2. **任務 1／2 主案 vs 備案**：任務 1 備案省略職權邊界收尾句、任務 2 備案僅限同屏顯示，兩者的取捨邏輯相同（是否要求每句各自完整、或允許依賴同屏搭配），建議風控一併裁決以保持一致。
3. **任務 3 欄位 10（費率查證狀態）**：主案是全新情境改寫、備案逐字貼近既有核可句僅換受詞，需風控明示是否比照 D6/`unsupported_market` 先例視為需要重新核可的新句，或可直接沿用既有句面。
4. **任務 3 欄位 11（除權息還原狀態）**：本批判斷五句既有核可文案可零修改沿用至 Kelly 明細（同一事實、同一次回測的第二個顯示位置），非新情境；請風控確認此判斷是否成立。
5. **任務 5（FR-4 徽章）**：主案沿用淨值頁既有三態標籤字面（與 PRD 驗收條件本身引用的字面一致）、備案為 Kelly 專屬用詞，請風控擇一。
6. **任務 6（口徑限定語）**：`BacktestReportView` 側主案「依平倉筆數計」較精確、備案「依成交筆數計」逐字貼合審查原文；Kelly 面側兩案僅繁簡差異；並請風控確認 Kelly 面限定語需併入後端白名單守門（非沿用 `BacktestReportView` 的既有前端例外）這一點的落地路徑判斷是否正確。

## 交接（第五批）

- 逐字定稿後 → risk-compliance-officer 第六輪 CONFIRMED／退修；六類全數 CONFIRMED 即完成 `work/reviews/2026-08-19-C5-Kelly-文案批審.md`「仍缺」清單中「f\*≤0 句／`quantity_range` 零額度句／FR-5／FR-6 句／FR-4 徽章」與五輪微批列管的「`BacktestReportView` 口徑區辨句」全部項目，連同前四批 21 項合計補齊 C5 文案面。
- dev-lead：①`limits.py:918-920` 的 `notional_caps` 依 D-5 排除 `f*<=0` 時的 `kelly_fraction` 鍵（任務 1／2 出貨前提）；②任務 3 欄位 10/11、任務 4、任務 6 Kelly 面限定語比照既有慣例集中為後端常數（`app/api/kelly_wording.py` 或同層新模組），前端逐字渲染；③任務 6 `BacktestReportView.tsx:27` 標籤沿用既有前端例外，可由前端直接改字，但需同步 `test_kelly_wording.py`／前端清冊測試不誤判此處為新增的前端「勝率」字面違規。
- qa-reviewer：新增本批六類逐字守門測試；`notional_caps` 排除 `kelly_fraction`（`f*<=0`）的白盒測試；FR-5 十一欄位完整性測試（PRD 既有 AC 已列，本批文案面補齊後 AC 可轉綠）。

---

# 第六批（2026-08-22）— 欄位 11 除權息還原狀態：四降級分支 Kelly 情境版重擬

## 依據與退修範圍

- 退修來源：`work/reviews/2026-08-19-C5-Kelly-文案批審.md` 第六輪批審「欄位 11 除權息句：退修（本輪唯一）」——`DIVIDEND_ADJUSTED_NOTE` 沿用核可維持不動；其餘四降級分支（`disabled`／`never_synced`／`no_events`〔TW／非 TW〕／`unusable_events`）因原句串接的 `DIVIDEND_BIAS_SCOPE_NOTE` 含「策略欄」畫面指涉，在 Kelly 明細（無「策略欄」／「Buy & Hold 欄」並排兩欄結構）不可查證，不予零修改沿用，改由 creative-lead 另擬 Kelly 情境版。
- 對照原文（一字不動、僅供比對，`app/api/backtest.py:80-134`）：
  - `DIVIDEND_DISABLED_NOTE`：「本次依請求關閉除權息還原（adjust_dividends=false），報酬率不含股利；台股高配息情況下會低估。」+`DIVIDEND_BIAS_SCOPE_NOTE`
  - `DIVIDEND_NOT_SYNCED_NOTE`：「本回測未還原除權息：本機尚未同步過任何除權息資料（未執行 uv run python -m app.dividends.sync）。台股高配息情況下，未還原的報酬率會低估。」+`DIVIDEND_BIAS_SCOPE_NOTE`
  - `DIVIDEND_NO_EVENT_NOTE_TW`：「本回測未還原除權息：本機雖有除權息資料，但查無本商品在此區間的除權息紀錄。可能是該期間真的沒有配息，也可能是資料覆蓋不足（目前只涵蓋台股上市，不含上櫃），本系統無法分辨兩者；若實際有配息，此報酬率會低估。」+`DIVIDEND_BIAS_SCOPE_NOTE`
  - `DIVIDEND_NO_EVENT_NOTE_NON_TW`：「本回測未還原除權息：本系統的除權息資料目前只涵蓋台股上市，不涵蓋本市場，因此不是『查無配息』而是『沒有資料可查』。若本商品有配息，此報酬率會低估。」+`DIVIDEND_BIAS_SCOPE_NOTE`
  - `DIVIDEND_UNUSABLE_NOTE`：「本回測未還原除權息：查到本商品在此區間的除權息紀錄，但欄位不足以推算調整因子，已整筆略過而非用推估值代替。此報酬率會低估。」+`DIVIDEND_BIAS_SCOPE_NOTE`+「請重跑同步，或回報此代號與區間供人工覆核來源欄位。」
  - 原句被指名的問題句 `DIVIDEND_BIAS_SCOPE_NOTE`：「此低估（若發生）落在 Buy & Hold 與持有期間的報酬；策略欄因進出場時點不同，偏誤幅度不一定相同。」——Kelly 明細只顯示這筆輸入所依據的**單一次**回測的完整回合統計（FR-5 十一欄位、`episodes.py` 回合層數字），不並排顯示「策略欄」與「Buy & Hold 欄」兩欄，故此句「策略欄」「Buy & Hold」對照的指涉在 Kelly 頁面不可查證，本批僅替換這一段收尾限定語，`disabled`／`never_synced`／`no_events`／`unusable_events` 四段的**主體事實不動**（未還原之因＋台股高配息下可能低估，逐一由 `app/api/backtest.py` 對應常數如實轉述，非新事實）。
- 本批重擬的是**新畫面的新常數**（供 Kelly 明細使用，尚未落地，不改動 `app/api/backtest.py` 任何一字）；上述五句原文只作對照，未經改動。
- 程式依據（措辭＝行為）：`app/api/backtest.py:194-252`（`resolve_dividend_adjustment`：`requested=False→disabled`／`not store.is_synced()→never_synced`／`adjustment.applied=False` 時依 `events_skipped` 二分 `unusable_events`／`no_events`，`no_events` 再依 `market` 二分 TW／非 TW）；`app/api/backtest.py:136-154`（`dividend_notes_by_code`，`reason_code→句子` 一對一對應）；`app/api/kelly.py:640`（`dividend_reason_code=str(run.response.dividend_adjustment["reason_code"])`——確認同一次回測算出的同一個 `reason_code` 存進 `kelly_inputs`，Kelly 明細顯示的正是這個代碼，不是另一次量測）；`app/backtest/episodes.py`（Kelly 面「勝率」「盈虧比」的分母是完整回合，非 fill，見任務 6 已定稿限定語「（依完整回合計）」）。

## 統一收尾限定語（取代 `DIVIDEND_BIAS_SCOPE_NOTE`，Kelly 情境版，四分支共用同一段）

> 此低估（若發生），指的是這筆 Kelly 輸入所依據的這次回測算出的完整回合報酬——也就是這裡的勝率與盈虧比所依據的數字；本頁未提供 Buy & Hold 或其他呈現方式的對照數字可供比對幅度。

**逐條自檢（此收尾句本身）**
- 核心事實保留：①「低估（若發生）」的條件語氣逐字保留，未改成確定語氣；②「範圍限定」語意保留——原句限定「這個低估幅度講的是 Buy & Hold／持有期間，不代表策略欄也一樣」，新句限定「這個低估幅度講的是這次回測的完整回合報酬（即 Kelly 用的數字），本頁沒有另一組數字可比對幅度是否相同」——功能對等（都是「不要把這個幅度隨意套到別的數字上」），只是把「比對對象」從「同畫面另一欄」換成「本頁沒有另一組數字」，因為 Kelly 明細確實沒有並排另一欄。
- 移除「策略欄」畫面指涉：全句無「策略欄」「Buy & Hold 欄」等排版位置詞，改用「這裡的勝率與盈虧比」「本頁」指涉 Kelly 明細本身可查證的內容。
- 否定層數：「未提供」1 個獨立否定（子句：本頁是否提供對照數字），受詞具名、可查證（Kelly 明細 FR-5 十一欄位表未含任何 Buy & Hold 或其他策略的欄位，見任務 3 欄位清單，僅 11 項皆屬本次回測自身）。單句內僅 1 層，遠低於 ≤2／≤4 上限。
- 禁用字：無「建議／應該／賣出／清倉／減碼至／最佳倉位／信賴區間／信心水準」。
- 與欄位 10 不重複：全句無「應視為待查證」或同義措辭，講的是「幅度不可比對」而非「數字有效性待查證」，兩者主題不同（欄位 10 講費率未查證、本句講低估幅度的比對範圍），可同屏並存不重疊。
- 措辭＝行為依據：「這裡的勝率與盈虧比所依據的數字」對應 `episodes.py` 完整回合統計即 `ctx.win_rate`／`ctx.payoff_ratio` 的來源（`limits.py:595-615` 代入處）；「本頁未提供…對照數字」對應任務 3 FR-5 十一欄位表（`work/stock-desk-C5-Kelly-揭露文案起草.md` 第五批 §3）确实不含 Buy & Hold 或跨策略欄位，可查證為真。

---

## 分支一：`disabled`（使用者關閉還原）— Kelly 情境版

### 主案

> 這筆 Kelly 輸入所依據的回測，當時依請求關閉了除權息還原（`adjust_dividends=false`），算出的報酬率不含股利；台股高配息情況下，這裡顯示的勝率與盈虧比可能因此被低估。此低估（若發生），指的是這筆 Kelly 輸入所依據的這次回測算出的完整回合報酬——也就是這裡的勝率與盈虧比所依據的數字；本頁未提供 Buy & Hold 或其他呈現方式的對照數字可供比對幅度。

**逐條自檢**
- 核心事實不刪：「未還原→報酬率可能低估」保留（「算出的報酬率不含股利」＝未還原的直接後果；「可能因此被低估」＝低估可能性，對應原句「會低估」，改「會」為「可能」是刻意收斂——原句 `DIVIDEND_DISABLED_NOTE` 對「台股高配息」情境用「會低估」的確定語氣，但那是對 Buy & Hold／持有期間報酬的陳述；本句主詞換成「勝率與盈虧比」〔回合層統計〕，是否同樣必然低估未經驗證，故用「可能」而非搬用「會」，此為措辭＝行為的必要收斂，非弱化揭露）；「低估範圍限定語意」保留於收尾句。
- 移除「策略欄」指涉：主體與收尾均未出現「策略欄」；主詞明確指向「這裡顯示的勝率與盈虧比」（Kelly 明細本身），對應「這筆 Kelly 輸入所依據的回測」的陳述要求。
- 否定層數：「不含股利」1（子句 A：報酬率組成事實）；收尾「未提供」1（子句 B：本頁呈現範圍）。共 2 個獨立否定，分屬不同子句、受詞具名，遠低於 ≤4 上限；單句內各僅 1 層。
- 因果詞：無裸因果；「當時依請求關閉了…，算出的報酬率不含股利」是條件（關閉→不含股利）的直接對應陳述，非跳躍推論；「可能因此被低估」的「因此」承接「不含股利＋台股高配息」兩個已交代前提，非裸接續。
- 禁用字：無禁用字面。
- 措辭＝行為依據：`app/api/backtest.py:211-222`（`resolve_dividend_adjustment`，`not requested` 分支，`reason_code="disabled"`，`requested=body.adjust_dividends`）；`DIVIDEND_DISABLED_NOTE`「報酬率不含股利」逐字承接同一事實；「這裡顯示的勝率與盈虧比可能因此被低估」為新增推論，依據：回合層報酬（`episodes.py` round trip P&L）以價格序列計算，未還原時同樣不含持有期間應得的股利現金流，此推論與原句「Buy & Hold／持有期間報酬會低估」同一機制、僅換算到回合層，方向一致、幅度未知（故用「可能」不用「會」）。

### 備案

> 這筆 Kelly 輸入所依據的回測，因請求端關閉了除權息還原（`adjust_dividends=false`），算出的報酬率沒有含入股利；在台股高配息的情況下，這裡的勝率與盈虧比有可能被低估。此低估（若發生），僅描述這筆 Kelly 輸入所依據的這次回測本身算出的完整回合報酬（即這裡勝率與盈虧比的計算基礎）；其他呈現方式（例如 Buy & Hold）的低估幅度是否相同，本頁不做比較。

**逐條自檢**：核心事實與否定層數判斷同主案（「沒有含入」「不做比較」共 2 個獨立否定）；收尾句改用「僅描述…」＋「本頁不做比較」的敘事順序（先講範圍、後講不比較什麼），與主案「先講本頁沒有什麼、再點名比對對象」語序相反，供風控依語感選用；措辭＝行為依據同主案。

---

## 分支二：`never_synced`（本機從未同步過除權息資料）— Kelly 情境版

### 主案

> 這筆 Kelly 輸入所依據的回測未進行除權息還原：本機當時尚未同步過任何除權息資料（尚未執行 `uv run python -m app.dividends.sync`）。台股高配息情況下，這裡顯示的勝率與盈虧比可能因此被低估。此低估（若發生），指的是這筆 Kelly 輸入所依據的這次回測算出的完整回合報酬——也就是這裡的勝率與盈虧比所依據的數字；本頁未提供 Buy & Hold 或其他呈現方式的對照數字可供比對幅度。

**逐條自檢**
- 核心事實不刪：「未還原→可能低估」保留；「未同步過除權息資料」的原因說明逐字承接 `DIVIDEND_NOT_SYNCED_NOTE`（含指令原文 `uv run python -m app.dividends.sync`，一字不動）。
- 移除「策略欄」指涉：同分支一。
- 否定層數：「未進行」1（子句 A：本次是否還原的事實）；「尚未同步過…（尚未執行…）」同一子句內 2 個否定詞、指向同一件事（本機同步狀態），依分歧②「單一子句內否定詞上限 2 個」判斷為 1 個獨立否定單位；收尾「未提供」1（子句 C）。共 3 個獨立否定單位，分屬不同子句、受詞具名（還原狀態／本機同步狀態／本頁呈現範圍），符合「跨子句 ≤4」；子句內未超過 2。
- 因果詞：無裸因果；「本機當時尚未同步過…，可能因此被低估」的「因此」承接「未同步→未還原→可能低估」這條有明確前提支持的鏈，非跳躍推論。
- 禁用字：無。
- 措辭＝行為依據：`app/api/backtest.py:223-230`（`not store.is_synced()` 分支，`reason_code="never_synced"`）；指令字面對應 `DIVIDEND_NOT_SYNCED_NOTE` 原文，未改一字。

### 備案

> 這筆 Kelly 輸入所依據的回測沒有進行除權息還原，原因是本機當時還沒有同步過除權息資料（尚未執行 `uv run python -m app.dividends.sync`）；在台股高配息的情況下，這裡的勝率與盈虧比有可能被低估。此低估（若發生），僅描述這筆 Kelly 輸入所依據的這次回測本身算出的完整回合報酬（即這裡勝率與盈虧比的計算基礎）；其他呈現方式（例如 Buy & Hold）的低估幅度是否相同，本頁不做比較。

**逐條自檢**：核心事實、否定層數（3 個獨立單位）判斷同主案；「原因是」取代「：」的因果標示更直白；措辭＝行為依據同主案。

---

## 分支三：`no_events`（查無除權息紀錄，依市場分 TW／非 TW）— Kelly 情境版

### 主案（TW）

> 這筆 Kelly 輸入所依據的回測未進行除權息還原：本機雖有除權息資料，但這段期間內查無本商品的除權息紀錄——可能是這段期間確實沒有配息，也可能是資料涵蓋範圍有限（目前只涵蓋台股上市，尚未涵蓋上櫃），本系統無法判斷是哪一種情況；若實際上有配息，這裡顯示的勝率與盈虧比可能因此被低估。此低估（若發生），指的是這筆 Kelly 輸入所依據的這次回測算出的完整回合報酬——也就是這裡的勝率與盈虧比所依據的數字；本頁未提供 Buy & Hold 或其他呈現方式的對照數字可供比對幅度。

### 主案（非 TW）

> 這筆 Kelly 輸入所依據的回測未進行除權息還原：本系統的除權息資料目前只涵蓋台股上市，不涵蓋這個市場，因此不是『查無配息』，而是『沒有資料可查』；若本商品有配息，這裡顯示的勝率與盈虧比可能因此被低估。此低估（若發生），指的是這筆 Kelly 輸入所依據的這次回測算出的完整回合報酬——也就是這裡的勝率與盈虧比所依據的數字；本頁未提供 Buy & Hold 或其他呈現方式的對照數字可供比對幅度。

**逐條自檢（TW／非 TW 共通部分）**
- 核心事實不刪：兩個假設（真的沒配息／資料覆蓋不足）與「系統無法分辨」的立場逐字承接 `DIVIDEND_NO_EVENT_NOTE_TW`／`DIVIDEND_NO_EVENT_NOTE_NON_TW`，未省略任一假設，也未替系統做它做不到的判斷。
- 移除「策略欄」指涉：同分支一；收尾句換成 Kelly 版本。
- 因果詞：非 TW 版「因此不是…而是…」對應原句既有結構，前提（資料範圍不涵蓋此市場）明確支持結論，非裸因果。
- 禁用字：無。
- 措辭＝行為依據：`app/api/backtest.py:232-251`（`adjustment.applied=False` 且 `adjustment.events_skipped==0` 時 `reason_code="no_events"`）；`dividend_notes_by_code`（`backtest.py:136-154`）依 `market` 二分 TW／非 TW，逐字承接對應原句主體。

**否定層數——本項需向風控明示的落差（required 待裁定）**
- **TW 版**：逐句計數獨立否定單位——「未進行」(1)／「查無」(1)／「沒有配息」(1，假設一)／「尚未涵蓋」(1，假設二，含「目前只涵蓋台股上市」的對比語境)／「無法判斷」(1)／收尾「未提供」(1)。共 **6 個獨立否定單位**，逐一分屬不同子句、受詞具名（是否還原／有無紀錄／有無配息假設／資料覆蓋範圍／能否分辨／本頁呈現範圍），**超過分歧②「每句上限 4 個」**。
- **非 TW 版**：獨立否定單位——「未進行」(1)／「不涵蓋」(1)／「不是」(1，因果對比子句)／收尾「未提供」(1)。共 **4 個**，恰在上限。
- **判斷與請示**：TW 版之所以超額，是因為原句 `DIVIDEND_NO_EVENT_NOTE_TW`（2026-08-10 批次核可、早於 2026-08-19 分歧②「每句 ≤4」口徑）本身就需要交代「兩個互斥假設＋系統無法分辨」這組不可再壓縮的事實——這是 Kelly 版沿用的**既有核可核心事實**，非本輪新增；本輪新增的收尾句僅貢獻其中 1 個否定單位（「未提供」）。**請風控明示**：(i) 沿用既有核可核心事實時，其既有否定層數是否視為沿用豁免、不重新計入分歧②上限（比照「既有 21 項無字面重疊」的沿用邏輯）；或 (ii) 一律從嚴，要求 TW 版拆成兩段顯示以降到每段 ≤4（例如把「兩個假設＋無法分辨」拆成獨立一句、「若有配息則可能低估＋收尾範圍限定」拆成另一句）。本批先提供主案（不拆句、貼近原文結構），並在下方備案提供拆句版本供風控直接選用。

### 備案（TW，拆兩句降低單句否定數，供風控在嚴格口徑下選用）

> 【原因】這筆 Kelly 輸入所依據的回測未進行除權息還原：本機雖有除權息資料，但這段期間內查無本商品的除權息紀錄——可能是這段期間確實沒有配息，也可能是資料涵蓋範圍有限（目前只涵蓋台股上市，尚未涵蓋上櫃），本系統無法判斷是哪一種情況。
> 【範圍】若實際上有配息，這裡顯示的勝率與盈虧比可能因此被低估；此低估（若發生），指的是這筆 Kelly 輸入所依據的這次回測算出的完整回合報酬——也就是這裡的勝率與盈虧比所依據的數字，本頁未提供 Buy & Hold 或其他呈現方式的對照數字可供比對幅度。

**逐條自檢**：【原因】句獨立否定單位 4 個（未進行／查無／沒有配息／尚未涵蓋／無法判斷——精確數為 5，仍超額，因兩個假設＋無法分辨判斷是同一組不可拆的事實，拆句無法把這組事實本身的否定數降到 4 以下，只能把「未提供」挪到第二句）；【範圍】句獨立否定單位 1 個（未提供）。拆句後第二句降到安全值，第一句仍是既有核可事實的既有否定數（非本輪新增），與主案的判斷（豁免請示）本質相同，只是排版上拆成兩段——**若風控要求「每一段顯示都必須 ≤4」而非「整塊揭露 ≤4」，此拆句版本仍無法讓第一段達標，須風控指示是否需要進一步刪減兩個假設之一（但刪減會違反「核心事實不得刪」的任務要求，兩者衝突，最終仍需風控裁定，creative-lead 不擅自二選一）**。

### 備案（非 TW）

> 這筆 Kelly 輸入所依據的回測沒有進行除權息還原：本系統的除權息資料目前只涵蓋台股上市，還沒有涵蓋這個市場，所以嚴格來說不是『查無配息』，而是『沒有資料可查』；如果本商品有配息，這裡的勝率與盈虧比有可能被低估。此低估（若發生），僅描述這筆 Kelly 輸入所依據的這次回測本身算出的完整回合報酬（即這裡勝率與盈虧比的計算基礎）；其他呈現方式（例如 Buy & Hold）的低估幅度是否相同，本頁不做比較。

**逐條自檢**：獨立否定單位 4 個（沒有進行／還沒有涵蓋／不是／不做比較），與主案相同、恰在上限；核心事實與措辭＝行為依據同主案。

---

## 分支四：`unusable_events`（查到紀錄但欄位不足以推算）— Kelly 情境版

### 主案

> 這筆 Kelly 輸入所依據的回測未進行除權息還原：查到本商品在此區間的除權息紀錄，但欄位不足以推算調整因子，已整筆略過而非用推估值代替；這裡顯示的勝率與盈虧比可能因此被低估。此低估（若發生），指的是這筆 Kelly 輸入所依據的這次回測算出的完整回合報酬——也就是這裡的勝率與盈虧比所依據的數字；本頁未提供 Buy & Hold 或其他呈現方式的對照數字可供比對幅度。請重跑同步，或回報此代號與區間供人工覆核來源欄位。

**逐條自檢**
- 核心事實不刪：「查到紀錄但欄位不足以推算」「整筆略過而非用推估值代替」逐字承接 `DIVIDEND_UNUSABLE_NOTE`；「未還原→可能低估」保留；末句「請重跑同步，或回報此代號與區間供人工覆核來源欄位」為既有已核可的資料品質回報指引（非交易行動指令），逐字沿用不改動。
- 移除「策略欄」指涉：同分支一；收尾範圍限定改為 Kelly 版本。
- 否定層數：「未進行」1（子句 A）；「不足以推算…而非用推估值代替」同一子句 2 個否定詞、指向同一件事（資料不可用故略過），依分歧②判斷為 1 個獨立單位（子句 B）；收尾「未提供」1（子句 C）。共 3 個獨立否定單位，分屬不同子句、受詞具名，符合 ≤4；子句內未超過 2。
- 因果詞：無裸因果；「欄位不足以推算調整因子，已整筆略過」是有明確前提支持的直接對應（不足以推算→略過），非跳躍推論。
- 禁用字：無；末句「請重跑同步，或回報…」為資料維運性質指引，非交易操作建議，與既有「請重新確認數字後更新」同類（風控已於分歧①/微批一先例背書此類非交易行動指引可用）。
- 措辭＝行為依據：`app/api/backtest.py:236-251`（`adjustment.applied=False` 且 `adjustment.events_skipped>0` 時 `reason_code="unusable_events"`）；`DIVIDEND_UNUSABLE_NOTE` 原文「查到…但欄位不足以推算調整因子，已整筆略過而非用推估值代替」逐字承接。

### 備案

> 這筆 Kelly 輸入所依據的回測沒有進行除權息還原：系統查到本商品在這段期間的除權息紀錄，但欄位不足以推算調整因子，因此整筆略過，沒有用推估值頂替；這裡的勝率與盈虧比有可能因此被低估。此低估（若發生），僅描述這筆 Kelly 輸入所依據的這次回測本身算出的完整回合報酬（即這裡勝率與盈虧比的計算基礎）；其他呈現方式（例如 Buy & Hold）的低估幅度是否相同，本頁不做比較。請重跑同步，或回報此代號與區間供人工覆核來源欄位。

**逐條自檢**：核心事實、否定層數（3 個獨立單位）判斷同主案；「因此整筆略過，沒有用推估值頂替」把因果標示挪到句中，語感更口語；措辭＝行為依據同主案。

---

## 第六批待風控裁定事項彙總（欄位 11 四分支部分）

1. **四分支收尾限定語是否核可**：統一收尾句「此低估（若發生），指的是這筆 Kelly 輸入所依據的這次回測算出的完整回合報酬……本頁未提供 Buy & Hold 或其他呈現方式的對照數字可供比對幅度」是否可取代 `DIVIDEND_BIAS_SCOPE_NOTE` 在 Kelly 明細的角色；四分支各自主體事實段落是否認可依原句如實轉述（僅去除「策略欄」指涉、視情況將「會」收斂為「可能」）。
2. **`no_events`（TW）否定層數超額（required，本批最大懸項）**：TW 版獨立否定單位達 6 個，超過分歧②「每句 ≤4」上限，成因是既有核可核心事實（兩個互斥假設＋系統無法分辨）本身不可再壓縮；請風控裁定（i）沿用既有核可事實時是否豁免重新計入分歧②上限，或（ii）要求採備案拆句版本，或（iii）指示如何在不刪核心事實的前提下進一步降否定數（creative-lead 目前找不到第三種寫法）。非 TW 版已壓在 4 的上限內，無需裁定。
3. **主案 vs 備案**：四分支各自的主備案差異僅在敘事語序與「未提供」vs「不做比較」的收尾措辭，內容事實一致，請風控依語感擇一；`no_events`（TW）額外多一組「拆句 vs 整段」的排版選項待裁。
4. **落地前提**：本批四分支＋收尾句一旦 CONFIRMED，須：①後端新增獨立常數模組（比照分歧①機制，不放進 `app/api/backtest.py`，避免與既有已核可字面混淆）；②`dividend_reason_code` 分支邏輯於 Kelly 面組裝時只讀 `reason_code` 選句，不重新判斷；③逐字守門測試（含標點）；④與任務 3 條件 42（FR-5 十一欄位僅 `source=="backtest"` 顯示）一致，欄位 11 亦僅 `backtest` 來源顯示。

---

## 7. 原始值標示句（tech-architect 條件 46 裁決約束 9，設定頁「原始值檢視」）

依 `work/stock-desk-C5-Kelly-條件46裁決.md`：採 (a1) 互斥檢視（生效值檢視 ↔ 原始值檢視，兩態不得同時渲染於 DOM），入口限 `source == "backtest_overridden"` 且 `backtest_win_rate`／`backtest_payoff_ratio` 皆非 null。原始值檢視開啟時，畫面上不存在生效值、(e-manual)、(f)——本句是該畫面**唯一的語意錨**，三要件缺一即讓讀者誤以為這兩個數字仍在使用中或仍受查核。

查證（措辭＝行為）：`app/kelly/models.py:254-270`（`KellyInputRecord.overriding`：`backtest_win_rate`／`backtest_payoff_ratio` 保留系統算出的原值，`win_rate`／`payoff_ratio`〔生效值〕才被覆寫）；`app/advice/limits.py:608-611`／`825-826`（`kelly_allowed_weight`／`_check_kelly_fraction` 只讀 `ctx.win_rate`／`ctx.payoff_ratio`，即列上的生效值欄位，從未讀 `backtest_win_rate`／`backtest_payoff_ratio`——第③要件「本條上限用的是生效值」逐字對應此事實）。

### 主案

> 這兩個數字，是這筆 Kelly 輸入回測帶入當時由系統算出、之後被你手動調整仍保留下來的原始值；目前生效的是你調整後的數字，不是這裡這兩個。本條上限的計算，用的是生效值，不是這裡顯示的原始值。

**逐條自檢**
- 三要件逐一對應且未省略：①「這兩個數字，是這筆 Kelly 輸入回測帶入當時由系統算出、之後被你手動調整仍保留下來的原始值」；②「目前生效的是你調整後的數字，不是這裡這兩個」；③「本條上限的計算，用的是生效值，不是這裡顯示的原始值」——三句依裁決約束 9 原文順序排列，一步未跳。
- 否定層數：「不是這裡這兩個」1（子句：生效值的歸屬）；「不是這裡顯示的原始值」1（子句：上限計算的依據）。共 2 個獨立否定，分屬不同子句、受詞具名（生效值是什麼／上限算什麼），遠低於 ≤4 上限，單句內各僅 1 層。
- 因果詞：無裸因果；純陳述事實的並列句。
- 禁用字：無「建議／應該／賣出／清倉／減碼至／最佳倉位／信賴區間／信心水準」。
- 與 (e-manual)／(f) 不重複：(e-manual) 講「勝率≠機率」的數學論證、(f) 講「系統不查核／不調整」的免責聲明，兩句在互斥檢視規則下本就不與本句同屏（條件 46 約束 3）；本句只負責「這是哪個版本的數字、目前算哪一個」的歸屬陳述，職責不重疊。
- 措辭＝行為依據：見小節前言查證段。

### 備案

> 這裡顯示的勝率與盈虧比，是回測帶入當時系統算出、你後來手動調整後仍保留下來的原始值——不是現在用來計算的數字。目前生效、實際用於本條上限計算的，是你調整後的數字。

**逐條自檢**：三要件同主案對應（①在前半句；②③合併於後兩句「不是現在用來計算的數字」＋「目前生效、實際用於本條上限計算的，是你調整後的數字」）；否定層數降為 1（「不是現在用來計算的數字」），將②③的否定表述改用一次否定＋一次肯定收尾表達，語氣更輕；其餘判斷同主案。

---

## 8. 原始值檢視的 OOS 日期標籤（條件 46 約束 10，不得複用 FR-5 欄位 2 字面）

FR-5 欄位 2 CONFIRMED 標籤為「樣本外（OOS）區段（起訖日期）」（第五批任務 3，`source=="backtest"` 的十一欄明細專用）；依條件 46 約束 6／10，原始值檢視只准三項極簡標籤—值列（`backtest_win_rate`／`backtest_payoff_ratio`／OOS 起訖日期），**不得**渲染成表格式「明細」外觀、也不得讓 OOS 日期標籤與 FR-5 明細同名，避免使用者誤以為原始值檢視也是（或連向）完整的十一欄明細。

### 主案

> 原始回測的樣本外期間

**逐條自檢**：與 FR-5 欄位 2「樣本外（OOS）區段（起訖日期）」逐字比對——去除「（OOS）區段」與「（起訖日期）」兩個括號限定，改用「原始回測的」前綴錨定這是原始值檢視專屬標籤，非十一欄明細的欄位 2；字元集合不同、語境不同（原始值檢視只有三項標籤—值列，無法被誤讀為完整明細表）；否定層數 0；措辭＝行為依據：對應 `oos_start_date`／`oos_end_date`（`models.py:163-164`，`overriding` 保留不覆寫），與 FR-5 欄位 2 讀同一組資料庫欄位，但顯示情境（三項極簡列 vs 十一欄表格）不同，依「重新情境需重新核可」慣例另立標籤。

### 備案

> 原始回測樣本外起訖日

**逐條自檢**：同主案判斷，更短版本（省略「的」「期間」，改「起訖日」）；與 FR-5 欄位 2 字面差異更大（無「樣本外」與「區段」相鄰排列），供風控在「精簡度」與「與 (g-3)『樣本外區段結束於』用詞呼應程度」之間選用；否定層數 0。

---

## 9. (g-overridden) `backtest_overridden` 過期句（條件 46 附帶發現，比照 (g-3) 結構）

裁決指出：`backtest_overridden` 列過期時，新鮮度錨仍是 `oos_end_date`（`models.py:346-364`：`anchor_moment` 僅 `source == "manual"` 走 `updated_at`，其餘〔含 `backtest_overridden`〕一律走 `oos_end_date`），但既有 (g-2)「來源：手動輸入」與 (g-3)「來源：回測帶入」皆未涵蓋「回測帶入、已手動調整」這個來源態——(g-2) 的來源標示不實（此列並非手動輸入且錨點機制也不同），(g-3) 的來源標示則省略了使用者已覆寫的事實。

查證：`app/kelly/models.py:360-364`（`anchor_moment`，`backtest_overridden` 與 `backtest` 同走 `else` 分支、皆用 `oos_end_date`）；`tests/test_kelly_ageing.py:68-71`（`source="backtest_overridden"` 案例驗證錨點行為與 `backtest` 一致）；來源標示措辭對齊已 CONFIRMED 的 (fr6-overridden)：完整句「原本由回測帶入，之後**經你手動調整**」、短標籤「來源：回測帶入，**已手動調整**」（第五批任務 4）。

### 主案（沿用短標籤字面「已手動調整」）

> 此標的的 Kelly 輸入（來源：回測帶入，已手動調整）已過期——樣本外區段結束於 {anchored_on}，距今 {age_days} 天，超過 {days} 天的新鮮期，本條上限暫不評估；請重新執行回測並確認後更新。

**逐條自檢**
- 結構比照 (g-3)：唯一差異是來源括號內文字（「來源：回測帶入」→「來源：回測帶入，已手動調整」），錨點語（「樣本外區段結束於」）、三佔位符（`{anchored_on}`／`{age_days}`／`{days}`）、收尾行動指引（「請重新執行回測並確認後更新」）逐字沿用 (g-3) 既有核可結構，未新增或刪除任何機制性文字。
- 來源標示準確性：「回測帶入，已手動調整」逐字取自 (fr6-overridden) 短標籤（第五批任務 4，`來源：回測帶入，已手動調整；原始回測值仍保留可查`）的前半段，非本輪新造詞——沿用已核可字面降低重新核可的語感風險。
- 否定層數 0（全句無否定詞），與 (g-2)／(g-3) 既有判斷一致（能用肯定句陳述事實時不刻意加否定）。
- 因果詞：無裸因果。
- 禁用字：無。
- 行動指引選擇：沿用 (g-3) 的「請重新執行回測並確認後更新」而非 (g-2) 的「請重新確認數字後更新」——因為造成「已過期」的根源是 `oos_end_date` 這個回測錨點過舊，不是使用者手鍵的覆寫值本身過舊；重新執行回測才能更新錨點，若使用者仍要覆寫，可在拿到新回測結果後再次調整，這一步不是本句要承諾的範圍，故不額外加字。
- 措辭＝行為依據：見小節前言查證段。

### 備案（改用完整句字面「經你手動調整」）

> 此標的的 Kelly 輸入（來源：回測帶入，經你手動調整）已過期——樣本外區段結束於 {anchored_on}，距今 {age_days} 天，超過 {days} 天的新鮮期，本條上限暫不評估；請重新執行回測並確認後更新。

**逐條自檢**：與主案唯一差異是來源括號內「已手動調整」→「經你手動調整」，取自 (fr6-overridden) 完整句「之後經你手動調整」的字面而非短標籤；其餘結構、否定層數（0）、措辭＝行為依據判斷同主案；供風控依「與哪一句 fr6-overridden 呼應」的偏好選用。

---

## 第六批待風控裁定事項彙總（任務 7-9 部分，條件 46 追加項）

5. **任務 7 原始值標示句**：三要件是否完整覆蓋約束 9；主案 vs 備案（否定層數 2 vs 1）擇一；與互斥檢視規則（畫面不含 (e-manual)/(f)）搭配是否成立。
6. **任務 8 OOS 日期標籤**：主案「原始回測的樣本外期間」vs 備案「原始回測樣本外起訖日」是否確實不會被讀為 FR-5 欄位 2 明細，或需要更大幅度的字面區隔。
7. **任務 9 `(g-overridden)` 過期句**：主案沿用 (fr6-overridden) 短標籤「已手動調整」vs 備案沿用完整句「經你手動調整」擇一；行動指引沿用 (g-3)「請重新執行回測並確認後更新」（而非 (g-2) 的手動確認指引）的判斷是否成立。
8. **落地前提（任務 7-9 共通）**：條件 46 約束 2（入口限 `source=="backtest_overridden"` 且 `backtest_win_rate`／`backtest_payoff_ratio` 皆非 null）與約束 19（PortfolioContext 尚未帶 source 欄位，落地前提未解）皆為工程閘門，非文案本身可解；本三句 CONFIRMED 不代表可出貨，仍需等待 K4 互斥檢視元件與 PortfolioContext 來源欄位落地。

## 交接（第六批，含條件 46 追加三項）

- 逐字定稿後 → risk-compliance-officer 第七輪裁決；本批共九項待決：欄位 11 四分支＋收尾句（`no_events` TW 否定層數超額為最大懸項）、任務 7 原始值標示句、任務 8 OOS 日期標籤、任務 9 `(g-overridden)` 過期句。
- CONFIRMED 後：dev-lead ①於 Kelly 揭露常數模組新增欄位 11 五則字串（四分支+共用收尾，`no_events` 依 market 分 TW/非TW）；②新增任務 7-9 三則常數，任務 9 併入既有 `RISK_CONFIRMED_WORDING`／`g-*` 命名體系（建議 key `g-overridden`）；③前端 FR-5 欄位 11、原始值檢視三項標籤—值列逐字渲染，不得自行拼接或改寫；④互斥檢視元件（生效值檢視／原始值檢視）依條件 46 約束 1-5、14-16 落地，PortfolioContext 補 source 欄位（條件 19）為任務 7-9 出貨前提。
- qa-reviewer：新增本批逐字守門測試（含標點）；`dividend_reason_code`→句子映射五分支完整性測試；欄位 11 僅 `source=="backtest"` 顯示測試（同任務 3 條件 42）；`g-overridden` 錨點測試（`source=="backtest_overridden"` 且過期時觸發，`anchored_on` 純日期渲染同 6-A）；互斥檢視四情境單元測試（同條件 46 約束 14）與反向守門（禁用欄位不得出現，約束 16）。
