# stock-desk 族群動能排行 — 方法論（第一階段，2026-09-24，v1 草案第三版）

> 依據：`work/stock-desk-族群動能-派工單.md`。
> - §1：CEO 裁定採「族群動能排行＋歷史比例」框架，不做「預測會漲」。
> - §4：風控預審，QR-1～QR-6。
> - §5：風控第二次裁定，口徑與 point-in-time 缺口。
>
> 必守：`.claude/skills/backtest-protocol/SKILL.md` 的五條鐵律與紅線。
> 出稿：quant-researcher。**本輪只寫方法論，沒有寫程式、也沒有跑回測，所以本文沒有任何實證數字。**
> 用語：不用「命中率」；統計量稱為「歷史比例」或「高於基準次數」。API 欄位用 `beat_count`、`sample_count`、`base_rate`（QR-6）。
> 狀態：待 qa-reviewer 審方法。§12 的回補路徑 (b) 方法草案待風控審。§10 待 data-engineer 回覆。

### 第三版修訂摘要（對應風控第二次裁定）

| # | 修訂 | 位置 |
|---|---|---|
| 1 | G0 加入 D9 費率查證（`verified_on` 不得為 null）；費率未查證時，`gate_status` 只能是 `not_evaluated` | §6.3 |
| 2 | 三態 `passed`／`failed`／`not_evaluated` 各有明確判定條件。存活者偏差、分類 PIT、除權息歷史三項中任一有缺口，就是 `not_evaluated`。**刪除原 T6「書面接受分類 look-ahead 即放行」的例外** | §6.3、§8 T6 |
| 3 | **前瞻累積**成為轉態主路徑 (a)：從 ETL 上線日起，逐日保存 PIT 上市名單、產業分類、除權息公告；累積到 N ≥ 150 個不重疊樣本才判定，整段視同樣本外。偏誤版回測只能作內部研究，一律標示「含已知偏誤，不得上畫面」 | §5、§11 |
| 4 | 回看窗長 L（5 或 20）與持有期 H（5）拆成兩個參數。L 由 `method_version` 靜態決定，不得依門檻結果切換；一旦切換就是新版本，並計入 Holm 比較次數 | §3、§11 |
| 5 | 判定：p_net 對 max(q_gross, 50%)＋5 個百分點。呈現：主視圖 p_net 對 q_net；「詳細」列出四個數字。「均量」改為成交金額倍數，只作描述，不進排名 | §4、§6 |
| 6 | 新增回補路徑 (b) 的偏誤上界估計方法草案（只有方法，不執行） | §12 |

---

## 0. 先講結論（包括不好看的部分）

1. **第一階段上線的是「排行」，不是「歷史比例」。** 排行只描述已發生的相對強弱：近 5 日族群等權報酬減等權全市場報酬，照常上線。**歷史比例在主視圖和「詳細」都不列示**（風控第二次裁定 §5-4）。原因是目前拿不到三項資料：已下市個股、各時點的產業分類、歷史除權息。用現有資料回推過去，會帶進幅度無法估計的系統性偏誤，而顯著性門檻處理不了系統性偏誤。
2. **想列示歷史比例，要靠時間累積**（路徑 a）：從 ETL 上線日 D0 起逐日保存當時的資料。滿 150 個不重疊的 5 日樣本才判定，大約 3 年。這些樣本在凍結參數之後才產生，所以是最乾淨的樣本外資料。
3. **即使累積滿 3 年，最可能的判定結果仍然是 `failed`。** 門檻要求扣成本後的第 1 名比例 p_net 超過 max(q_gross, 50%) 至少 5 個百分點，而且 95% 下界也要高於 max(q_gross, 50%)。一次來回成本約 0.585%（費率尚未查證），事前估計大約會吃掉 8–12 個百分點，換算下來，扣成本前的比例大約要 65% 才過得了。產業動能的文獻證據主要在月級別；週級別比較常見的是短期反轉。所以我對結果的事前預期偏不利。
4. **窗長固定為 v1 的 L=5**，由 `method_version` 靜態決定。L=20 是已登記、但尚未啟用的候選。之後如果要切換，就是新版本：畫面五處同步修改、送風控限縮複審、告知 CEO，並計入 Holm 比較次數。**不得因為某個窗長過了門檻就改用那個窗長。**
5. **「記憶體族群」第一階段做不到**，因為官方產業別只有「半導體業」。**族群內成分股也不做任何評價**：只依近 5 日漲跌幅列出，說明族群的數字由誰貢獻（QR-5）。

---

## 1. 方法摘要

| 項目 | v1 設計 | 為什麼 |
|---|---|---|
| 族群 | TWSE 官方產業別（`t187ap03_L` 的兩碼代碼） | 唯一有公開來源、而且不是事後命名的分類 |
| 母體 | 只含上市普通股，並依上市天數與流動性篩選（§2.3）；常駐「僅上市」tag | 上櫃的產業別目前是 NULL，不自行猜測合併 |
| 權重 | **等權**，期初等權後持有至期末（§2.2） | 沒有流通股數資料；等權最好解釋，也照得著買 |
| 報酬 | 回看窗：遇除權息日的成分股**排除**（§2.4 選項 2）。標籤：以前瞻保存的事件還原 | 依風控第二次裁定 §5-4 |
| 排名 | **單一變數**：R_g(t, L) − R_EW(t, L)，v1 的 L=5 | 風控 R-B 規定排名只用 S_A 單一變數 |
| 描述欄位 | 上漲 k／n 家（同一個 L 窗）；成交金額 5 日均／20 日均倍數（只放「詳細」） | 只描述，不進排名 |
| 基準 | **等權全市場**（B_EW，同母體、同方法）用於判定與主視圖。加權指數只放在「詳細」當參考 | 等權對等權，才能把族群效果和規模效果、台積電權重、股利分開（§5.5） |
| 標籤 | t 日收盤出排行 → t+1 開盤進場 → 持有 H=5 個交易日，t+5 收盤出場；扣來回成本後報酬高於 B_EW 才算「高於」 | 使用者收盤後才看到排行，最早只能隔天開盤動作 |
| 判定資料 | 路徑 (a) 前瞻累積，N ≥ 150；路徑 (b) 回補，須另外通過偏誤上界審查（§12） | 風控第二次裁定 §5-4 |
| 狀態 | `passed`／`failed`／`not_evaluated`（§6.3） | 同上 |

**與既有設施的關係**（本輪不寫程式，只列接點）：

- **沿用**：
  - `app/backtest/costs.py` 的 `CostModel`
  - `app/backtest/splits.py` 的 `walk_forward_splits`
  - `app/backtest/episodes.py` 的 `wilson_interval`
  - `app/backtest/event_study.py` 的 `non_overlapping_indices`
  - `app/backtest/report.py` 的 `PerformanceMetrics`
  - `app/dividends/adjust.py`
- **不引用** `app/advice`、`app/signals` 的評價輸出（風控 §4.1d；dev-lead §4.7：sectors 不得 import advice）。
- **缺口**：`run_backtest` 是單一資產引擎。需要一個 point-in-time 的多資產籃子回測器，以及一個 PIT 快照儲存（§5.2），這兩者由 tech-architect 併入 ADR-0012 修訂。

---

## 2. 族群指數構建

### 2.1 等權 vs 市值權重

- 市值權重在第一階段**做不到**：沒有流通股數。用成交金額加權當替代也不行，因為成交金額和當期報酬正相關，權重本身就帶著報酬資訊。
- 等權的**好處**：一句話說得清楚，「族群內每一檔漲跌幅的平均」；而且等金額買一籃子就能照做。
- 等權的**代價**：偏重小型股；成分少的族群容易被單一個股左右。
- **v1 採用等權**，基準也用同一種等權方法。
- 不做 winsorize，因為那會多出一個可以調的參數。改為輸出「單一成分股最大貢獻比例」：超過 50% 時標記 `single_stock_dominated=true`，只揭露，不影響排名。
- 市值權重留待第二階段，屬於新假設，要另外登記。

### 2.2 算法：期初等權、期間持有

```
R_g(t, L) = mean_{i in C_g(t, L)} ( P_i(t) / P_i(t−L) − 1 )
# C_g(t, L)：第 t 日合格、而且 (t−L, t] 窗內沒有除權息日、價格完整的成分
```

不採用「每日等權報酬連乘」。那等於每天再平衡，會因為買賣價差反彈（bid-ask bounce）而系統性高估小型股的報酬。窗內價格不完整（例如停牌）的成分股，排除並計數揭露。

### 2.3 成分股與覆蓋率門檻（只用 ≤ t 的資料；覆蓋率門檻依風控 R-E 核可）

| 門檻 | v1 值 |
|---|---|
| 證券類別 | 只含上市普通股。排除 ETF／ETN、存託憑證（代碼 91）、特別股、受益證券、權證 |
| 其他業（代碼 20） | 不作為族群排名，但成分股**仍計入 B_EW** |
| 上市天數 | ≥ 60 個交易日 |
| 流動性 | 20 日成交金額中位數 ≥ NT$1,000 萬，且 20 日內有成交的天數 ≥ 18 |
| 族群最小成分數 | 經過流動性篩選、除權息排除之後，合格成分 **≥ 5 檔** |
| 族群覆蓋率 | **≥ 90%**；未達者不列入排行，列在「詳細」 |
| 整體覆蓋率 | **≥ 98%**；未達時整卡降級為「不足」 |

**覆蓋率的分母**是第 t 日 PIT 母體中當日應有交易的股票，並扣除公告暫停交易的股票（暫停交易另外揭露；拿不到暫停交易名單時，暫停交易的股票算作缺漏）。**分子**是有效日線的檔數。

**小族群偏誤監控**：比較第 1 名族群在各成分數組別（5–9、10–19、≥ 20 檔）的出現頻率，和隨機名次的期望頻率。某一組超過期望的 2 倍時，標紅揭露。

### 2.4 除權息、減資與面額變更（QR-4）

- **回看窗（排行，決策端）**：v1 固定採用**選項 2，排除除權息日**。成分股的除權息日如果落在 (t−L, t]，這一檔就從該窗排除。除權息日在第 t 日已經是已知資訊（`TWT48U_ALL` 逐日同步、逐日保存），不會洩漏未來。
  - **代價**：7–8 月高殖利率族群的成分股會被大量排除，族群可能因此跌破覆蓋率或成分數門檻，當期不列入排行。這種情況照實揭露。
  - **上線條件**：`TWT48U_ALL` 的同步紀錄至少要涵蓋最近 L 個交易日，否則無法確認窗內有沒有除權息事件，排行不上線。
- **標籤（持有期的結果端）**：持有人真的會領到股利，所以標籤必須還原，否則會把股利記成損失。這不是 look-ahead，因為標籤本來就是事後觀察的結果。
  - 還原因子取自**前瞻保存**的除權息事件（前收與參考價，或可以換算成乘法因子的欄位），方法沿用 `app/dividends/adjust.py`。
  - 因子必須是**乘法因子**；如果來源是加法調整，報酬就不是尺度不變量，§8 T4 的論證不成立。
- **減資、面額變更、分割**：同樣需要事件表。另外設一道保險：窗內單日報酬超過漲跌幅限制加容差時，視為有未處理的公司行動，把這一檔排除並記錄。

---

## 3. 排名訊號與參數拆分

### 3.1 候選清單（QR-3：全部事先登記）

| 代號 | 定義 | v1 狀態 | look-ahead 風險點 |
|---|---|---|---|
| C1 | R_g(t,5) − R_EW(t,5) | **v1 啟用**（`method_version` 的 L=5） | ① 用 t 日收盤出訊號，又以 t 日收盤成交；② 用今天的名單回推母體，造成存活者偏差；③ 還原方式是加法調整；④ 盤中用不完整資料更新排行，和回測不一致 |
| C2 | R_g(t,20) − R_EW(t,20) | 已登記，未啟用 | 同 C1；窗越長，越容易碰到除權息 |
| C3 | 上漲家數比 | 已登記；**只作描述**（「上漲 k／n 家」），不進排名 | 除息日未處理會被算成下跌；暫停交易會被算成平盤；成分股必須在第 t 日決定 |
| C4 | 成交金額倍數（5 日均／20 日均） | 已登記；**只作描述**，只放「詳細」 | 必須用金額不用股數；必須是盤後全日值，不能用盤中快照 |
| C5 | C1 與 C2 的名次平均 | 已登記，不檢定 | 無法合規描述（「綜合」是 VETO 字），成分股也無法依同一變數排序 |
| C6 | C1～C4 的任意組合 | 已登記，不檢定 | 同 C5 |

名次同分時，依族群代碼升冪排列。

### 3.2 參數拆分：回看窗長 L 與持有期 H

- **L（回看窗長）**：只能是 5 或 20，由 `method_version` 靜態決定，v1 為 L=5。畫面上五處同時跟著 L 走：標題 chip、族群報酬、上漲家數、成分股表頭與排序、降級句。
- **H（持有期）**：v1 為 H=5，和 L **不連動**。例如 L=20 時，H 仍然可以是 5。再排名的間隔等於 H，所以樣本彼此不重疊。
- **禁止動態選窗**：不得依門檻結果（或任何統計結果）在執行期間切換 L。切換 L 或 H 一律視為新版本，必須依照 §11.2 處理：凍結、計入 Holm、送風控限縮複審、告知 CEO。
- 我事前在統計上比較偏好 L=20，因為雜訊比較小，也比較不會撞上週級別的反轉。但 v1 為了和定稿字面、產品一致，採用 L=5。這個取捨已寫明，而且是在看任何資料之前決定的。
- **多重比較的計數規則**：只要某個版本或訊號在**同一段判定資料**（前瞻資料庫）上算過統計量，就計入比較次數 m，即使那個版本從來沒上過畫面。v1 的 m 從 1 開始。在前瞻資料庫上計算 C2，m 就變成 2，而且之後的每一次判定都要用新的 m。偏誤版內部研究使用的是 D0 之前的資料，和判定資料不重疊，所以不計入 m。

---

## 4. 資料與訊號輸出定義（給 dev-lead、tech-architect、frontend-engineer）

每個完整交易日、每個族群輸出一列。欄位不使用 `hit_rate`、`win_rate`、`score`、`rating`。

| 欄位 | 含義 |
|---|---|
| `method_version` | 例如 `sector-rel-v1.0-L5-H5`。線上排行與判定共用同一個版本字串 |
| `lookback_days` / `holding_days` | L、H，由版本決定，唯讀 |
| `as_of` | 最後一個全市場完整交易日（所有族群用同一日期） |
| `market_scope` | `twse_only` |
| `sector_code` / `sector_name` | 官方產業別 |
| `rank` | 名次；不合格時為 null，並附 `excluded_reason` |
| `sector_return_L` / `benchmark_return_L` / `rel_return_L` | **只描述過去** |
| `up_count` / `constituent_count` | 上漲 k／n 家（同一個 L 窗） |
| `turnover_value_ratio_5_20` | 成交金額倍數＝族群成分股近 5 個交易日平均成交金額 ÷ 近 20 個交易日平均成交金額（20 日含最近 5 日）。**只放「詳細」，只作描述，不進排名** |
| `coverage_ratio` / `missing_count` / `suspended_count` / `ex_date_excluded_count` | 覆蓋率與排除原因 |
| `top_contributor_share` / `single_stock_dominated` | §2.1 |
| `gate_status` | `passed` / `failed` / `not_evaluated`（§6.3） |
| `not_evaluated_reason` | enum：`pit_history_missing`、`accumulating`（附 `accumulated_samples`、`accumulation_start`）、`fee_unverified`、`stale_recompute`、`version_mismatch`、`data_quality`、`lookahead_tests_failed`、`demo_data`。前端依此選擇原因句（見 §6.3 的待補字面） |
| `historical_stat` | **`not_evaluated` 時必須是 null**：API 層就不給數字，前端無從誤用。只有 `passed` 或 `failed` 時才有值，內容：`rank_scope="rank_1"`、`sample_count`、`beat_count_net`、`beat_count_gross`、`base_rate_net`、`base_rate_gross`、`ci_low_net`、`ci_high_net`、`benchmark="equal_weight_market"`、`sample_start`、`sample_end`、`computed_at`、`method_version` |
| `constituents[]` | `symbol`、`name`、`return_L`、`held`。依 `return_L` 降冪排列，同分依代碼；不附任何名次數字、任何規則引擎欄位 |
| `data_source` | `demo_synthetic` 時強制 `not_evaluated` |

**偏誤版研究的結果永遠不經過這個 API**（§5.4）。

---

## 5. 回測與判定設計

### 5.1 時序規則（唯一版本）

```
第 t 日收盤後：只用 recorded_at ≤ t 的資料決定母體、分類、排除、訊號、名次
第 t+1 日開盤：等金額買進第 1 名族群的合格成分（名單固定為第 t 日判定結果）
第 t+H 日收盤（H=5）：全部賣出
excess_net   = R_basket(開盤 t+1 → 收盤 t+H) − 來回成本 − R_EW(同期間)
excess_gross = R_basket(同上) − R_EW(同期間)
「高於」 ⇔ excess > 0（剛好等於 0 不算）
下一次：第 t+H 日收盤出排行 → 第 t+H+1 日開盤進場（樣本不重疊）
```

- **開盤漲停買不到**：t+1 開盤價 ≥ 參考價 × 1.095 的成分股，從籃子剔除（事先登記）。跌停賣不出去不另外模擬，只揭露。
- **診斷版本**（不作為判定依據）：另算收盤 t → 收盤 t+H。如果優勢只出現在這個版本，代表優勢來自隔夜跳空，使用者實際上拿不到。
- **起點相位**：共 5 種相位。主結果固定從 D0 之後第一個合格交易日起算，其餘 4 種相位全部列出，不挑選。
- 等權全市場是對照，**不扣成本**；族群一方每個樣本各扣一次來回成本（風控定稿「詳細」句）。

### 5.2 路徑 (a) 前瞻累積（主路徑）

**PIT 快照儲存**（實作歸 tech-architect／data-engineer；以下是我對它的方法論要求）：

| 快照 | 內容 | 規則 |
|---|---|---|
| 上市名單 | 代號、證券類別、上市日、狀態（含暫停交易） | 每個交易日保存一份，附 `recorded_at` 與內容 hash |
| 產業分類 | 每檔的 `t187ap03_L` 代碼 | 同上。分類改變時，照實留下新舊兩筆 |
| 除權息公告 | `TWT48U_ALL` 每日全量 | 同上。除權息日當天或前一天的那份，必須帶有算出乘法因子所需的欄位；如果沒有，要另外保存當天的參考價 |
| 日線 | 全市場開、高、低、收、股數、金額 | 同上 |

- **只增不刪、不覆寫**：更正以新的一列寫入（附 `recorded_at`），不修改舊列。判定時，第 t 日只能讀 `recorded_at ≤ t` 的列。
- **快照缺漏**：某日沒有抓到快照時，沿用前一份（那份在第 t 日確實已知，仍然符合 PIT），並標記 `carried_forward=true`。連續缺漏超過 5 個交易日時，受影響的樣本視為無效，不計入 N，並記錄在執行紀錄中。**不得**用事後抓到的資料回填成當時的快照。
- **不需要回溯的舊資料**：D0 當天的上市名單、分類就是當時的 PIT 資料。這些股票在 D0 之前的日線，在 D0 時已經是已知資訊，可以拿來暖身（上市天數、20 日流動性），不構成存活者偏差，因為母體是在 D0 決定的，不是事後回推。所以樣本從 D0 起就能開始累積。
- **整段視同樣本外**：`method_version` v1 的參數在 D0 之前就凍結並 commit，前瞻資料在凍結之後才產生，沒有任何參數用這些資料擬合過。walk-forward 在這裡沒有訓練窗可言。我仍然用 126 個交易日一段切開報告（沿用 `walk_forward_splits` 的測試窗幾何），供 G5 穩定度檢查與逐段揭露使用。
- **時程**：N ≥ 150，以一年約 49 個樣本計算，大約需要 3.1 年。主視圖顯示 `accumulated_samples`（依風控定稿句「目前已累積 {n} 個」）。
- **首次轉態**（從 `not_evaluated` 變成 `passed` 或 `failed`）**必須重送風控**。

### 5.3 walk-forward 在本案的角色

backtest-protocol 鐵律 3 要求 walk-forward，並標示樣本內與樣本外。本案處理如下：

- **判定資料**：前瞻段，全部是樣本外，逐段列出。
- **偏誤版內部研究**（§5.4）：用 `walk_forward_splits(train=504, test=126, step=126)` 分開列出樣本內與樣本外。它的用途是管線驗證和量級參考，**不是判定依據**。

### 5.4 偏誤版回測（只限內部研究）

- **內容**：用現有資料回推 D0 之前的期間。會用到現行分類，而且可能缺少已下市個股與歷史除權息。
- **標示**：每一份輸出、每一張圖、每一列執行紀錄都要標示**「含已知偏誤，不得上畫面」**，並列出三項偏誤各自的預期方向：
  - 存活者偏差：往上高估；
  - 分類 look-ahead：偏向動能；
  - 未還原的除權息：高殖利率族群被低估。
- **隔離**：結果不進 API，也不進 `historical_stat`。由 tech-architect 在 ADR-0012 規定程式邊界，建議加一個測試，證明 API 的統計量只從 PIT 快照儲存計算。
- **不得用來調參**：看過偏誤版結果之後，如果想改參數，就是新版本，必須在前瞻判定前登記；而且因為研究者已經看過相關資料，保守起見也計入 m。
- 結果不論好壞都寫進只增不刪的執行紀錄（§11.2）。

### 5.5 成本模型

- 費率一律透過 `CostModel` 從設定讀取。目前的預設值是手續費 0.1425%（買賣都收，法定上限）、證交稅 0.3%（一般股，賣出時收），一次來回約 0.585%。**`verified_on=null`，尚未查證。** 查證前 `gate_status` 只能是 `not_evaluated`（§6.3 G0）。
- 滑價主結果用 0 bps；敏感度另列每邊 10 bps、20 bps。
- 判定用的比例：每個樣本扣一次完整的來回成本。策略層報告則依實際週轉扣成本。量級參考：如果每週都完全換籃，成本拖累約 28.7%／年。

### 5.6 策略層標準報告（backtest-protocol 欄位缺一不可）

策略定義：每 H 日持有第 1 名族群的等權籃子，依實際週轉扣成本。欄位用 `PerformanceMetrics` 的全部欄位：CAGR、年化波動、Sharpe／Sortino、最大回撤與起迄日、勝率、盈虧比、交易次數、週轉率，並和 B_EW、B_BH 並列。這份報告是給 qa-reviewer 與 CEO 看的研究報告，其中「勝率」這個名稱不會出現在 API 或畫面上。

### 5.7 基準：加權指數 vs 等權全市場

| 基準 | 用途 |
|---|---|
| **B_EW 等權全市場**（同一合格母體；包含其他業、成分不足族群的股票；同一種算法、同一種除權息處理） | 判定用，也是主視圖的基準；畫面寫全名「等權全市場」 |
| B_BH 發行量加權股價報酬指數，買進持有（沒有時改用 0050 還原價，並標示） | backtest-protocol 要求的 Buy & Hold 對照；沿用 `BUY_AND_HOLD_NOTE` |
| B_TAIEX 加權股價指數 | 只放在「詳細」當參考，另外算一組 q |

**等權族群對加權指數的結構性偏差**：

1. **規模效果**：等權偏重中小型股。拿它和加權指數比，差距大多反映的是大小型股的報酬差，而不是族群強弱。
2. **單一個股集中**：台積電在加權指數中的權重極高，「高於加權指數」在很多期間幾乎就等於「高於台積電」。大型權值股領漲的期間（例如 2023–2025 年），幾乎所有等權族群都落後，q 會遠低於 50%。
3. **產業權重不對稱**：用加權指數當基準時，等權的半導體族群實際上是在和台積電比。
4. **股利**：價格指數不含股利，和還原後的族群報酬尺度不同。
5. **B_EW 的處理**：兩邊用同一種權重、同一個母體、同一種還原方式，上述偏差大多互相抵銷。報告會並列以 B_TAIEX 為基準的 q，讓第 2 點的偏差直接用數字呈現。

---

## 6. 統計量、檢定與三態門檻

### 6.1 統計量（只用判定資料，也就是前瞻段）

| 統計量 | 定義 |
|---|---|
| N（`sample_count`） | 有效、不重疊的 H 日樣本數 |
| k_net、p_net | 第 1 名族群 excess_net > 0 的次數與比例 |
| k_gross、p_gross | 第 1 名族群 excess_gross > 0 的次數與比例 |
| q_net、q_gross（`base_rate_*`） | 同樣那些週裡，**全部合格族群**「高於」比例的平均（先算每一週，再跨週平均）；q_net 是每個族群都扣一次來回成本後的版本 |
| 95% 區間 | Wilson（用 `wilson_interval`），另附 block bootstrap 百分位區間；信賴水準依 m 校正為 1 − 0.05/m |
| N_eff | N ×（二項變異數 ÷ block bootstrap 變異數） |
| 平均與中位數超額報酬 | 扣成本前、扣成本後各一組，附 bootstrap 區間 |
| 分名次統計（QR-2） | 第 1 名到第 K 名，各名次的上述數字；**主視圖只用第 1 名**，其餘名次只放研究報告 |

**呈現口徑**（依風控第二次裁定定稿）：

- **主視圖**：p_net 對 q_net，**兩者都已扣來回成本**。
- **「詳細」**：p_gross、q_gross、p_net、q_net 四個數字都列，並標明口徑。
- **禁止**把 p_net 和 q_gross 並排呈現（VETO）。

### 6.2 檢定

1. **Wilson 下界**：以 p_net 計算，信賴水準 1 − 0.05/m。
2. **Block bootstrap**：circular block，平均區塊長度 4 個樣本，10,000 次，seed 記錄。區塊長度 2 和 8 的結果也要列出。
3. **隨機族群置換檢定**：每週從當週合格族群中隨機抽一個，取代第 1 名；重複 10,000 次。p 值 < 0.05/m 才算通過。
4. **多重比較**：m 依 §3.2 的規則累計。只有一個版本會上畫面，所以直接用 α/m 判定，比 Holm 的逐步程序更保守。

**檢定力**（假設樣本獨立；序列相依時需要更多樣本）：單尾 α=0.025、檢定力 80%

| Δ（相對於 50%） | 需要的 N | 約幾年（每年約 49 個樣本） |
|---|---|---|
| +5 pp | 783 | 16 |
| +8 pp | 305 | 6.2 |
| +10 pp | 194 | 4.0 |
| +15 pp | 85 | 1.7 |

| N | 可偵測的最小真實比例 | Wilson 下界 > 50% 所需的觀察比例 |
|---|---|---|
| 60 | 約 68% | 約 63.3% |
| 150 | 約 61% | 約 58.7% |
| 250 | 約 59% | 約 56.4% |

**扣成本的影響（事前估計，不是實測）**：假設族群 5 日超額報酬的標準差在 2–3%，來回成本 0.585% 大約是 0.2–0.3 個標準差的平移，會讓比例下降約 8–12 個百分點。再加上 +5 個百分點的效果量，扣成本前的比例大約要 65% 才過得了。

### 6.3 三態判定（v1 凍結）

令 **b = max(q_gross, 50%)**。

**`not_evaluated`**：前提不成立。只要下列任一項成立，就是這一態：

| # | 條件 | reason |
|---|---|---|
| NE-1 | 判定資料不是 PIT：母體不含當時已下市個股（存活者偏差）、分類不是當時分類、除權息沒有當時的事件紀錄。**三項中任一有缺口就算**。偏誤版回測一律屬於這一條 | `pit_history_missing` |
| NE-2 | 前瞻累積的有效樣本 N < 150，或 N_eff < 60 | `accumulating` |
| NE-3 | **D9 費率尚未查證**（`CostModel.verified_on` 為 null） | `fee_unverified` |
| NE-4 | 距離上次重算已超過 20 個交易日 | `stale_recompute` |
| NE-5 | 線上的 `method_version` 和統計量的 `method_version` 不一致 | `version_mismatch` |
| NE-6 | data-engineer 的品質檢查沒有通過（未來日期、重複、覆蓋率） | `data_quality` |
| NE-7 | §8 的 T1～T9 沒有全部通過 | `lookahead_tests_failed` |
| NE-8 | 資料是 `demo_synthetic` | `demo_data` |

- 呈現方式：主視圖和「詳細」都**不列出任何比例數字**；API 的 `historical_stat` 回傳 null。
- 「未達門檻」這句話**不得**用在這一態（風控 §5-4）。
- **待補字面**：風控定稿的 `not_evaluated` 句，目前只涵蓋 NE-1 和 NE-2（資料缺口與累積中）。NE-3～NE-8 需要各自的原因句，請 creative 起草、送風控審。

**`failed`**：前提全部成立（NE-1～NE-8 都不成立），統計也算過了，但下列任一項不成立：

| # | 條件 |
|---|---|
| G1 | N ≥ 150，而且 N_eff ≥ 60（風控暫定 60，這裡調嚴為 150） |
| G2 | p_net 的 Wilson 下界 > b，**而且** block bootstrap（區塊長度 4）的下界 > b；兩者的信賴水準都是 1 − 0.05/m |
| G3 | **p_net − b ≥ 5 個百分點**（事先訂定的最小效果量） |
| G4 | 置換檢定 p 值 < 0.05/m；扣成本後的平均超額報酬點估計 > 0 |
| G5 | 各 126 日段中，至少前半段與後半段的 p_net 都 > b；5 種相位的 p_net 都 > b；剔除最常出現的族群之後，p_net 仍 > b |
| G6 | 最近 12 個月（約 49 個樣本）的 p_net > b |

- 呈現方式：主視圖用風控定稿的降級句；「詳細」照實列出 k／N、四個數字與區間。

**`passed`**：前提全部成立，而且 G1～G6 全部成立。

- 呈現方式：主視圖用風控定稿的歷史句（p_net 對 q_net）；「詳細」列出四個數字、定稿四句、區間、檢定方法、各段結果、成本參數與查證日期，以及 Buy & Hold 對照。
- 極端事件註記只在這一態、只放「詳細」。

**轉態規則**：

- 每完成一個新樣本就重算一次。
- 從 `passed` 變成 `failed` 是自動、立即的。
- 從 `failed` 變成 `passed`，只在季度檢視時處理，並須經 qa-reviewer 確認。
- 從 `not_evaluated` **第一次**轉成其他狀態，必須重送風控。

---

## 7. 族群內成分股列示（QR-5）

### 7.1 v1：只描述，不評價

- 排序依據是成分股自己的近 L 日漲跌幅（v1 為 5 日，已依 §2.4 排除除權息），和族群排名用的**是同一個已發生變數**。族群的 R_L 就是這些數字的等權平均。
- 同分時依代碼排列；不使用任何第二變數。
- 支持風控 §4.1f 的建議：列出「漲跌幅最大 2 檔＋最小 1 檔」。這樣呈現的是族群內的離散程度，比只列前 3 檔更誠實。
- 不附歷史比例，也不附任何規則引擎輸出。這份清單不宣稱任何未來性質，所以**不需要回測**。成分股層級的比例**不計算、不呈現**。
- 要讓 CEO 知道：排在最前面的，必然是已經漲最多的股票；個股的週報酬常見短期反轉。這就是定稿句「列示順序僅依近 5 日漲跌幅，不代表任何優先順序」存在的理由。

### 7.2 待辦：另立方法論（不在本階段）

- 暫定名稱為「族群成分股觀察條件對照（研究）」。名稱不得含分數、評分、評級、rating。
- 前提：先完成個股層級的驗證，套用和 §6.3 同一套三態門檻，並另外送風控。
- 已知問題：
  - 五條件刻畫的是「拉回」，和動能可能方向相反；
  - 第 6 條依賴持倉狀態；
  - 規則引擎的權重代表優先序，不是機率；
  - 可能和個股頁「本次未支持進場」的結論衝突。

---

## 8. Look-ahead 與偏誤偵測測試（QR-4；NE-7 要求全部通過）

| # | 測試 | 做法與判定 |
|---|---|---|
| T1 | **未來擾動不變性** | 隨機抽 50 個決策日 t，把 t 之後所有列（價格、名單、分類、事件，並以 `recorded_at` 為準）全部換成雜訊，重新計算第 t 日的母體、排除清單、指數、名次與成分股順序。結果必須逐位元相同 |
| T2 | 向量化與逐日重算一致 | 整段資料一次算出的第 t 列，要等於只餵 ≤ t 資料時算出的最後一列 |
| T3a | **Shift 測試：注入洩漏的正向對照** | 故意把訊號改用 t+H 的收盤（價格往前 shift），比例必須大幅上升。上升幅度的閾值先用合成資料校準再凍結。沒有上升，代表測試本身失效；真實訊號如果已經接近洩漏版的水準，判定為疑似洩漏 |
| T3b | Shift 測試：延遲 1 日 | 預期溫和變差。如果優勢完全消失，標記「優勢可能集中在隔夜或微結構」 |
| T4 | **除權息不洩漏** | 回看窗：排除清單只能依 `recorded_at ≤ t` 的公告決定。標籤：比較全段還原與 PIT 還原的報酬，兩者要一致。測試路徑上必須真的有除權息事件，不能是空測 |
| T5 | **存活者偏差** | 第 t 日的母體，必須等於第 t 日的名單快照，而不是最新的 directory。建一檔在 t+k 下市的股票，它必須出現在第 t 日的母體中 |
| T6 | **分類 PIT** | 第 t 日的族群歸屬，必須等於 `recorded_at ≤ t` 的最新分類快照。建一檔在 t+k 改類的股票，它在 t 必須屬於舊族群。**沒有 PIT 分類就是 NE-1，沒有例外**（原「書面接受即放行」的例外已刪除） |
| T7 | 基準一致 | 第 t 日 B_EW 的成分，等於同一天的合格母體 |
| T8 | 安慰劑 | ① 在族群大小不變的條件下隨機打亂產業標籤；② 把訊號序列循環平移 k 週。第 1 名比例都應該落在 q 的 bootstrap 區間內 |
| T9 | 線上與判定一致（QR-1f） | 同一天的線上 API 名次、成分股順序，要和判定管線的第 t 日結果相同；兩邊共用同一份實作、同一個 `method_version`。盤中不更新排行 |
| T10 | **偏誤版隔離** | API 的 `historical_stat` 只能由 PIT 快照儲存產生。拿偏誤版研究的輸出去餵 API 路徑，必須被拒絕 |

籃子回測器必須在**結構上**保證第 t 日只拿得到 ≤ t 的切片；一碰到未來資料就直接拋出例外。

---

## 9. 失效條件

| 類別 | 條件 | 處置 |
|---|---|---|
| 市場結構 | 漲跌幅限制、交易制度、稅率變更（日期由 data-engineer 查證） | 前瞻期間內如果發生，前後分段報告；由 CEO 決定是否把變更日當成新的 D0 |
| 分類變更 | TWSE 新增或調整產業類別、個股改類 | 由 PIT 快照自然處理。族群定義大改（新增或刪除代碼）時，揭露受影響的樣本 |
| 極端事件 | 加權指數 20 日已實現波動超過歷史第 95 百分位 | 只在 `passed` 狀態、只在「詳細」顯示風控定稿句；高波動期分開統計 |
| 單一個股主導 | `single_stock_dominated=true` | 標記 |
| 資料 | 覆蓋率未達門檻、資料過舊、交易日曆無法確認、快照連續缺漏超過 5 日 | 依風控 §4.5 處理；受影響的樣本不計入 N |
| 統計 | G6 觸發、超過 20 個交易日未重算 | 分別轉為 `failed`、`not_evaluated` |
| 費率 | `verified_on=null` | `not_evaluated`（NE-3） |

---

## 10. 需要 data-engineer 提供的資料

| # | 資料 | 路徑 (a) 前瞻 | 路徑 (b) 回補 |
|---|---|---|---|
| D1 | 全市場日線（開、高、低、收、股數、**金額**） | 從 D0 起每日保存 | 歷史逐日資料，**含已下市個股** |
| D2 | 證券主檔（類別、上市日、下市日） | 每日快照 | 含已下市個股 |
| D3 | 產業分類 | 每日快照 `t187ap03_L` | 歷史分類與生效日；缺這項就要走 §12 的偏誤上界方法 |
| D4 | 除權息事件（前收、參考價或乘法因子）；減資、面額變更 | 每日快照 `TWT48U_ALL`，並確認除權息日當天可以算出因子 | 歷史事件（需確認是乘法因子） |
| D5 | 加權指數、加權報酬指數日線；0050 | 每日 | 歷史 |
| D6 | 交易日曆 | 必要 | 必要 |
| D7 | 參考價或漲跌停價 | 每日 | 歷史 |
| D8 | 暫停交易名單（必要）、處置股、全額交割股 | 每日快照 | 歷史 |
| D9 | **手續費與證交稅的法規查證**，寫入 `CostModel` 設定的 `verified_on` 與來源 | **NE-3 的前提** | 同左 |
| D10 | 品質檢查、快照 hash、每日覆蓋率、缺分類比例 | 必要 | 必要 |

**最小歷史長度**：

- **路徑 (a)**：D0 前至少 60 個交易日的日線，只用來暖身；之後前瞻累積約 3.1 年，達到 N=150。
- **路徑 (b)**：暖身約 3 個月＋樣本外 150 個樣本（約 3.1 年）；如果要留 walk-forward 研究窗，再加 2 年，合計約 5.3 年。建議從 2016-01-01 起算。

---

## 11. 事先登記與參數歷程（QR-3）

### 11.1 v1 凍結參數（在 D0 之前 commit）

| 參數 | 值 |
|---|---|
| `method_version` | `sector-rel-v1.0-L5-H5` |
| 回看窗長 L | 5（只允許 5 或 20；靜態） |
| 持有期 H／再排名間隔 | 5／5（與 L 不連動） |
| 排名變數 | C1（單一變數）。C2～C6 已登記，未啟用 |
| 母體、上市天數、流動性 | 上市普通股，排除代碼 20（只計入 B_EW）與 91；上市 ≥ 60 個交易日；20 日成交金額中位數 ≥ NT$1,000 萬；20 日內有成交 ≥ 18 天 |
| 最小成分數、覆蓋率 | 5；族群 ≥ 90%；整體 ≥ 98% |
| 除權息 | 回看窗排除（選項 2）；標籤用 PIT 因子還原 |
| 開盤漲停剔除 | ≥ 參考價 × 1.095 |
| 判定 | 三態（§6.3）：b = max(q_gross, 50%)；效果量 5 個百分點；N ≥ 150；N_eff ≥ 60；α/m |
| Bootstrap | 區塊長度 4（另列 2、8）、10,000 次、seed 記錄 |
| 滑價 | 0 bps（敏感度：10、20 bps） |
| 成分股 | 依 return_L 排序，同分依代碼 |
| 敏感度（**只列出，不拿來挑選**） | 流動性門檻 NT$500 萬、NT$2,000 萬；最小成分數 3、8 |

### 11.2 紀錄機制

1. **事先登記**：本文連同上表 commit 之後，把 commit hash 寫在 `work/stock-desk-族群動能-回測紀錄.md` 的第一列。這份紀錄檔只增不刪。
2. **每次執行都寫一列**：日期、程式 commit、資料快照 hash、`method_version`、資料類型（**前瞻判定**，或**偏誤版研究**。後者必須標「含已知偏誤，不得上畫面」）、結果、三態判定、當時的 m。**失敗和難看的結果也要寫。** 已寫的列不得刪改。
3. **新版本**：改 L、H 或任何參數，都要開新的 `method_version`。流程是：寫明改了什麼、為什麼改 → 凍結 → m 加 1 → 送風控限縮複審 → 告知 CEO。新版本的統計量可以用前瞻資料庫重算整段（因為資料是 PIT 的），但計入 m。
4. **禁止動態選窗**：不得依任何統計結果在執行期間切換版本。
5. **反轉是新假設**：如果結果顯示反轉，「改買最弱的族群」也要依第 3 點處理。
6. **審查**：版本變更與首次轉態都要經 qa-reviewer 審查；首次轉態另外要重送風控。

---

## 12. 回補路徑 (b)：偏誤上界估計方法草案（只有方法，本輪不執行）

**適用情境**：data-engineer 已經找到含已下市個股的歷史日線與主檔（D1、D2），也找到了歷史除權息事件（D4），但**歷史產業分類（D3）仍然缺少**。

如果連 D1、D2、D4 也缺，本方法不適用，只能走路徑 (a)。原因是存活者偏差涉及的是「整檔股票消失」，那一檔的報酬本身就未知，無法用有限區間框住。

**目標**：不去猜分類，而是算出「在所有可能的分類下，第 1 名比例會落在哪個區間 [p_min, p_max]」（部分識別，Manski 型邊界），並以最不利的端點判定。

### 12.1 界定「不確定成員」

1. 蒐集所有**有日期的**分類證據，例如：TWSE 公告的產業類別調整（包含新代碼啟用日）、可取得的歷史快照（公開的網頁存檔、第三方資料集附帶的更新日期）、年報等。每一項證據都要附來源與日期，由 data-engineer 整理。
2. 個股 i 在時點 t 的分類：
   - 如果 t 前後兩份有日期的證據一致，就視為**確定**。這依賴一個假設：兩份證據之間最多只改過一次類，不會改過去又改回來。這個假設必須揭露。
   - 如果不一致，或者 t 落在第一份證據之前，i 在 t 就是「**不確定成員**」，它的候選族群集合 S_i(t) 是證據中出現過的所有族群。沒有任何證據的已下市個股，候選集合就是「所有族群」（最寬鬆，也最保守）。
3. 不確定成員一律**計入 B_EW**，因為它是否屬於市場是確定的。受影響的只有族群歸屬。

### 12.2 每週的判定區間

對每個決策日 t：

1. **族群報酬區間**：每個族群 g 的 R_g，在「不確定成員放進或不放進 g」的所有組合下的最小值與最大值。等權平均的極值可以精確計算：由確定成員出發，依報酬由低到高（算最小值）或由高到低（算最大值）逐一加入不確定成員，只要加入後能讓平均往該方向移動就加入。這一步是 O(n log n)。
   族群是否達到最小成分數與覆蓋率門檻，也用兩個極端（全部不確定成員都計入、全部都不計入）分別判斷。只在其中一種極端下合格的族群，視為「可能參與排名」，一併納入下一步。
2. **第 1 名是否確定**：如果某個族群的區間下界，高於所有其他族群的區間上界，第 1 名就是確定的；否則第 1 名不確定。
3. **標籤區間**：對每一個「可能是第 1 名」的族群，用同樣的方法算出 excess_net 的區間。

| 週次分類 | 條件 | 計入 |
|---|---|---|
| **確定高於** | 所有可能的第 1 名，其 excess_net 下界都 > 0 | p_min 與 p_max 都計入 |
| **可能高於** | 至少一個可能的第 1 名，其 excess_net 上界 > 0 | 只計入 p_max |
| 其他 | — | 兩者都不計入 |

q_gross、q_net 用同樣方法求出區間 [q_min, q_max]。

### 12.3 判定與上界

- **偏誤上界**：p_max − p_min，也就是分類不確定性最多能讓比例移動多少。它和抽樣誤差分開報告。
- **保守判定**：用 p_min 對 max(q_max, 50%) 套 §6.3 的 G1～G6，也就是最不利的組合。信賴區間使用 Imbens–Manski 型的「識別區間＋抽樣誤差」合併區間，下界取 p_min 的 Wilson 下界。
- **可行性門檻**（事先訂定）：如果「第 1 名不確定」或「標籤不確定」的週次超過 20%，或 p_max − p_min 超過 10 個百分點，就判定路徑 (b) **不可行**，因為邊界太寬，沒有資訊量。這時只能維持路徑 (a)。
- **驗證**：前瞻累積滿 1 年後，用 PIT 快照實際觀察每年改類的比例，回頭檢查 12.1 的「最多改類一次」假設，以及不確定成員的集合夠不夠大。如果實際改類比例高於假設，路徑 (b) 的結果作廢。
- **送審**：路徑 (b) 的結果（包括區間、不確定週次比例、可行性判定）必須先送風控，才能影響 `gate_status`；首次轉態也要重送風控。在這之前，路徑 (b) 的一切產出都屬於 §5.4 的內部研究，一律標示「含已知偏誤，不得上畫面」。

---

## 13. 已知偏誤與限制

1. **第一階段沒有歷史比例**。在路徑 (a) 累積滿 N ≥ 150（約 3 年）之前，`gate_status` 一律是 `not_evaluated`。
2. **偏誤版研究的三種偏誤**方向各不相同，幅度也無法估計（§5.4），只能作內部參考。
3. **檢定力有限**（§6.2）。「未過門檻」代表證據不足，不代表沒有效果。
4. **序列相依**：用 N_eff、block bootstrap、置換檢定緩解，但無法完全消除。
5. **成交假設**：以開盤價、收盤價全額成交；跌停不模擬；滑價只做敏感度分析；手續費用法定上限。
6. **費率尚未查證**，所以一律是 `not_evaluated`。
7. **只用價量資料**，也只含上市股票。
8. **選用 L=5 是為了配合產品一致性**；在統計上，它可能比 L=20 更吵（§3.2）。
9. **除權息排除的代價**：除息旺季會有族群因為成分被大量排除而不列入排行。
10. **成分股依已實現漲跌幅排序**，排在前面不代表之後還會漲（§7.1）。
11. **前瞻期間也會碰到制度變更**，可能需要重新設定 D0（§9）。

---

## 14. 交接與待辦

- **data-engineer**：
  - 路徑 (a) 的每日快照：D1～D4、D8，特別要確認 `TWT48U_ALL` 能不能算出乘法因子。
  - **D9 費率查證**，這是 NE-3 的前提。
  - 路徑 (b) 的來源調查：歷史日線與主檔（含已下市個股）、歷史除權息、有日期的分類證據。
- **tech-architect**（ADR-0012 修訂）：
  - PIT 快照儲存：只增不刪，帶 `recorded_at`，缺漏時沿用前一份，不得回填。
  - 三態 `gate_status` 與 `not_evaluated_reason`。
  - `not_evaluated` 時 `historical_stat` 必須是 null。
  - 偏誤版研究和 API 在程式層級隔離（T10）。
  - 多資產籃子回測器。
  - sectors 模組不得 import advice。
- **risk-compliance-officer**：
  - 審 §12 的回補路徑 (b) 方法草案；
  - 確認 §6.3 的 NE-3～NE-8 需要各自的原因句（目前的定稿句只涵蓋資料缺口與累積中）。
- **product-manager／creative-lead**：
  - NE-3～NE-8 原因句的起草；
  - 成交金額倍數只放在「詳細」；
  - 採用「最大 2 檔＋最小 1 檔」列示。
- **qa-reviewer**：審本文，重點是 §6.3 的三態、§5.2 的前瞻設計、§8 的 T1～T10、§12；並呼叫 Codex 提供第二意見。
- **quant-researcher（下一輪）**：
  - 在 D0 前 commit §11.1 的凍結參數；
  - 實作 T1～T10 的測試規格；
  - 偏誤版研究只做管線驗證，並依規定標示。
