# 派工單:CODEX-ENV 修復 Codex CLI 環境

- 狀態:done
- 日期:2026-08-06
- 派工者:coordinator(主 session)
- 承辦:devops-sre(背景 agent)
- 分支:claude/ai-company-org-expansion-445xvv
- 預算:輸出上限約 60k token;僅限四步驟,不擴 scope
- 存檔點紀律:每完成一個檔案立即 commit

## 範圍

1. 安裝 `@openai/codex` CLI 並以 `codex --version` 驗證
2. 建立冪等安裝腳本 `scripts/setup-codex.sh`
3. 撰寫 `docs/codex-setup.md`(含認證與降級策略)
4. 檢查 `.claude/` 內 review 相關 skill/command,補降級策略

## 恢復指引(若環境回滾、agent 無聲死亡)

- 檢查上列四項產出是否已 commit(`git log --oneline`、`ls scripts/setup-codex.sh docs/codex-setup.md`)
- 缺哪項就以同樣預算重派 devops-sre 補齊,不必全部重做

## 結果

- 安裝成功:`codex-cli 0.146.1`(`npm install -g @openai/codex`,未遇 TLS 問題)
- 產出:`scripts/setup-codex.sh`(冪等,重複執行驗證通過)、`docs/codex-setup.md`、
  `.claude/commands/review.md` 與 `code-review-checklist` skill 補降級策略
- Commit:`09757a4`(setup-codex.sh,與協調者提交併入)、`cfd5fa5`、`e530788`
- 用量:約 41k token(預算 60k 內)
- 待辦缺口:`OPENAI_API_KEY` 需 CEO 於環境設定提供;未設定前 `/review` 走降級策略
