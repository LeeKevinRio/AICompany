# 派工單:RISK-FIX 風控 VETO 六條必修

- 狀態:done(R1–R6 全部修完,待 qa-reviewer 複審)
- 日期:2026-08-08
- 派工者:coordinator(主 session)
- 承辦:frontend-engineer(背景 agent;R4 含新鮮度判定邏輯,一併承作)
- 分支:product/stock-desk
- 預算:輸出上限約 80k token
- 存檔點紀律:每完成一條 R 立即以指定路徑 commit 並 push origin product/stock-desk

## 範圍(僅 R1–R6,不動 suggested 項)

- R1:`format.ts` insufficient_data 標籤改定調 §1.2 原文「資料不足,本次不提供操作評估」
- R2:建議卡動作標籤改用 `adviceWording.ts` 單一來源(名詞化+歸屬語),刪第二套標籤
- R3:建議卡免責聲明比照 DisclaimerBanner(text-sm、邊框、緊鄰標題)
- R4:過舊顯著提示改以 `last_bar_date` 交易日差計算;來源降級另以徽章表達
- R5:componentWordingScan 補掃 AdviceCardView、format.ts、RiskGauge、LimitsCheckList、operationSummary
- R6:「總資產」三處補限定語「總資產(已估值部位市值)」

詳細規格見 `work/reviews/risk-final-review.md`。

## 恢復指引

- 逐條檢查 R1–R6 對應檔案在 origin/product/stock-desk 是否已有修正 commit;缺哪條補派哪條

## 結果

全部六條修完,逐條 commit + push 至 origin/product/stock-desk:

- R1 `8b348c6`:`format.ts` insufficient_data 標籤改回定調原文「資料不足，本次不提供操作評估」。
- R2 `cd2910e`:刪除 `format.ts` 的 `CARD_ACTION_LABELS`/`cardActionLabel` 第二套裸動詞標籤,
  `AdviceCardView.tsx` 改用 `adviceWording.ts` 的 `buildAttributedHeadline`(「規則評估：加碼參考」形式)。
- R3 `a643a3c`:`AdviceCardView.tsx` 免責聲明從卡片底部 `border-t text-xs` 移到標題旁,
  比照 `OperationSummaryPanel` 的 `DisclaimerBanner`(`text-sm`、邊框、緊鄰標題)。
- R4 `d506cd6`:新增 `lib/tradingCalendar.ts::tradingDaysSince`(附 8 個單元測試),
  `operationSummary.ts` 的過舊顯著提示改依 `last_bar_date` 與當下的交易日差(>1 個交易日才觸發)
  計算,不再誤綁 `cached_stale`;另補 3 個整合測試釘住新行為。
- R5 `88ed5e0`:`componentWordingScan.test.ts` 的 `SCANNED_FILES` 補入 `AdviceCardView.tsx`、
  `format.ts`、`RiskGauge.tsx`、`LimitsCheckList.tsx`、`operationSummary.ts`;掃出
  `RiskGauge.tsx` 既有的「勝率」禁用詞誤用(非 R1/R2 範圍但擋住掃描通過),已改寫為
  「歷史交易的獲勝比例」,語意不變。
- R6 `b0f14f4`:`SettingsForm.tsx` 三處(單一標的佔比上限、總曝險上限、Kelly 部位硬上限)
  與 `RiskGauge.tsx:38` 的「總資產」補限定語「總資產(已估值部位市值)」;`NetWorthSection.tsx`
  的「只用在第 3 條的分母」說明維持原樣未動。

驗證:每條完成後皆跑 `npm run typecheck`(全綠)與 `npm run test`;全部完成後再跑一次完整套件,
`vitest run` 5 個測試檔、125 項測試全數通過。

未動 suggested 項(S1–S7)、未擴 scope。
