# 派工單：確認面板五輪定稿落地（最後微輪）

- 日期：2026-08-12
- 派工對象:dev-lead(跨棧:backend + frontend)
- 狀態:build
- 依據:`work/reviews/快市排程-風控前置意見.md` 末段「五輪定稿(2026-08-12)」(commit f9702e0)
- 性質:風控已 APPROVE 的逐字定稿落地,**文案字面不得再動**;純工程實作,完成後不需回審(生效日路線 (a) 經裁定免回審)。

## 工作項

### 1. 前端 `RuleSetConfirmPanel.tsx` — ⑥ 同意句移位+換文

- 現行 :114-116 的單句(位於按鈕下方)移除。
- 在資本欄位與按鈕(:90 的 `div.mt-5` 區塊)**上方**渲染核可兩句,不摺疊、不 tooltip,`text-sm` 以上、`text-neutral-300` 以上:

  > 確認即表示這份規則集為你本人設定。
  > 確認之後,帳冊裡的每一筆指令,都是你自行設定的規則之機械執行結果,非本系統的判斷或建議。

- 核心子句「你自行設定的規則之機械執行結果,非本系統的判斷或建議」為逐字保護區。
- JSDoc 補「帳冊裡的」為真前提:EXIT 指令永不併入 DirectiveLedger,若日後併入須回審。
- JSDoc 既有 WORDING 段同步(句子位置與內容已變)。

### 2. 前端 — ④ 資金用途句(欄位旁常駐)

- 資本欄位旁常駐渲染(禁 placeholder/tooltip/僅錯誤時顯示):

  > 這個金額同時設定為你目前可動用的現金;系統會以「此金額 ×{deploy_ratio}」計算出部署資金(TOTAL_DEPLOY),之後每一批進場的股數都以這筆部署資金為基礎換算。

- `{deploy_ratio}` 由後端下發**含 % 的完整字串**;前端禁 ×100、禁自行補 %、禁硬編數字;「分母」一詞禁止出現。

### 3. 後端 — `RuleSetResponse` 增欄位

- `GET /api/playbook/rule-set` 回應新增 `deploy_ratio_pct: str`,值 = `wording.ratio_pct(params.deploy_ratio) + "%"`(沿用既有 normalize,無尾零)。
- 守門測試:斷言回應中的 `deploy_ratio_pct` 與 active params 的 `deploy_ratio` 換算一致(渲染數字==active_params 慣例)。

### 4. 後端 — 生效日對齊(路線 (a),免回審)

- 消除「確認當日 in-force 查不到自己那列」的狀態:首次確認的 `effective_date` 對齊評估資料日,或讓 in-force 查詢接受確認當日那列(擇一,以現有 `service.confirm_rules` / `store.in_force_effective_date` 實作侵入最小者)。
- 補測試:確認當日 `GET /today` 的 `rules_effective_date` 即可讀(§6 ②(a) 分支),不落入 (b) 讀取失敗句。

### 5. 防漂移守門(二擇一,擇後端下發或前端守門測試)

- 若不改為後端下發全文:前端 vitest 守門測試斷言 RuleSetConfirmPanel 渲染輸出含核心子句逐字;並確認 componentWordingScan 已涵蓋本檔。

## 驗收

- backend pytest 全綠(基準 1364+),mypy strict、ruff 過。
- frontend vitest 全綠(基準 328+),typecheck 過。
- Conventional Commits(繁中),推送 `product/stock-desk`。

## 回報

- 完成後回報:改動檔案、測試數、commit hash;不得改動任何逐字定稿以外的使用者可見文案。
