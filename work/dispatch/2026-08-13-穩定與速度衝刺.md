# 派工單：穩定與速度衝刺（2026-08-13）

- 依據:CEO 指示「繼續製作,開發讓專案穩定或快速」。
- 三線並行(各自隔離 worktree),完成後 qa-reviewer 批次審查+風控覆核措辭增量。

## Lane 1(data-engineer):行情 provider 鏈快速失敗

- 問題:資料源不可達時,MarketDataService 的 primary+backups 重試×逾時串起來,
  單次請求最壞可拖數十秒(列管紀錄約 58s),前端頁面等待期間形同卡死。
- 目標:來源不可達時**秒級失敗**進入既有降級鏈(快取/unavailable),不改變降級語意
  與誠實揭露;成功路徑行為不變。
- 手段自選(共享 deadline/縮短單源逾時/單請求週期內失敗短路),不得引入背景常駐狀態
  使測試不可重現;參數集中可調並附註解說明取值依據。

## Lane 2(dev-lead):警示過舊判定改真實交易日曆(C4)

- 現況:過舊警示以「日曆日差 >10」保守替代(門檻 10 屬估算值無引註,QA 列管)。
- playbook 已有 TradingCalendar(backend/app/playbook/calendar.py);評估抽至共用位置
  (不破壞 playbook 既有 import 與測試),alerts 過舊判定改真實交易日差,門檻降回
  1 交易日。
- 涉及使用者可見文案的變動:落地新句但**標記待風控覆核**,覆核由協調人隨後統一送審;
  被退修即微輪回改。

## Lane 3(frontend-engineer):D1 風控列管措辭清償批+favicon

- favicon 404:補上 favicon 資產,console 不再噴 404(e2e 驗收長期噪音)。
- D1 清單(裁決出處:work/reviews/ 下 risk-final-review 相關檔,先讀原文):
  S1 時間戳標「(台北時間)」、S2 not_evaluable 空進度條、S3 context_notes 收合、
  S4 放寬確認僅涵蓋總曝險、S5 as-of 空句退化、S6 禁用詞清單前後端鏡像漂移、
  S7 美股選項無完整驗證仍可選;加 adviceWording.ts:124 半形逗號統一、:95 裸「總資產」
  補限定語、SettingsForm.tsx:42「加碼建議/加碼參考」用詞對齊。
  有既定裁決字面的照裁決落地;無字面的自擬並標待審。
- D4(設定頁資料來源區塊過期):**只起草新措辭放進回報,不落地**(裁決要求送審後才改)。

## 驗收

- 各線:backend pytest(基準 1386)/frontend vitest(基準 336)/mypy/ruff/tsc 全綠。
- push 被拒(非網路錯誤)時:`git pull --rebase origin product/stock-desk` 後重推。
- 收攏後:qa-reviewer 批次審查全部 diff;風控覆核 Lane 2/3 的措辭增量+D4 草案。
