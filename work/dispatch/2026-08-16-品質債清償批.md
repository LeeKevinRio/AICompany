# 派工單：品質債清償批（2026-08-16）

- 依據:CEO 指示「繼續下一批開發」;目標:機會清單 D3/D5/D6/D7 全清,列管歸零。
- 兩線並行(隔離 worktree+各自 lane 分支,rebase 後推 product/stock-desk);
  文案增量統一送風控批次覆核後落地;完成後 qa-reviewer 批次審查。

## Lane 1(dev-lead,backend)

- **D7**:positions/store.py `_migrate_add_sector`(:74-89)與
  `_migrate_opened_at_to_nullable`(:92-116)的併發缺陷——沿用 directory store
  的 try/except duplicate column 形狀;rebuild 型 migration 依實況評估最小防護
  (至少不得讓兩行程同時 rebuild 損毀資料,必要時 BEGIN IMMEDIATE);補並發測試。
- **D5①**:app/data/http.py read timeout 8s 的 reachable-but-slow 邊界——若 mock
  架構可驗「回應 7.9s 仍成功」則補假時鐘測試;若 MockTransport 無法模擬讀取延遲,
  以註解記錄理由並改為文件化 8s 取值依據(不硬湊無效測試)。

## Lane 2(frontend-engineer,跨棧允許)

- **D6(P1,CEO 實測回報)**:ETF 持倉在第 2 條(產業佔比)的成因句誤導——先找出
  該句產生處(「此標的的持倉尚未填寫產業別…填入後啟用」,可能在 backend book_limits
  成因或前端);依 instrument_type(leveraged_etf/futures_etf/一般 ETF 判定方式先盤點
  現況欄位)區分:ETF 類改為「ETF 不適用產業分類」方向的誠實成因句(**自擬標待審**),
  個股維持現句。ETF 檔仍列「未納入」(行為不變,只改成因說明)。
- **D5③**:NavBar 窄按鈕的 S7 註記截斷——實測(build 後檢視或量測字寬)是否會出現
  半截揭露;會的話 NavBar 該處改用不含註記的短標籤(marketLabel)或 CSS 容納,
  不得出現半截。
- **D5④**:設定頁 TW 列備援鏈補終局「→ 資料不足」與 US 列格式統一(**標待審**,
  字面照 US 列已核可格式類推)。
- **D5⑤**:US 主要來源欄括號語氣與 S7 對稱化(**標待審**,方向:欄內併入未經真實
  環境驗證的提示或中性化)。
- **D3**:①tradingDaysSince 相關 UTC 跨日行為文件化(現況已改後端交易日曆,確認
  該列管是否已被 C4 自然解決,是則記錄結案);②措辭掃描區分「禁止語境/承認缺失
  語境」防誤殺(掃描 helper 加語境白名單機制+測試);③insufficient_data 分支
  不帶 staleDataNotice(確認現況,若已修記錄結案,未修則修)。

## 風控批次覆核範圍(兩線完成後)

- D6 新成因句、D5④ TW 列字面、D5⑤ US 欄字面;
- 併請裁決 D5②(2026-08-13 衝刺覆核遺留):as-of 與過舊提示雙缺時,方向性建議卡
  是否降級為不提供操作評估(涉行為變更,風控提方案後報 CEO 確認)。

## 驗收

- backend pytest(基準 1488)+mypy+ruff;frontend vitest(基準 383)+tsc+build 全綠。
- 各線 lane 分支作業,`git rebase origin/product/stock-desk` 後
  `git push origin HEAD:product/stock-desk`;被拒非網路錯誤重拉重推。
