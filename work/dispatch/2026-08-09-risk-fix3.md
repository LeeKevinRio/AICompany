# 派工單:RISK-FIX3 風控二次 VETO 三條必修(V1–V3)

- 狀態:done(qa PASS + 風控 APPROVE,2026-08-09 全流程收尾)
- 日期:2026-08-09
- 承辦:frontend-engineer → qa-reviewer(核對後端分母一致)→ 風控複審(僅兩檔)
- 分支:product/stock-desk
- 預算:工程約 30k;qa 約 20k;風控約 20k
- 紀律:修完立即 commit + push origin product/stock-desk

## 範圍(核可措辭見 work/reviews/risk-final-review.md 複審節)

- V1 SettingsForm.tsx:61 第 3 條分母改風控核可句
- V2 SettingsForm.tsx:68 第 4 條補限定語(核可句)
- V3 adviceWording.ts STALE_DATA_PROMINENT_NOTICE 改 A 案帶參數,同步修 :116 過期註解
- 順手(suggested):SettingsForm.tsx:55 第 2 條補齊一致性

## 恢復指引

- origin/product/stock-desk 上兩檔若已含核可措辭 → 工程關已完;再查本單狀態與 reviews 紀錄

## 結果

- 狀態:frontend-engineer 已完成,待 qa-reviewer / 風控複審。
- V1 `SettingsForm.tsx:61`(max_gross_exposure):description 改為
  「組合已估值部位市值合計占你在設定頁自報之帳戶總淨值的上限（比例，可大於 1 代表允許槓桿）。」
- V2 `SettingsForm.tsx:68`(max_loss_per_trade):description 改為
  「以 ATR 停損距離估算，觸及停損時損失占總資產(已估值部位市值)的上限。」
- V3 `adviceWording.ts`:`STALE_DATA_PROMINENT_NOTICE` 常數改為
  `buildStaleDataProminentNotice(lastBarDate, days)` 函式,措辭與審查紀錄逐字一致
  (半形逗號、句末全形句號);`:116` 過期註解已改述為日曆日年齡邏輯。
  `operationSummary.ts` 呼叫端已改帶 `lastBarDate` 與 `calendarDaysSince(lastBarDate)`。
- 順手:`SettingsForm.tsx:55`(max_sector_weight)補上「總資產(已估值部位市值)」限定語
  (已核對 `limits.py`:該條分母同為 `total_equity_twd`,且恆 `not_evaluable`,安全)。
- 測試:`adviceWording.test.ts`、`operationSummary.test.ts` 斷言同步更新為呼叫
  `buildStaleDataProminentNotice(...)`;`componentWordingScan.test.ts` 無需改動(掃描
  對象不含被移除常數名)。
- 驗證:`npm run typecheck` 全綠(0 error);`npm run test` 5 個檔案、130 個測試全數通過。
- Commit:`c862a27`(`fix: 修復風控二次 VETO 之 V1-V3 措辭問題(設定頁分母、過舊資料提示)`),
  已 push 至 `origin/product/stock-desk`。
- 過程註記:提交途中曾遇另一 agent 同時對同分支操作,一次 commit 因 ref lock 失敗、
  後續遠端歷史被改寫,已重新確認工作目錄內容、typecheck/test 全綠後乾淨重 commit 並成功
  push,無內容遺失。
