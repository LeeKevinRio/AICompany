# 2026-09-19 「一眼一句」簡化 — 審查紀錄

- 派工：`work/stock-desk-一眼一句簡化-派工單.md`（CEO 原話、診斷、七部門觀察報告）
- 規格：`work/stock-desk-一眼一句-PRD.md`、`work/stock-desk-一眼一句-視覺規範.md`、`work/stock-desk-一眼一句-文案稿.md`、
  `work/stock-desk-一眼一句-實作規格.md`（dev-lead 收斂稿，施工唯一依據）
- 分支：`product/stock-desk`

## 1. 量測方法

Playwright（Chromium `--no-proxy-server --lang=zh-TW`）開 `http://localhost:3000`，demo 資料（`app.demo.seed`，2330／0050／00631L，
3 條警示規則、0 事件），等 advice 載入後取 `<main>` 各直接子節點 `innerText` 去空白計字。腳本 `scratchpad/measure.mjs`（未入版控）。
注意：沙盒 Chromium 預設語系標籤 `en-US@posix` 會讓 lightweight-charts 拋 `Invalid language tag`、K 線不畫；指定 `--lang=zh-TW` 後正常。
CEO 本機為 zh-TW 不受影響。

## 2. 字數 before／after（預設視圖，未展開任何「詳細」）

| 區塊 | before | after（含徽章列與主視覺文字） | after（僅敘述性內容） | 目標 |
|---|---|---|---|---|
| 技術分析 | 1,746 | 196 | 68 | ≤ 80 |
| 操作摘要 | 668 | 304 | 178 | ≤ 160（超 18：規則名與反面論點為引擎鎖定字面） |
| 關鍵價位參考 | 820 | 484 | 141 | ≤ 160 |
| 六項觀察條件 | 397 | 156 | 134（同步時 56） | ≤ 120（超 14：R8 不同步時的資料時間句） |
| 建議卡（summary） | 1,331 | 90 | 90 | ≤ 110 |
| 個股頁不含頁尾 | ~5,000 | 1,243 | — | ≤ 800（未達；差額為五段資料徽章列＋價位階梯標籤） |
| 首頁風險儀表 | 1,484 | 286（10 行） | — | ≤ 260／≤ 10 行（超 26：H2／H3／H4／H5 風控必留） |
| 首頁全頁 | 1,946 | 791 | — | — |

## 3. 缺陷修正

- `quantity_range.basis` 兩次渲染（`quantityRangeText` 已含 basis，`restoresComplianceWarning` 又是同一段）→ `operationSummary.ts`
  拆 `quantityRangeShares`／`quantityRangeBasis`，basis 全頁一次；`restores_compliance=false`＋防禦型動作時以 `role="alert"` 常駐（風控 R4）。
  新增 `renderToStaticMarkup` 測試斷言 basis 恰出現一次（兩種情境）。

## 4. 各部門結論

- **product-manager**：PRD spec；量化 AC 改絕對字數上限。
- **art-lead**：視覺規範 B.1–B.6。
- **creative-lead**：入口字與一句結論模板（文案稿 B1–B5）；六項觀察沿用 `buildConditionCount`。
- **risk-compliance-officer（預審）**：APPROVE_WITH_CONDITIONS；最低常駐線 R1–R10／H1–H5；免責句可改結論同列 text-xs neutral-400；
  保留否決紀錄：R3／R10 依 Phase 8 §2 第 2、5 項，若 CEO 要推翻需新的書面裁定。
- **data-engineer**：TPEx adapter 改新站端點（`e792ffc`），端點未在沙盒實測，待 CEO 本機 `verify_market_data.py --tpex-symbol 6147`。
  D-8 冷卻持久化在 SQLite，重啟不清；修好後同一代號最多等 1 小時。
- **qa-reviewer（後端 TPEx）**：第一輪 NEEDS_CHANGES（非阻擋，三點 medium：表頭「股」碰撞、版面異常未計入 skipped_months、邊界測試）→
  修正後複審 PASS；殘留 low（`tables` 缺失）與 nit（docstring）已一併收掉。Codex 未執行（本機無 CLI）。
- **frontend-engineer A／B**：個股頁／首頁落地；vitest 703 passed、tsc 綠。B 自行修正手機視口（375px）名稱欄被壓縮問題，改 flex-wrap。
- **qa-reviewer（前端，第一輪）**：PASS、無阻擋。鎖定字面零改動、子集合守門無遺漏、R1–R10／H1–H5 逐條通過、測試改動屬層級語意。
  五項建議：signals 三態被收進 details（medium）、`DETAILS_SUMMARY_ADVICE` 未使用、`defaultOpen` 無消費者、上限名稱 `truncate` 窄版面、
  規則全停用時「0 條規則已設定」。Codex 未執行。
- **risk-compliance-officer（逐字審＋落地核對）**：APPROVE_WITH_CONDITIONS。15 則新字面全數 APPROVE（「已設定」＋「查詢時間」獲採納，
  「啟用中」維持不核可）；R1–R4、R6–R10、H1–H4 符合；R5 不符（signals 狀態被摺疊）、H5 符合但缺守門。
  required：R-A1 signals 狀態回主視圖；R-A2 `oneLinerWording.ts`／`AlertStatusStrip.tsx` 納入掃描並逐字釘住、H4／H5 釘住、
  「無法載入警示狀態：」補常數；R-A3 警示狀態列第四態（總開關關閉）；R-A4 `riskGauge.ts` header 註記 H5 取代 2026-08-09 條件、處理 `defaultOpen`。
  suggested：S1 「查看計算依據」略低估內含物、S2 觀測值最高在 details 重複、S3 「去設定」語氣。
- **第二輪修正**：A 落地 R-A1（signals 徽章列與 Error／Insufficient 回主視圖）、R-A2（兩檔納入掃描、逐字釘住全部新常數與 H4／H5、
  `ALERTS_LOAD_ERROR_PREFIX` 常數化、移除未用的 `DETAILS_SUMMARY_ADVICE`）；B 落地 R-A3（第四態 `ALERTS_SCHEDULER_DISABLED`，
  優先序 C→D→A→B，settings 失敗退回 A/B/C；B 態 N 改規則總數）、R-A4（header 註記、移除 `defaultOpen`、移除上限名稱 `truncate`）。
  vitest 719 passed、tsc 綠。技術分析區可見字數 196→302（signals 徽章列回主視圖；敘述性內容仍 68）。
- **risk-compliance-officer（確認性覆核）**：APPROVE。D 態字面逐字核可；四項 required 均已落地；suggested 三項
  （page.tsx 過期註解——已改；signals 失效訊息巢狀於 bars ok 之內——記錄；D 態在無規則時亦顯示——維持）。
- **qa-reviewer（第二輪）**：第一輪五項建議已全數處理（#1＝R-A1、#2 移除死碼、#3＝R-A4、#4 移除 truncate＋320px 截圖、#5 N 改規則總數）。

## 5. 列管

- 個股頁整頁預設視圖 1,243 字 > 800：五段「資料時間｜來源｜徽章」列重複（可考慮頁級共用一顆徽章，需風控 R5 重審）、`PriceLadder` 九列標籤。
- 首頁警示狀態列「查詢時間」非排程實際評估時間：後端無欄位；data-engineer 另案評估在 `AlertEventListResponse` 加 `last_evaluated_at`。
- 「排程未啟用」第三種空狀態（creative-lead B4 狀態三）：後端無排程狀態端點，本批未做。
- 建議卡 `blocked_notices`（加碼被上限擋下）目前收在 details 內；是否應在 summary 提示，待 CEO 裁決。
- TPEx 新端點成交量單位（仟股／股數）以表頭判斷，仍待 CEO 本機真實回應確認。
- ADR-0009 D-8 冷卻：修復資料源後無法立即生效，devops-sre 評估受控的「清除單一代號冷卻」CLI。

## 6. 同日後續：資料源事故（TPEx 表頭、台銀匯率挑戰頁、持倉體檢）

- **CEO 本機實測**（`verify_market_data.py --tpex-symbol 6147`）：TPEx 新站可連（HTTP 200、stat ok）但真實表頭為「日 期」（含空白）與「成交張數」；
  台銀匯率 CSV 端點每日回 HTML「Challenge Validation」防爬頁；FinMind／Alpha Vantage 未設 key。
- **TPEx 表頭修正**（`9a2c1d1`）：表頭去空白比對、「張」×1000；qa 複審 PASS；CEO 重跑 → tpex PASS、8 筆 fresh。
- **持倉資料體檢**（`06965da`）：`python -m app.data.diagnose [--probe] [--clear-cooldown]`；qa 三輪（PASS、兩點建議已收：以觀察交易日判過舊、
  移除打不到的備援分支）。
- **匯率梯子**（`59bc502`、`1b3602e`）：data-engineer 實作；風控 VETO yfinance 揭露句第一稿 → 定稿落地、挑戰頁原因句 APPROVE、四項條件記入 ADR-0011；
  qa 第一輪 NEEDS_CHANGES（blocking：總覽頁備援匯率無揭露）→ dev-lead 補 `Valuation.fx`／`fx_disclosures`、frontend 補徽章與揭露句 →
  修正期間全套件抓到 playbook 邊界違規（函式內 import 拉進 advice）→ 抽 `services/fx_notes.py` → qa 第二輪 PASS。
  qa 交接：涉及 UI，待 qa-e2e 實機驗收；`FxRateResult.reason` 串到前端列管。
- 全套件：2685 passed，既有列管的多程序 SQLite 偶發 1 例（單跑通過）。

## 7. CEO 第二次裁定批次（主視圖去免責／說明句、標籤白話）

- 裁定與風控否決紀錄／絕對底線／字面定案：派工單 §4.1～§4.3。
- 第一波純搬移（A 個股頁、B 首頁）→ 第二波新字面（風控逐字審：四項 VETO 改採風控定稿）→ 風控確認性覆核 APPROVE
  （一項 required：compact 徽章 fresh＋reason 混源時退回完整渲染，已修）→ qa PASS（兩項非阻擋：候選分支 `showBadge` 寫死為 true
  致 fallback 為死碼、`SummaryCards` 去重未逐條列於派工單）。
- 風控將 §4.2 否決紀錄範圍延伸涵蓋 R6（停損基準句）與停利卡交叉引用句；「依規則」chip 的 text-xs neutral-400 為下限。
- 量測（demo 2330，預設視圖）：技術分析 111、操作摘要 70、關鍵價位 384（含階梯表）、六項觀察 30、建議卡 8；首頁風險儀表 183、全頁 662。
  頁尾揭露區 2,070 字維持（CEO 2026-09-06 裁定常駐不摺疊），是否收成「詳細」待 CEO 裁定。
- 列管：候選分支 `showBadge` 改可注入並補 DOM 測試；compact 徽章 `isWithinTtl=true` 分支測試；qa-e2e 實機（details 鍵盤操作、chip 對比）。
- commits：`b05d1c8`（個股頁）、`dc8dd42`（首頁）。

## 8. CEO 第三次裁定批次（頁尾收成詳細）與產品本質提問

- 裁定原話、解讀、風控否決紀錄（記錄式 VETO、不阻擋）與 F-1～F-7 放行條件落地狀態：派工單 §5.1～§5.2。
- 落地：`PageFooterDisclosures` 單一 `<details>`、summary 為既有標題常數、字面零改動；`globals.css` `@media print` 強制展開（F-3）；
  守門 L2→L2'，新增 L0（`layout.tsx` 全站免責釘住，F-7）與 F-3 守門。F-5「錨點導向自動展開」無觸發點（頁面無指向頁尾的錨點），已書面記錄限制。
- qa-reviewer：PASS，一項 low（`aria-hidden` 剔除須有界限）已收；Codex 未執行（本機無 CLI）。vitest 760 passed、tsc 綠。
- 量測（demo 2330，預設視圖）：頁尾揭露區 2,070 → 10 字（僅 summary 標題）；個股頁全頁 626 字。
- 產品本質：dev-lead 代答「這個網站在做甚麼」（派工單 §5.3），承認決策數據散在三區塊、缺「距最新收盤 %」；
  提案「決策卡」（§5.4）→ creative-lead 文案稿（不設標題、四格沿用常數）、art-lead 視覺規範 B.7、
  風控預審 APPROVE_WITH_CONDITIONS（「距現價」與寫死符號 VETO，改「距最新收盤 」＋`fmtSigned` 動態符號；12 條 required）。
- commits：`b13302c`（程式）、`631ae0f`（文件）。
