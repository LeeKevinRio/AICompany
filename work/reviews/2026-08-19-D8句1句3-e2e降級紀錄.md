# D8 句 1 / 句 3 — qa-e2e 實機驗收降級紀錄

- **日期**:2026-08-19
- **對象**:commits `7850d12`(句 1)、`8cf77dd`(句 3)
- **結果**:**實機驗收未能執行**——本雲端 session 的 qa-e2e 工具集缺少 preview_*(截圖/console/network/inspect)與 Bash,無法啟動 app 與操作頁面。此為環境限制,非產品缺陷。

## 已完成的替代檢查(靜態複核,不視同實機通過)

- 句 3:兩個 render 分支(正常、invalid-symbol fallback)皆渲染 `TRADINGVIEW_CHART_DISCLOSURE_FULL_STATEMENT`,單一 `<p className="mb-2 text-sm text-neutral-300">`,無摺疊邏輯;widget 載入失敗時 fallback 訊息為額外的第二個 `<p>`,揭露段落仍保留。
- 句 1:`resolveAsOfStatement` 正常分支(gap 非 null)輸出與加句前完全一致;單元測試明確斷言正常情境不含新句、gap-null 情境同字串同出。

## 正式驗收狀態

句 1、句 3 目前記為:**qa-reviewer PASS + 靜態複核,未經實機**。

## 補驗方式(CEO 本機,約 1 分鐘)

1. `git pull` 後依 local-run 流程重啟前後端。
2. 開任一持倉個股頁 → 互動圖表頁籤:確認圖表下方揭露文字結尾多了「此圖表顯示的價格與成交量，可能與本系統自有的行情資料鏈不一致；本系統不會將兩者互相校正。」且樣式與先前一致。
3. 操作摘要面板:確認「本評估基於 YYYY-MM-DD 收盤資料。」等既有句顯示正常(句 1 的觸發分支結構上不可達,正常情況不應看到新句)。

CEO 回報看到的畫面後,本紀錄補記為實機通過或開缺陷單。
