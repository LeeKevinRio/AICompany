# D8 句 2 落地 — qa-reviewer 補審紀錄

- **日期**:2026-08-19
- **審查對象**:commit `1fa49d7`(feat: 非台股持倉補列產業集中風險殘餘揭露(D8 句 2))
- **審查依據**:`work/reviews/2026-08-19-三句補充揭露-風控批審.md`(句 2 CONFIRMED 逐字句與附帶條件)、`code-review-checklist` skill
- **結論**:**PASS**,`BLOCKING_ISSUES=false`

## 核對摘要

1. **逐字比對**:`NO_SECTOR_UNSUPPORTED_MARKET_RESIDUAL_RISK_DETAIL` 與風控 CONFIRMED 句逐字元全等(含頓號、分號、句號,33 字元)。
2. **常數獨立性**:CAUSE 常數原字不動抽出;residual 為獨立常數,與 ETF 版無共用、無 alias,主詞正確區分「此持倉」vs「此 ETF」。
3. **出貨通路收斂**:`NO_SECTOR_DETAILS` 的 `unsupported_market` 只指向串接後常數;CAUSE 無單獨出貨通路(全域 grep 確認)。
4. **守門測試**:重打字面(hand-retyped literal)verbatim guard,與常數同 commit;含「有 CAUSE 必有 RESIDUAL」掃描、常數不等斷言、三狀態(unfiled/mixed/no_position)不外溢端到端測試。
5. **端到端**:真實美股持倉經 `GET /api/portfolio/limits` 驗證兩句同出、順序固定 CAUSE 在前,且 ETF 句未外溢至此路徑。
6. **列管註記**:limits.py 原 :189-192 列管註記已正確改寫並引用風控批審紀錄出處。

## 第二意見(Codex)

`codex --version` → command not found(exit 127),本環境無 Codex CLI。依公司慣例**降級為人工複核**,由本次逐檔核對取代跨廠商觀點。

## Non-blocking

- 風控批審中自標 suggested(「此持倉」與同族「此標的」用語一致性)明確不擋,dev-lead 未處理為正確;留待日後批次統一。
