# C5 38c6207 — qa-reviewer 補審紀錄

- **日期**:2026-08-22
- **審查對象**:commit `38c6207`(fix: 選項二回改、觸發標籤落地與任務 8 撤銷(條件 92/102-105))
- **結論**:**PASS**,`BLOCKING_ISSUES=false`(2227 測試、coverage 97.63%、mypy strict、ruff 實跑綠)

## 核對摘要

1. **選項二回改**:OriginalValuesView 五鍵凍結(set 相等測試);oos_period 移除;walk_forward 回退僅 backtest;反向斷言(無 oos_* 欄位、overridden 回應無「樣本外」、日期字面不出現)。
2. **任務 8 撤銷**:常數刪除(英文註解留痕無中文夾帶);清冊移除 task-8;「原始回測的樣本外期間」shipped 全檔(含前端)零出現;「最近一次」併禁;L12 封閉性寫入 docstring+鍵集凍結;任務 7 一字未動。
3. **條件 102/103**:觸發標籤逐字;import_trigger_label 恆有值非 Optional、四格參數化正向+與對話框互異斷言。
4. **條件 104**:「重新執行回測並帶入」零出現;「帶入回測結果」顯式 allowlist 一筆(標題定義行),全 repo 僅此一處實測。
5. **條件 105**:422 六碼+500 反向(同斷言 store 無列)+成功後 source_label 轉正,兩側閉合。
6. 清冊 72 筆集合相等(-task8/+trigger 計數註解同步)。

## Non-blocking

1. 條件 104 掃描 `.py` 過濾使 .tsx 不在範圍——今日前端零出現無實害;**K4c-2 落地時補前端條款**(已轉發該 lane)。
2. 批審檔行號留痕 :823→現 :833(內容比對無守門風險,下次落檔校正)。
3. ruff format 漂移屬 repo 既有狀態(110 檔),CI 僅閘 check;是否導入 format 閘門另議(記入待辦)。
4. 前端側條件 103/L11 待 K4c-2 渲染樹斷言,屆時再審。

## 第二意見(Codex)

CLI 不可用,依慣例降級。
