# C8 殘項補齊 — qa-reviewer 補審紀錄

- **日期**:2026-08-23
- **審查對象**:commit `1ba57c7`(test: C8 殘項補齊——C8-13 渲染層三態守門與 C8-11 成對守門),diff 4 檔 +175/−3,純測試樹與治理註解
- **審查依據**:`work/reviews/2026-08-23-C8-顯示語意-風控批審.md`「追加裁示」一節(C8-11/C8-12/C8-13 + 附帶查獲)
- **結論**:**PASS**,`BLOCKING_ISSUES=false`

## 逐點驗收(全部紅燈植入實測)

### C8-13(`backtestRoundTripCountRender.test.ts`,5 支)
- **實測 1**:對 `formatCount` 植入 `?? 0` 空值正規化 → 新測試 5 支中 **3 支轉紅**,其餘 **31 個測試檔全綠**——證實 dev「無聲回退、現有測試維持全綠」之說屬實,風控指出的那條路徑真的存在,現已被擋住。
- **實測 2**:把該列 `render` 改指向繞過 `formatCount` 的替代 formatter(`String(m.num_round_trips ?? 0)`)→ 同支測試**仍轉紅(4/5 失敗)**,證實斷言對象確為 `metricRows(...)` 建出的該列 render 而非 formatter 本身,**換 formatter 也擋得住**。
- non-blocking(low):本檔無 meta 自守,但 C8-13 本就未要求,屬合理範圍。

### C8-11(`kellyWinRateQualifierPairing.test.ts`)
- **實測**:分別刪除守門 ②(label 槽掃描)與 ③(全檔「勝率」零出現)→ 新增的 C8-11 meta 測試(`markers.toHaveLength(2)`)**各自獨立轉紅**(1 vs 2),符合「刪任一即須回審」。
- **meta 自我指涉檢查(本批最需驗的一點)**:逐字比對 regex 原始碼 `/\[C8-11 pair member: [a-z][a-z -]*\]/g` 與 `/\bit\("[②③]/g` 對照其自身書寫方式,確認兩者**皆不會命中自己的原始碼文字**(marker 標記後緊接的是 `[`,不是 `[a-z]` 要求的小寫字母,句法上必然不自匹配);基線跑測 markers 恰為 2、`it("[②③]` 恰為 2,間接印證未膨脹計數。**非假守門。**
- non-blocking(low,dev 已自陳非新發現):meta 只檢查「宣告與標記還在」不檢查斷言邏輯仍有效——實測把守門 ② 的 body 掏空為 `expect(true).toBe(true)`(保留 marker 與 `it("②...`)→ 7 支測試**仍全綠**,meta 未能偵測。dev 已誠實揭露,AST 分析成本確不成比例,可接受。

### 附帶查獲(`componentWordingScan.test.ts`)
已解除「the 「勝率」 line above」懸空指涉,改引 D3②/risk-fix-review.md N2 的語境規則本身,並記錄舊先例已由 C8-2 刪除。**實查** `ALLOWED_SOURCE_CONTEXTS`:目前僅 1 個檔案鍵(`TradingViewChartPanel.tsx`)、1 筆字串條目,與註解「now the whitelist's only entry」相符。

### C8-12(`test_kelly_wording.py:722-734`)
docstring 加在 `_shipped_sources()`——此函式定義出貨面掃描範圍,是**唯一能觸發「擴及 backend/tests/」風險的決策點**;義務文字同時涵蓋「不得擴範圍」與「擴了也不得剪短字面、須改顯式豁免」兩層。判定為合理處置:風險是條件式的(先擴範圍才有剪字面誘因),放在範圍定義處比放在 300 行外的 `REJECTED_LITERALS` 元組旁更能在動念修改的當下被看到。
- non-blocking(low):`REJECTED_LITERALS` 定義處未有對稱的簡短指標註解可回指此義務,非必要但可增防呆冗餘。

## 未觸碰與全套驗證

- 未觸碰任何逐字定稿字面、`獲利因子`、`engine.py`/`episodes.py`/`report.py`;`CONFIRMED_VERBATIM` 仍為 retyped dict(非 import)。
- 全套實跑覆核(不採信 dev 回報):pytest **2287**、mypy、`ruff check` 綠;vitest **535**、tsc、next build 綠。dev 對 `ruff format --check` 既有狀態的宣稱屬實。
- working tree 全程乾淨(每次紅燈植入後皆還原並以 `git status --porcelain` 確認)。

## 第二意見(Codex)

`which codex`/`codex --version` 皆 exit 127,無法執行 `/review`,依慣例降級並明確註記;本輪以逐檔閱讀 + **五次紅燈植入實測** + 全套驗證實跑補償。

## C8 狀態

C8-11/C8-12/C8-13 與附帶查獲四項 required 殘項**已補齊並通過**。唯 **C8-10(實機驗收)未完成,C8 不得標記 done**。

## 協調人補記

C8-10 首次派工遇環境問題:本遠端容器**未掛載 preview MCP**,qa-e2e 職能無 Bash 亦無 preview 工具,正確地拒絕以讀 code 代替實機觀察並退回 BLOCKING。經查環境**預裝 Chromium 且 Playwright 已配置**(`/opt/pw-browsers/`),遂改派 qa-automation 以 Playwright 執行,並套用同一套 e2e 紀律(不得改 `apps/` 下任何檔案、腳本與截圖寫 scratchpad、不得以讀 code 代替實機觀察)。
**員工線待辦**:qa-e2e 的 agent 敘述假設 preview MCP 可用,遠端容器不成立——應另開 `chore/agent-*` 分支把降級路徑(改用預裝 Chromium + Playwright)寫進該 agent 敘述或 skill,避免每次 UI 驗收先撞一次牆。
