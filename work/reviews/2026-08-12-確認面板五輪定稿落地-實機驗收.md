# 實機驗收紀錄：確認面板五輪定稿落地

- 日期：2026-08-12
- 驗收者:qa-automation(以 Playwright + Chromium 執行 e2e 等效驗收)
- 降級說明:qa-e2e 於本環境缺 preview 工具,拒絕無實測回報(正確處置);改由 qa-automation
  實際起前後端、瀏覽器操作、量測 computed style、收集 console/network。截圖與原始資料
  留存於 session scratchpad(container 生命週期內有效),關鍵結果照錄於本檔。
- 驗收對象:branch product/stock-desk @ 9845195;backend 以全新 SQLite(env 覆寫路徑)
  起於未確認狀態;repo 檔案零改動,結束後 git status 乾淨。

## 結論

**PASS(4/5 全過;第 5 項唯一 console error 為既有 favicon 404 瀏覽器層探測,
與本輪改動無關,已在列管清單)。**

## 逐項結果

1. ⑥ 同意兩句:逐字命中(全形標點);font-size 14px;色彩正規化後 = rgb(212,212,212)
   (neutral-300,達下限);DOM 順序在資本欄與按鈕之前,句 1 在句 2 之前。PASS。
2. ④ 資金用途句:常駐於資本欄 wrapper 內(非 placeholder/tooltip);比例渲染「70%」;
   句內無 0.7/7000%/%%;全頁無「分母」。PASS。
3. 舊句「之後的指令將以此為據」整頁不存在。PASS。
4. 送出流程:空值按鈕 disabled → 輸入 1000000 啟用 → POST /confirm-rules 200 →
   面板卸載、今日指令帳冊出現;§6 末句「採用規則版本 1（生效日 2026-08-12）。」
   ——具體日期,未落入「生效日期讀取失敗」分支(路線 (a) 生效日對齊實測成立)。PASS。
5. console/network:21 筆 network 全 200(today/rule-set/confirm-rules 皆實測);
   唯一 console error 為瀏覽器對 /favicon.ico 的隱性探測(獨立重現腳本查明,
   不在 page response 事件流,非應用程式 fetch),既有議題非本輪迴歸。條件 PASS。

## 快市排程主線狀態

至此確認面板五輪定稿落地完成 dev → qa-reviewer(PASS)→ 風控六輪單點裁決(APPROVE)
→ e2e 等效驗收(PASS)全鏈,「快市排程」主線工程收官;殘餘議題見列管清單
(favicon 404、provider retry fail-fast 等)。
