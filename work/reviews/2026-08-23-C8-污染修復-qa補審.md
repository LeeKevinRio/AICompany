# C8 回測報告污染修復 — qa-reviewer 補審紀錄

- **日期**:2026-08-23
- **審查對象**:`b6990ad`(fix: 回測報告統計單位改完整回合層(C8))、`adb4aab`(docs: C8 修復回歸對照表與產出腳本落檔)
- **審查依據**:機會清單 C8 條目(含閘門條款)、架構收斂裁決 4(report 層修=bug fix 不需 ADR)、D9 量化意見 §2.1、`code-review-checklist` skill
- **結論**:**PASS**,`BLOCKING_ISSUES=false`(pytest 2279、mypy strict、ruff 全綠)

## 核對摘要

1. **單一來源紅線(最重)— 通過**:`_trade_stats` 改吃 `RoundTripAttribution | None`,`win_rate = round_trips.stats.round_trip_win_rate`(直接讀同一物件,非重算);report.py 內無第二份回合抽取邏輯(逐行確認);同值斷言測試覆蓋 full + walk-forward IS/OOS 四段(含 strategy 與 buy_and_hold 兩邊)。
2. **profit_factor 定義 — 通過**:分子/分母逐行核對與 docstring 一致(回合層 realized_pnl 加總);與 `round_trip_payoff_ratio`(平均報酬率比)在程式中未被混用。
3. **num_closing_trades 語意不動 — 通過**:`_num_closing_fills` 僅計數,與舊碼 `len(realized)` 逐行同義;`build_segment_report` 的日期字串篩選僅餵 fill 計數/turnover,回合統計走 index-based `attribute_round_trips`,未沿用禁止的日期回推路徑。
4. **測試改動是修復非放寬 — 通過**:跨界回合測試 `oos.win_rate` 由 1.0 改 `is None` 理由成立(跨界回合被排除、該窗無完整回合、依構造應為 None,而非借用 fill 層殘留值),註解明確標註 C8 修復;新增 `test_profit_factor_sums_realized_pnl_over_round_trips` 以獨立算出的 gross_profit/gross_loss 比對,非同義反覆。
5. **約束 25 不回歸 — 通過且轉強**:三方等式斷言 `metrics["win_rate"] == metrics["round_trip_win_rate"] == item["win_rate"]`,`num_closing_trades > oos_round_trips` 續釘。
6. **禁區零改動 — 通過**:engine.py、frontend/、app/api/kelly*、app/kelly* 零出現;episodes.py 僅兩處 docstring,`extract_episodes`/`attribute_round_trips`/`round_trip_stats`/`stats_from_returns` 邏輯逐行零改動。
7. **回歸對照表 — 通過**:實跑 `scripts/c8_regression_comparison.py` 輸出逐格與 markdown 表核對相符(唯一差異為末欄手動中譯);`legacy_fill_stats` 逐字重現修復前原邏輯(與 b6990ad 前一版 diff 比對一致);D9 21pp 級距重現的敘述誠實(rsi_reversal +32.5~33.7pp 幅度更大;breakout 反向 -13.8pp 未被隱去或美化)。

## Non-blocking

- (low,可維護性)`PerformanceMetrics` 欄位行內註解與模組頂端 docstring 資訊重複度高,日後其一漏更新易生文件內部矛盾;可考慮合併成單一參照點,不影響本次判定。

## 第二意見(Codex)

CLI 不可用(`codex: command not found`),依慣例降級為本地單審(逐檔 diff+腳本實跑重現+全套測試/mypy/ruff)。

## 轉風控(qa 指定必看重點)

1. `BacktestReportView.tsx:43` 標籤「勝率（依結算筆數計）」修復後**已不實**——後端已改回合層,文案與實際計算脫鉤。
2. `win_rate` 與 Kelly 回合層勝率修復後**同名且同口徑**(單一來源),原核可語境「同名不同口徑」的區辨警語已不適用,需整體重審「勝率」「獲利因子」兩列顯示語意;`profit_factor`(回合總獲利/總虧損)與 `round_trip_payoff_ratio`(平均獲利率/平均虧損率)仍為不同統計量,區辨語需保留。
3. C8 條目閘門「不得以『只改數字不改文案』跳過閘門」——本批正確地只動後端與文件、未動前端文案,符合閘門要求。
