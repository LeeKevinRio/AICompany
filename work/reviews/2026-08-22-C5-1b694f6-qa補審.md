# C5 1b694f6 — qa-reviewer 補審紀錄

- **日期**:2026-08-22
- **審查對象**:commit `1b694f6`(fix: 生效上限改用實際 budget 與第十/十一輪缺口修正(C5 條件 84-101))
- **結論**:**PASS**,`BLOCKING_ISSUES=false`(2204 測試、mypy strict、ruff 實跑綠;基準三種範圍交叉確認 docs commits 無夾帶)

## 核對摘要

1. **條件 87(BLOCKING)確認修正**:SettingsDep 注入實際 budget;兩層守門實跑(非預設 budget 2.50%、與 /api/advice 第 5 條 threshold 逐值相等 pytest.approx(0.025));kelly.py 無 RiskBudget() 殘留。
2. **條件 96/98/101**:第三格分支在新 `_selection_bias_for_row()`(_disclosures 呼叫),共用 _selection_bias 未觸及(422/absent 不會回傳本句);append 先於查計數以原始碼字串序凍結;422 全代碼+500 路徑反向斷言不含本句。
3. 條件 96 句逐字(python 字串比對含全形標點);條件 86 單一定義來源且訊息字面零改動(執行期驗證 True);條件 84/85 型別收斂 tuple 非 Optional(mypy 層面禁 None);條件 89 拆分附出處+窄化後正向斷言;90/91/92/93 正反向齊;100 無 0/零/1/{} 斷言+備案進 REJECTED_LITERALS;99 常數識別字掃描。
4. 「整個揭露區塊不顯示」暫行處置查證從未落地為程式碼(僅 logger+None),無殘留,clean。

## Non-blocking

- 條件 99 識別字掃描面限 kelly_wording 自身(本次新常數涵蓋已足);嚴格全 repo 解讀留待日後批次擴大。

## 第二意見(Codex)

CLI 不可用,依慣例降級。
