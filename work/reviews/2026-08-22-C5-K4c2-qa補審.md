# C5 K4c-2 前端 — qa-reviewer 補審紀錄(第一輪)

- **日期**:2026-08-22
- **審查對象**:`dd2a090`→`239e122`→`6e9c4d5`(b0001c8..6e9c4d5,19 檔)
- **結論**:**BLOCKING_ISSUES=true(7 項)**,退修;測試機制設計紮實(條件 74 雙層 call-site guard、73 四格、93 逐段、72/103 aria、92 五鍵、零字面掃描機制皆合規),全套驗證綠(前端 492/tsc/build、後端 2227 無回歸)。

## BLOCKING B1-B7

- **B1(high)互斥破功**:KellyManualInputForm 以生效值預填且恆常渲染於 panel 下方——原始值檢視開啟時生效值仍在 DOM,條件 46 約束 3/驗收 15 必炸。修法:表單納入互斥切換或移入生效值分支。
- **B2(high)條件 57 未攔 pydantic 前綴**:實測範圍違規 422 msg=「Value error, 勝率必須大於 0…」,shape test 放行混種句。後端半邊:PydanticCustomError 或 422 handler 剝前綴使核可句逐字出線+後端逐字斷言;前端補「Value error,+中文」測試案例。
- **B3(high)條件 94/74 失敗路徑快取回退**:refetch 失敗沿用 stale 變體,最壞情境免對話框直接覆蓋(74 被繞過)。修法:handler 直呼 getKellyDisclosures+try/catch,取不到即中止,不回退 prop。
- **B4|L11 觸發**:buildKellyImportRequest 硬寫 initial_cash=1_000_000(主表單有此可編輯欄,非產品無此參數)。修法:補欄位比照 BacktestForm;否則照 L11 回審。
- **B5|L7 觸發**:window.confirm 刪除告知未審——**已由十四輪條件 109 先行處置**(刪除組暫緩出貨,告知句 creative 起草中)。
- **B6(high)422 拒件被 overlay 遮蔽**:失敗時對話框續存,ImportRefusalPanel 渲染於 z-50 bg-black/60 底層,條件 22/3-A 同屏淪為「DOM 有但壓黑」。修法:onError 關閉對話框或拒件面板移入框內。
- **B7|顯著度地板**:effective_cap text-xs 低於 f_star_interval text-sm(條件 6「不低於」反向實作);(h) 句 text-xs 低於 (b)(c)(4-C/12/44)。修 class。

## Non-blocking 裁定(重點)

1. **KELLY_SURFACE 擴充(退 dev-lead)**:靜態前綴 tuple 不自動涵蓋四新檔,「kelly」scope 否決字面對新檔無守門;現行「glob 會涵蓋」註解錯誤須更正;前端自建守門保留為半邊。
2. 條件 76 自報不實(無 cancel 純函式)——留痕更正;endpoint 未呼叫留 e2e/後端。
3. 條件 97 覆核:expired 整格不渲染**符合字面**;代價=管理頁看不到過期訊號,建議後續後端把 (g) 納 disclosures(風控核可後)再開 expired 態(列後續)。
4. 條件 95 覆核:dev 理由成立,兩側合成滿足;BacktestReportView 註解誤記位置順手改。
5. 其餘:refetch 時點深度留痕;wordingScanHelpers 註解漂移;非數字輸入 client 檢核建議;未使用 import。

## Chrome 技術覆核(措辭面另送風控)

表單/區塊 chrome 與既有 register 同構、無方法論主張(與風控十四輪裁定一致);**新升級項**:panel 的「Back」「Original values」英文按鈕(零 Han 設計必然)——語言一致性與 E-5「可以查看」可達性請風控併裁;要中文須走後端常數送審。

## 第二意見(Codex)

CLI 不可用,依慣例降級;B2 以實際執行 pydantic 重現(非推測)。
