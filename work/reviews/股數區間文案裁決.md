# 風控裁決:建議股數區間文案(2026-08-09)

- 結論:修訂版 A APPROVE;版本 B 另案;版本 C 行動空間句 VETO(弱化上限約束力+與免責重複)。
- 實作完成後不需複審;**任何一字與核可全文不同即視為未過審**。

## 動作標籤槽 {label}(後端新增常數,取值須等同 adviceWording.ts:34-41)

add→加碼參考|reduce→減碼參考|take_profit→分批獲利了結參考|stop_loss→停損評估

## 核可全文(逐字)

① skipped_note(取代 limits.py:908-909,完整句接在句號後):
「以下上限本次缺少可用資料,未參與這個區間的計算:{skipped 頓號串}。未參與計算不等於已通過,這個區間沒有檢查過這幾條。」
(注意:用「缺少可用資料」不用「資料不足」——第 3 條有資料存在但過期失效的路徑)

② 賣出側 compliant=True(取代 limits.py:962-965):
「規則評估:{label},區間為賣出 {lower:,} ~ {upper:,} 股。目前部位已超出「{limit_name}」這條上限,賣出 {lower:,} 股後可回到這條上限的範圍內;區間上緣 {upper:,} 股是目前持股全數,是這個區間的上界,不是這條上限要求賣到的數量。{skipped_note}」

③ 賣出側 compliant=False(取代 limits.py:966-970):
「規則評估:{label},這個區間只有一個數字:{lower:,} 股,也就是目前持股全數。目前部位已超出「{limit_name}」這條上限,但這條上限主要由其他部位造成,把這一檔全部賣出後仍然超標。{skipped_note}」

④ add 分支(可選,要改就用這版,取代 limits.py:923-927):
「規則評估:加碼參考,區間為再買進 {lower:,} ~ {upper:,} 股。目前有納入計算的上限中,額度最緊的是「{LIMIT_NAMES[binding_id]}」;以它換算,買進後這條上限仍為通過的最大股數是 {upper:,} 股,也就是區間上緣。下緣 {lower:,} 股取上緣的一半,用意是給出一個範圍而不是單一數字,不是「最少要買」的數量。{skipped_note}」

## required 配套

- 前後端標籤契約測試(後端標籤表 == adviceWording HELD_ACTION_LABELS 四鍵)
- basis 數字千分位 {lower:,} 與卡片 toLocaleString 一致
- 新字串不得複用到警示推播路徑(alerts 禁用清單含賣出/減碼/建議)
- 改動處加「風控核可文案,修改須重新送審(2026-08-09)」註解
- 全庫搜舊字串斷言同步(「上緣為目前持股全數」「未納入計算的上限」「區間下緣取上緣的一半」「賣出後仍為違反」)
- reduce/stop_loss/take_profit 共用模板;compliant 兩分支必須兩套句;add 不得套「最少/最多」對稱句型

## suggested(不擋)

- AdviceCardView.tsx:89 basis 對比升 neutral-400(句子變長後);零股「不足 1 股不計」註記
