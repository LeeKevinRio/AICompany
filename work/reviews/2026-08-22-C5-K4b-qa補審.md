# C5 K4b-backend 落地 — qa-reviewer 補審紀錄

- **日期**:2026-08-22
- **審查對象**:commit `68c12c9`(feat: Kelly 第 5 條上限接通生效輸入與定稿揭露(C5 K4b-backend))
- **審查依據**:ADR-0006(D-5/D-6/D-7/D-8)、架構評估約束 1-37、六輪批審落地條件 1-50、`code-review-checklist` skill
- **結論**:**PASS**,`BLOCKING_ISSUES=false`(1878 測試、ruff、mypy 全綠)

## 核對摘要

1. **D-5 紅線(最重)**:f\*≤0 時 notional_caps 不含 kelly_fraction 鍵(白盒)、reduce/stop_loss/take_profit 反向斷言第 5 條永不為 binding cap、skipped 名單排除且既有 skipped_note/basis 字面未動——三組測試齊全,實作正確。
2. 四 (g) 分支/(e)/(e-manual) 互斥窮盡(三來源 × passed/violated property test)/(a-2) 與任務 1/2 順序(條件 41)/{anchored_on} 純日期/{days} 插值/固定位數反向釘假精確度——逐字核對無誤。
3. `kelly_usable()` 單一真偽來源;約束 12 AST 守門;kelly_wording import 結構掃描防重打(條件 2)。
4. D-7 逐檔化正確(缺 pair 排除而非借用);四條消費路徑(advice/portfolio/alerts/scheduler)接線核對一致。
5. models.py 置換僅二字、FORBIDDEN_IN_A_REFUSAL 未動有反向守門;allow_inf_nan=False 有測試證實走一般型別錯誤。

## Non-blocking 與後續狀態

1. overridden 暫走 (g-3) 未標暫代註記 → **已由條件 52 直接處置**:dev 正落地 (g-overridden) 定稿並停用過渡(進行中)。
2. **alerts.py/scheduler.py 缺 Kelly wiring call-site 回歸測試**(advice/portfolio 有 _KellySpy 防線,兩路徑不對稱)→ 併入 dev 現行 commit 補上。
3. 白名單歸屬(kelly_wording 5/limits 1)→ 風控第七輪條件 56 已書面准。
4. book.py import app.kelly.models 讀法 → tech-architect 已追認(條件46裁決附註),守門補強 `ea2923c` 已落地(傳遞閉包+limits 零 import 白盒)。

## 第二意見(Codex)

CLI 不可用,依慣例降級(逐檔 diff 比對+全量測試+ruff/mypy+grep 交叉驗證)。
