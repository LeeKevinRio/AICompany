# C8-10 實機驗收紀錄(風控 required)

- **日期**:2026-08-23
- **驗收對象**:product/stock-desk HEAD `1ba57c7` 的回測報告畫面
- **驗收依據**:`work/reviews/2026-08-23-C8-顯示語意-風控批審.md` 的 C8-10 四個確認點 + 額外檢查 + `ui-delivery-checklist` 第 1、3 節
- **結論**:**PASS,四個確認點與額外檢查全部通過,無 BLOCKING**

## 執行方式(職能降級說明)

本遠端容器**未掛載 preview MCP**,qa-e2e 職能無 preview 工具亦無 Bash,首次派工正確地拒絕以讀 code 代替實機觀察並退回。經查環境**預裝 Chromium 且 Playwright 可用**,遂改派 **qa-automation** 執行,套用同一套 e2e 紀律:不得改 `apps/` 下任何檔案(事後 `git status` 確認乾淨)、腳本與截圖一律寫 scratchpad、不得以讀 code 代替實機觀察(讀 code 僅用於定位表單欄位 id)。

**重現步驟**:
1. `cd apps/stock-desk/backend && uv run uvicorn app.main:app --port 8000`(須從 `backend/` 啟動,DB 走預設 `./data/stock-desk.db`)
2. `uv run python -m app.demo.seed`(**專案既有**離線示範資料種子,非新造:2330/0050/00631L 各 520 根日線,`source=demo_synthetic`,畫面與 API notes 皆標示「合成示範資料,不可用於任何真實決策」)
3. `cd apps/stock-desk/frontend && npm run dev`(port 3000)
4. Playwright:全域 `playwright@1.56.1`,`executablePath: '/opt/pw-browsers/chromium-1194/chrome-linux/chrome'`,**未執行 `playwright install`**

## 逐點結果

**確認點 1 — 兩區段表新標籤與完整回合數列:PASS**
真實 DOM 抓出的表格逐字比對:樣本內與樣本外**兩個區段**的第 11 列 label 逐字為 `勝率（依完整回合計）`(全形括號),緊接第 12 列逐字為 `完整回合數`,一字不差。

**確認點 2 — 緊鄰且同字級:PASS**
`getComputedStyle` 實測:`勝率（依完整回合計）` label cell `font-size: 14px; font-weight: 400`;`完整回合數` label cell `font-size: 14px; font-weight: 400`——**數值完全相同**(皆 Tailwind `text-sm`),同一 `<table>` 相鄰 `<tr>`、同 `<td>` 樣式(`px-3 py-2`),非 tooltip、非折疊、非 `text-xs` 註腳層。

**確認點 3 — Buy & Hold「—」與策略「0」:PASS(兩種情境皆實機構造成功)**
- 一般情境(`train=252, test=63`):Buy & Hold 欄完整回合數在樣本內/外**均顯示 `—`**。
- **「有交易但無完整回合」情境**(風控理由 5 第二段所指的那個畫面):以真實表單提交 `symbol=2330, start=2024-08-26, end=2026-08-21, train=100, test=30`,樣本內策略欄實際渲染 `交易次數 = 7（結算 6）`、**`完整回合數 = 0`**(阿拉伯數字 0,非「—」),同列 Buy & Hold 欄仍為 `—`。此情境先以 API 掃參數空間找到,再**原封不動透過真實表單、真實點擊重現**,畫面數字與 API 一致。**兩種空值語意在畫面上確實可區辨——風控項 4 裁定的三態設計實機成立。**

**確認點 4 — 免責句與時間戳未被擠出或降級:PASS**
`getBoundingBox` 實測:兩者 `visible: true`,且**都位於兩張 SegmentTable 之前**(免責句 y≈720、時間戳 y≈690,表格自 y>800 起)。逐字:「本回測報告呈現的所有數字皆屬歷史統計描述,不代表未來會重演。」/「產出時間:2026/08/24 00:29(台北時間)」。

## 額外檢查(全部通過)

- 舊標籤 `勝率（依結算筆數計）`:對整個 `document.body.textContent` 搜尋 **零出現**。
- `獲利因子`:畫面逐字未變,無括號限定語。
- 禁用項 `盈虧比`、`跨界排除`、`期末未平倉`:頁面文字**三者皆零出現**——**L-C8-1/L-C8-2 未觸發**。
- console error **0 筆**;network 失敗請求(status ≥ 400 或 requestfailed)**0 筆**。後端 log 有打 TWSE/TPEx 即時行情的 403(離線環境預期行為),report 回應本身標記 `source=demo_synthetic`、`status=cached_stale`,非應用程式錯誤。

## ui-delivery-checklist 第 1、3 節複核

- **3.1 頁面層級無橫向捲動:PASS**。375px 與 320px 兩個寬度下 `documentElement.scrollWidth === clientWidth`(375=375、320=320)。表格自身 `.overflow-x-auto` 容器有內部橫捲(scrollWidth 520 / clientWidth 299),屬專案既有、套用於所有列的容器設計;截圖確認捲到底時 Buy & Hold 欄「完整回合數＝—」清楚可見,與上方勝率列同高同排。
- **1.1 正文對比:發現既有缺口(非 C8 造成)**。實測:label 欄(`text-neutral-500`,**全表所有列共用**)前景 `lab(48.496 0 0)` 對背景 `#0a0a0a` 對比 **4.18:1**,低於 WCAG AA 正文門檻 4.5:1;數值欄 `text-neutral-100`/`text-neutral-300` 分別為 **18.15:1**/**13.36:1**,遠高於門檻。此樣式套用於 `BacktestReportView.tsx:131` 的全部 `row.label`,**非本次 C8 新增或修改**,故不判為 C8-10 的 BLOCKING(未新引入問題),但如實記錄實測數字 → **已轉列機會清單 D10**。

## 工作檔案

`/tmp/.../scratchpad/c8-e2e/`(`run.js`、`contrast.js`、`sweep.py`、全部截圖與 JSON)。`apps/` 目錄下**未做任何修改**,事後 `git status` 確認 working tree 乾淨。

## 協調人補記

**員工線待辦**:qa-e2e 的 agent 敘述假設 preview MCP 可用,遠端容器不成立——應另開 `chore/agent-*` 分支把降級路徑(預裝 Chromium + Playwright,`executablePath` 指定,不得執行 `playwright install`)寫進該 agent 敘述或 skill,避免日後每次 UI 驗收先撞一次牆。
