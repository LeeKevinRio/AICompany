# D8 句 3 落地 — qa-reviewer 補審紀錄

- **日期**:2026-08-19
- **審查對象**:commit `8cf77dd`(feat: TradingView 揭露補列價量不一致澄清句(D8 句 3))
- **審查依據**:`work/reviews/2026-08-19-句1句3重寫-風控批審.md`「句 3 第二輪 — CONFIRMED(修訂版)」(定稿字面+落地條件 1-10)、`code-review-checklist` skill
- **結論**:**PASS**,`BLOCKING_ISSUES=false`

## 核對摘要

1. **逐字比對**:補句常數與風控定稿逐字元一致(含全形逗號/分號/句號);FULL 常數=既有句+補句直接串接、開頭無空白、順序正確(startsWith/endsWith/串接等式三重鎖死)。
2. **既有常數與測試不動**:diff additive-only,既有三個 it block 與「勝率」白名單一字未動。
3. **渲染點排他性**:invalid-symbol 與正常渲染兩分支皆改渲染 FULL 常數;全 repo 無任何 call site 只渲染半句;樣式維持 `text-sm text-neutral-300` 未降級。
4. **反向斷言有效**:「各自獨立的兩條路徑」與「兩者」在 FULL 常數內各只出現 1 次,斷言具偵測力(防日後補回被刪分句)。
5. **「立即」白名單細查通過**:白名單條目為 doc comment 完整一整行原文,遮蔽機制為整段精確 substring 比對,檔內僅出現 1 次,不會遮蔽其他或未來含「立即」的文案;被遮蔽處僅在維護者 JSDoc、不在任何使用者可見字串;做法與既有「勝率」白名單先例一致。未過寬。
6. **doc comment**:含風控第二輪定稿註記與跨源校正列管警語;常數命名無 RECONCILED/VERIFIED 類字樣。

## Non-blocking

- doc comment 引用風控警語時將半形括號/逗號正規化為全形(與檔內慣例一致);該引文非受逐字釘住的定稿字面,不影響語意。日後若需逐字對照請留意全半形。

## 第二意見(Codex)

`codex` CLI 本環境不可用(command not found),依慣例**降級為人工複審**;建議日後 CLI 可用時補做。
