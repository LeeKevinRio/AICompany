# D8 句 1 落地 — qa-reviewer 補審紀錄

- **日期**:2026-08-19
- **審查對象**:commit `7850d12`(feat: 操作摘要補列交易日曆無法確認澄清句(D8 句 1))
- **審查依據**:`work/reviews/2026-08-19-句1句3重寫-風控批審.md`(句 1 CONFIRMED + 落地條件 1-9)、`code-review-checklist` skill
- **結論**:**PASS**,`BLOCKING_ISSUES=false`

## 核對摘要

1. **逐字比對**:`AS_OF_CALENDAR_UNCONFIRMED_STATEMENT` 以 Python `repr()` 逐字元比對與風控定稿完全一致(含全形逗號、句號)。
2. **觸發條件**:正確採 `dateIso !== null && tradingDaysBehind === null`(`dateIso = lastBarDate ?? observationEnd`),落地條件 1 點名的 `observation_window.end` fallback 漏格有專門測試釘住(超出最低要求的加分項)。
3. **既有分支不動**:兩既有分支輸出字面一字未動;與 `AS_OF_DATE_UNKNOWN_FULL_STATEMENT` 結構互斥且有測試釘死。
4. **單一字串/未拆節點/未擴面**:回傳單一字串;`OperationSummaryPanel.tsx` 未觸碰;`no_price`/`no_action` 未擴面。
5. **四分支測試齊備**:①互斥 ②未達門檻不含新句 ③達門檻反向斷言不疊句 ④gap null 同字串同出;逐字守門為重打字面且與常數同 commit;常數落入既有禁用詞+裸「即時」掃描(單獨與串接後兩種形式)。
6. **既有測試修改核對**:兩處 `trading_days_behind: null→0` 屬合理修正——原測試意圖是日期選取優先序,fixture 意外落入新分支,改 0 回到原本要測的格子,原斷言字面未動未放寬,附註解與指向專門測試。
7. **範圍紀律**:句 3 相關檔案未被觸碰,符合「句 3 未 CONFIRMED 前不得先行合併」。
8. **實測**:vitest 439/439 通過(19 檔)、`tsc --noEmit` 無錯,工作目錄乾淨。

## 第二意見(Codex)

`which codex` not found,本環境無 Codex CLI,依慣例**降級為人工複審**(以全套測試+型別檢查補強);建議日後 CLI 可用時對此 commit 補做第二意見。
