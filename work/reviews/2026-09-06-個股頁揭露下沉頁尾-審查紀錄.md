# 個股頁揭露句下沉頁尾 — 風控三輪＋qa-reviewer 審查紀錄（2026-09-06）

> CEO 裁定：「做第 4 項 揭露句下沉頁尾」。裁定書面、方案、風控否決紀錄與條件對照見
> `work/stock-desk-揭露下沉頁尾-CEO裁定與方案.md`。本紀錄只記審查結論與實測數字。

## 結論

- 風控第一輪（方案）：**ACCEPT_WITH_CONDITIONS**——A+1～A+10 最低保留線、逐句附條件、L1–L6 落地條件、兩項否決紀錄（結構、neutral-500）。
- 風控第二輪（實作）：**NEEDS_CHANGES**，範圍限定指引句一句（R1 漏「揭露事項」、R2 資料來源組需頁級變體）；其餘全部 APPROVE，`PAGE_LEVEL_DISCLOSURE_SECTION_INTRO` 准予退場。
- 風控第三輪（指引句）：見 §風控第三輪。
- qa-reviewer：**PASS**，`BLOCKING_ISSUES=false`；Codex 第二意見因環境無 CLI 未執行。
- 測試：`npm run typecheck` 通過；`npx vitest run` 36 files / 624 tests 全綠。

## 交付內容

| 項 | 檔案 | 說明 |
|---|---|---|
| 頁尾揭露區 | `app/components/PageFooterDisclosures.tsx` | `</main>` 前最後一個節點；`role="region"`；text-xs／neutral-400；四組固定順序；算式行仍 font-mono、與限定語同 `<li>`。 |
| 字面 | `app/lib/footerDisclosureWording.ts` | 標題「本頁揭露與計算依據」、導語、區塊指引句模板、頁級指引句。 |
| 組名常數 | `app/lib/sectionTitles.ts` | 操作摘要／技術分析／建議卡／槓桿型 ETF 專章，各 h2 同步改用。 |
| 組裝 | `page.tsx` | `resolveKeyLevelsAnchor` 抽出；`footerGroups`（資料來源→操作摘要→關鍵價位參考→槓桿型 ETF 專章）。 |
| builder | `buildKeyLevelsFooterItems`（KeyLevelsPanel.tsx）、`buildSummaryFooterItems`（operationSummary.ts） | 純函式，數字與面板同一計算。 |
| 退場 | `app/components/PageDisclosureSection.tsx` 刪除 | `NON_REALTIME_NOTICE` 改為頁尾第一組。 |

## 下沉／留原位

- **下沉**：`NON_REALTIME_NOTICE`；`coverageStatement`（候選）、`rulesStatement`；`KEY_LEVELS_HEADER_STALENESS_SELF_NOTICE`、`KEY_LEVELS_STOP_S5_NEUTRAL_NOTE`、「計算依據（逐項揭露）」標題＋XREF＋十條＋`buildFooterSample`；`chapter.disclosure`。
- **留原位（A+1～A+10）**：`asOfStatement`、`candidateEvidenceNotice`、`notComparableNote`、`DIRECTION_SHARE_QUALIFIER`、`KEY_LEVELS_PANEL_DISCLAIMER`、`KEY_LEVELS_TARGET_STANDING_NOTICE`、`buildRangeNotValuationNote`、`INDICATOR_OVERVIEW_LEGEND`、`KEY_LEVELS_HEADER_DASH_NOTICE`、`KEY_LEVELS_LADDER_NOTE`。
- **留原位（附條件句，選擇不產生新字面）**：`KEY_LEVELS_HEADER_UNADJUSTED_NOTICE`、`KEY_LEVELS_PULLBACK_EXPLAIN_NOTE`、`KEY_LEVELS_TARGET_ROW_TRAILING_NOTE`、`chapter.notes`／erosion `nature`、`context_notes`（列管待補件）。

## qa-reviewer 發現與處理

- [low] `KeyLevelsPanel.tsx`／`operationSummary.ts` 過期註解仍指 `PageDisclosureSection` → 已更新。
- [low] `<footer>` 在 `<main>` 內無 landmark role → 已加 `role="region"`。
- [low] `aria-label` 與 h2 重複 → 保留（無害）。
- [obs] L2 掃描的 `React.lazy` 正則不擋具名 `lazy(`；既有設計，列管。

## 風控第三輪：APPROVE

- 區塊指引句改為「頁尾「{t}」涵蓋本區資料來源、揭露事項與計算方式說明。」（R1 閉合，三類用詞與導語逐字同序）。
- 頁級指引句「頁尾「本頁資料與計算揭露」說明資料來源與更新頻率。」（R2 閉合，不自稱本區、不承諾計算方式）。
- 補件核可：INTRO 不得再渲染守門；方案 §5.5 缺口原文。
- 新增列管：M1 指引句三類列舉對單一句組屬多列（觸發條件：組內容再縮減時重審）；M2 INTRO 守門檔案清單改目錄掃描；沿用 `context_notes` 前置、qa-e2e L6-8、S1 組名相近、S3 小標 a11y。
- 兩項否決紀錄（頁尾集中架構、neutral-500）依章程 §0.5 續存。

## 實測數字（2330 持倉）

| 指標 | 圖形化後 | 下沉後 |
|---|---|---|
| 桌機 1280px 頁尾以上字數 | 5,973 | 4,857（−19%） |
| 頁尾字數 | — | 1,216 |
| 整頁字數 | 5,973 | 6,073（指引句＋頁尾標題／組名 +100） |
| 桌機：頁首到頁尾距離 | — | 約 7.0 屏（900px） |
| 手機 390px：頁首到頁尾距離 | — | 約 10.8 屏（844px） |
| 手機頁面橫向溢出 | 無 | 無 |

L6-8 qa-e2e 正式實機驗收（≤400px、200% 放大、六組完整可見）待補；上表為 frontend-engineer 以 Playwright 自測。

---

## 第二批：CEO 第二次書面裁定「那 10 句也下沉」（2026-09-06）

- 裁定原文：「那 10 句也下沉，我書面裁定推翻」。方案 §7 記錄裁定、實作對照、風控否決紀錄增補（否決項三～五）與 16 項已知風險清單。
- 實作：A+1～A+10 全部下沉，一字不改；頁尾改六組（新增技術分析、建議卡）；原位各留一句指引句；建議卡組與堆疊條共用 `hasDirectionShareBar` 閘門。
- 風控（第二次裁定後）：核對「一字不改、全頁恰好一次、原位指引句、同閘門」全數通過，無超範圍移除；required 一項（`buildSummaryFooterItems` 正向斷言、`page.tsx` 六組 `items:` wiring 斷言）已補；三處過期註解已更新。
- qa-reviewer：NEEDS_CHANGES（無 BLOCKING）→ 兩項 medium（過期 JSDoc、`buildSummaryFooterItems` 測試缺口）已修；`?? ""` 改為 null 斷言；`rangeZoneOf` 共用 helper。Codex 第二意見：環境無 CLI，未執行。
- 測試：typecheck 通過；vitest 36 files / 627 tests。

### 實測數字（2330 持倉，第二批後）

| 指標 | 圖形化後 | 第一批下沉 | 第二批下沉 |
|---|---|---|---|
| 桌機頁尾以上字數 | 5,973 | 4,857 | 4,530（−24%） |
| 頁尾字數 | — | 1,216 | 1,607 |
| 整頁字數 | 5,973 | 6,073 | 6,137 |
| 桌機頁首到頁尾 | — | 7.0 屏 | 6.7 屏 |
| 手機 390px 頁首到頁尾 | — | 10.8 屏 | 10.4 屏 |
| 手機橫向溢出 | 無 | 無 | 無 |
