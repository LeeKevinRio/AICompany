# C5 K3 落地 — qa-reviewer 補審紀錄

- **日期**:2026-08-19
- **審查對象**:commit `a216f01`(feat: Kelly 回測帶入端點與嘗試紀錄串接(C5 K3))
- **審查依據**:ADR-0006(accepted,D-2/D-3)、架構評估約束 26-37、架構收斂裁決+附註、K1 補審裁定(ageing_of 唯一判定點)、`code-review-checklist` skill
- **結論**:**PASS**,`BLOCKING_ISSUES=false`

## 核對摘要

1. **約束 32**:`execute_backtest` 抽取逐行核對為行為零變更(編排邏輯原樣搬移、route 一行轉接);既有 32 個 backtest API 測試未改動而通過,另有 3 個對等回歸測試。
2. **約束 31**:body 禁 p/b/f\*(extra=forbid+三參數化測試);單 handler 單 BacktestResult(spy 計數測試)。
3. **約束 27**:D-2 全欄位落地逐一斷言非 None;有限值斷言(非有限→500 不寫入,monkeypatch 注入 -inf 直接驗證)。**特審**:非有限分支 attempts 補寫 outcome=ok 但 CI 欄 NULL——核對 ADR D-2「outcome=閘門判定非寫入結果」為明文要求非 bug,K_observed 語意不受污染;non-blocking 建議日後鑑識需求可加稽核旗標區分,不必動 schema。
4. **約束 29/35**:六種 422 各自落列驗證;append 失敗→500;DELETE 不連坐(雙重測試);list/read 不讀 attempts。
5. **約束 34**:f_star 字面掃描白名單四檔與實測一致;上限計算不讀落地 f_star(可由生效 p·b 重算測試)。
6. **約束 28**:fraction_fn=limits.kelly_fraction 由 api 層 keyword-only 注入;fold 幾何窗(oos_observations=bar 數)非日期字串;計齡三回應路徑全走 `ageing_of()`。
7. canonical spec hash(sort_keys,欄位順序不影響 hash 有測試);seed 可重現(同 spec 同 bars 相同區間)。
8. 模組邊界 grep 三包互不 import 乾淨,組裝點唯一 `app/api/kelly.py`。

## 第 8 點裁示(K_distinct_specs)

**符合 ADR,不需 tech-architect 追認**:D-2 欄位表唯一快照欄即 `k_observed_at_write`;約束 30 的「落地」語境=兩計數可查並寫進揭露,`KellyAttemptStore.k_distinct_specs()` 即為實作;導出式非降級。

## Non-blocking

- 非有限分支的 attempts 列可考慮稽核旗標(日後鑑識);`KELLY_NON_FINITE_INTERVAL_MESSAGE` 的浮點字面渲染與 PB_NONE 修正併批(嚴重度 low,500 非常態路徑)。
- 排程提醒:K 顯示層動工前確認回傳 k_observed/k_distinct_specs 的端點會呼叫 attempts 的兩個查詢方法。
- sample_gate 三句訊息退修項(PB_NONE/INSUFFICIENT_DATA/SYMBOL_MISMATCH)為風控批審已列 dev 退修,非本 commit 新問題,提醒後續 commit 勿漏。

## 第二意見(Codex)

CLI 不可用,依慣例降級人工逐條比對。
