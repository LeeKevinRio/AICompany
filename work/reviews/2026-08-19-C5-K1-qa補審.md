# C5 K1 落地 — qa-reviewer 補審紀錄

- **日期**:2026-08-19
- **審查對象**:commit `10e8759`(feat: Kelly 輸入儲存層與手動路徑 API(C5 K1))
- **審查依據**:ADR-0006(accepted)、架構評估約束 1-37、架構收斂裁決、`code-review-checklist` skill
- **結論**:**NEEDS_CHANGES**(單一 high 項;其餘全數通過)→ 退回原 dev 依方案 (a) 修正中

## 通過項摘要

- 約束 1-6、7(閘門常數 20/5/5+六 reason_code+軟警示帶邊界 19/20/49/50)、13、27 欄位、29(append-only,檔內無 UPDATE/DELETE/REPLACE/DROP,源碼掃描測試釘住)、30(K_observed/K_distinct_specs 定義正確,900 天舊紀錄仍計入)、35(DELETE 不連坐,有測試)、37(import graph 守門測試+反例測試防守門失效)全對應到位。
- migration `BEGIN IMMEDIATE`+鎖內重查,雙執行緒併發測試通過;SQL 全參數化;`updated_at` 僅 server 戳記(client 帶入 422);422 拒絕不 clamp、前值不動;import-backtest 端點確認不存在(404 測試)且無 stub。
- sample_gate 訊息為中性事實敘述,無建議/揭露語句;docstring 自註「最終文案仍須風控審查」。
- 本地實跑:kelly 專項 85/85、全套 1605/1605 綠。

## 退修項(high)

`models.py::anchor_moment()`:backtest 系列且 `oos_end_date is None` 時 fallback 到 `updated_at`——方向**反了**:`updated_at` 恆晚於等於 `oos_end_date`,fallback 使 anchor 偏向現在、年齡變小、判定更新鮮,正是 D-4 要防的「以執行時間計齡讓舊區間偽裝新鮮」;docstring 的「only make it look older」論證與事實相反。目前經 API 不可達(死碼),但 K3 會繼承。**處置**:協調人採 qa 建議方案 (a)——移除 fallback,缺 anchor 一律保守判 `expired`、不回報偽造 age,docstring 改正,補測試;已退回原 dev 修正。

## 落差裁定

- ①`oos_observations` 欄位(D-2 本文漏列、約束 27 有):**接受**,約束效力優先;ADR errata 一句由協調人補記,不需 tech-architect 重裁。
- ②即上述退修項。

## 第二意見(Codex)

CLI 不可用(command not found),依慣例降級人工複核。

## 重審補記(2026-08-19,commit 35a62ad)

**PASS,退修結案。**`anchor_moment()` 移除反向 fallback 改回 None;`ageing_of()` 確認為 app/ 內唯一判定點(無其他生產路徑繞過);無錨列 expired+age_days=None+anchored_at=None,不產生可誤讀成新鮮的數字;docstring 論證已改正並註明退修出處。12 筆新測試(含「剛寫入仍判 expired」直接證偽舊論證);API 層 null 序列化正確。test_kelly_store WAL 種子修改核實為還原真實前置條件、斷言未放寬(qa 抽驗 30/30 過)。repo 級 WAL 併發 flake 確認未被本 commit 惡化,已立 D9 列管(機會清單)。Codex 降級人工照慣例。K1 全部收案。
