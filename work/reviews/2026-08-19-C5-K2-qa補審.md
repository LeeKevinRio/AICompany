# C5 K2 落地 — qa-reviewer 補審紀錄

- **日期**:2026-08-19
- **審查對象**:commit `e462d8a`(feat: 回測回合層抽取與 f* 區間基礎(C5 K2))
- **審查依據**:ADR-0006(accepted,D-5)、架構評估約束 9/22-28/33/37、架構收斂裁決+退化抽樣附註、`code-review-checklist` skill
- **結論**:**PASS**,`BLOCKING_ISSUES=false`

## 核對摘要

1. 約束 26 七類測試逐一驗證斷言真的測到宣稱性質(非 smoke):歸屬純度含 partition 完整性檢查;邊界敏感度用對稱差;加總不變量選 flat-to-flat 區段使排除數為零、等式非平凡成立;f* 注入守門驗區間退化+精確呼叫次數 draws+1;bar_index 不變量;退化抽樣雙向三測試正確區分(no_loss 以 inf 呼叫、no_win 不呼叫 fraction_fn 記 -inf、計數拆向)——與 quant 確認附註逐字吻合。
2. f* 算式唯一性:grep `episodes.py` 零命中 Kelly 形算術;`fraction_fn` keyword-only 無 default(TypeError 測試);import graph 守門(AST 版既有基礎設施重用)確認 app/backtest 不可達 app.advice/app.kelly。
3. report 新欄位成對命名、僅由 episodes 供給、Buy&Hold 回 None;既有 win_rate/profit_factor 值/語意/測試全未動(diff 純 additive);OOS 歸屬用 fold 幾何 index,舊日期字串篩選未被新路徑沿用。
4. 全套 1634 測試、ruff、mypy 綠。

## Non-blocking(兩項 low)

1. `_close_episode` 對非正 entry_equity 的 `ValueError` 注解稱「structurally unreachable」過強(engine 未 clip equity,極端虧損序列理論上可達);防禦行為本身正確(fail-loud),建議後續軟化措辭為「no known path in the current engine」。
2. bootstrap 百分位不插值(lower/higher rank)屬 dev 合理裁量、可驗證保守(只寬不窄),但未在 ADR/裁決明文——建議 quant 一句話簽認(協調人已回訪詢問)。

## 第二意見(Codex)

CLI 不可用,依慣例降級人工複核。

## 交接

純邏輯層無 UI,依交接協定不走 qa-e2e;K3(import-backtest 串接)待 K1 退修收斂後開工。
