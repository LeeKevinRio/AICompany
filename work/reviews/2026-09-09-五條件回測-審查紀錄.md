# 五項觀察條件 回測 — 審查紀錄（2026-09-09）

> CEO 指令：「先做回測」——六項觀察條件面板「六條同時成立之後怎麼走」。
> 方法論：`work/stock-desk-五條件回測-方法論.md`（quant-researcher）。
> 本環境只有 `demo_synthetic` 示範資料（TWSE／TPEx 連線被 proxy 拒），所有數字僅驗證管線，真實數字須 CEO 本機重跑。

## 範圍裁定

- 面板六條中**前五條**可由日線價量 point-in-time 重算，做成回測策略 `five_conditions` 與事件研究；
  **第 6 條（防禦型規則）依賴持倉狀態與規則引擎，價格序列無法重算，不納入**。四處明寫（策略 docstring、事件研究檔頭、CLI 頂部＋說明段、方法論 §1）。
- 門檻與 epsilon 全部鏡射面板常數（`entryObservation.ts`／`keyLevels.ts`／`indicatorBands.ts`），集中在 `strategies.py`，不接受請求覆寫；`five_condition_series()` 是唯一一份條件定義，策略在切片上、事件研究在整段上呼叫，有逐 bar 相等測試。
- 出場規則（quant 事前決定，無調參）：收盤 ≤ max(開倉基準價 − 2×ATR14, 開倉基準價 × 0.92)（ATR 未定義只用後者）、收盤 ≥ 開倉基準價 × 1.2、收盤 < MA60，任一即全出；出場檢查先於進場；持倉中再成立不重錨。參數與面板「關鍵價位參考」同源。
- 前後半切分：本策略未擬合任何參數，「樣本外」只是同一組固定門檻在另一段期間的穩定度檢查，不得宣稱「通過樣本外驗證」。

## Look-ahead 防護（四層）

引擎 `iloc[:t+1]` 結構性切片 → 特徵全為尾隨 `rolling(min_periods=window)`＋向量化 vs 切片逐 bar 相等測試 → 一格位移偵測（實測 11.4% > 1%）→ 除權息還原尺度不變性（逐日決策 0 天不同，持倉 17 根非空測）。

## demo_synthetic 實跑（僅驗管線）

`POST /api/backtest` 2330 / 2024-09-04～2026-09-01 / train 252 / test 63 → 4 folds：樣本內策略 +18.75%（B&H +16.61%）、樣本外策略 +3.68%（B&H +24.33%）、樣本外回合 11、Wilson 5.1–47.7%。事件研究全期 55 事件；非重疊子樣本 20／60 根僅 7／4 筆，區間與基準無法區分。**資料為合成，不描述任何真實標的。**

## qa-reviewer 第一輪：PASS，BLOCKING_ISSUES=false

- 門檻／epsilon／開區間／ATR 定義／出場公式逐項比對前端常數一致；look-ahead 四層測試齊全；API 由 `STRATEGY_WARMUP_BARS` 自動納入；前端 `STRATEGY_OPTIONS` 推導無漏補。
- [low] 方法論第 354 行「不變更前端任何檔案」與 diff 矛盾 → 已改。
- 列管：`test_positions_store.py::test_two_stores_opening_the_same_legacy_database_at_once_both_survive` 為既有 SQLite 併發 flake（非本次引入）。
- Codex 第二意見：環境無 CLI，未執行。

## 風控第一輪：NEEDS_CHANGES（非 VETO）

方法無紅線（無保證語、無單一目標價、無指令語氣；基準並列、非重疊子樣本、Wilson 區間、demo 警告皆在）。擋下四個「該說而沒說」：

- REQ-1／1a `report.notes` 加 five_conditions 專屬句（五非六＋出場規則算式＋固定衡量設定非建議；禁「進場價」→「開倉基準價」、禁具體價格數字）。
- REQ-2 「五非六」進 CLI 頂部（方法論 §8.2 節錄已掉尾端說明段為實證）。
- REQ-3 Kelly 匯入對話框不得只顯示裸標籤：(a) 加短句或 (b) 不列入。
- REQ-4 CLI 補常駐免責／用途句於「說明與限制」首句。
- REQ-5 `--no-adjust-dividends` 句改條件式（與 2026-08-10 核可句一致）。
- REQ-6 CLI 全部中文字串逐字釘住＋`shared/forbidden-terms.json` 掃描。
- REQ-7 FOOTNOTES⑤「不是機率保證」改詞（不開白名單）。
- REQ-8 標籤「五項觀察條件」：REQ-1＋REQ-3 落地則可維持。
- REQ-9～11（建議）：方法論 §0 三處措辭；CLI 頂部重述「後半非模型樣本外」；無資料訊息附示範資料效力邊界。

## 補件

- creative-lead 定稿三句（REQ-1／2／4）；實作者還原 REQ-1 完整因果「依賴持倉狀態與規則引擎，價格序列無法重算」並統一半形空格。
- 後端：`FIVE_CONDITIONS_NOTE`（僅 `strategy == "five_conditions"` 附加）、`SCOPE_NOTICE`（頂部）、`RESEARCH_USE_NOTICE`（`FOOTNOTES[0]`）、`DEMO_DATA_WARNING` 抽常數、REQ-5／7／11 字面；新檔 `tests/test_event_study_wording.py`（逐字釘住、位置斷言、禁語掃描）；`test_api_backtest.py` 斷言該 note 僅 five_conditions 出現。
- 前端：REQ-3 採 (b)——`KELLY_IMPORT_STRATEGY_OPTIONS`（去掉 five_conditions），`KellyImportDialog` 改用，測試釘住。後端 Kelly 匯入端點未另加拒絕（列管）。
- REQ-9、REQ-11 已做；REQ-10 未做（列管，避免本輪再增一句）。
- 驗證：後端 `uv run pytest` 2413 passed、ruff／mypy 綠；前端 tsc 通過、vitest 649 passed；`/backtest` 頁面實機選「五項觀察條件」跑出報告，注意事項印出新句；CLI 頂部與說明段實跑確認。

## 風控複審：APPROVE

- REQ-1～8 全數落地且字面正確；REQ-9、11 已做；REQ-10 列管接受。逐句核對出場規則揭露與 `strategies.py` 實作一致。
- 對 REQ-1 字面差異表態：`FIVE_CONDITIONS_NOTE` 落地版為唯一定稿，creative-lead 160 字版作廢；流程註記「實作者事後改動已核可文案屬先斬後奏，日後偏離須落地前回審」。
- 新增 suggested：REQ-12「2×ATR(14)」宜註明取當日值（下次動句併同處理）；REQ-13 後端 Kelly 匯入端點若日後有其他路徑可達須先補拒絕；REQ-14 `_FRONTEND_TERMS` 手抄子集宜補同步測試、測試註解殘詞清掉（已清）。

## qa-reviewer 複審：PASS，BLOCKING_ISSUES=false

- [medium] `FIVE_CONDITIONS_NOTE`「跌破」與程式 `<=` 邊界不對稱（停利「觸及」對應 `>=` 一致）→ 屬字面改動，與 REQ-12 併入下次回審（列管）。
- [medium] `_FRONTEND_TERMS` 漏「準確率」→ 已補。
- [high，非本次引入、已列管] 後端 Kelly 匯入端點未拒絕 `five_conditions`，繞過 UI 直打 API 仍可匯入且無揭露 → 列管並指定下一輪待辦（owner：dev-lead）。
- [low] `DEMO_DATA_WARNING` 註解位置描述 → 已改；測試名「before any number」語意略寬（列管）；「建議」語境判斷為字元視窗 heuristic（列管）；`kellyImportDialog.test.ts` 原始碼正則寧誤擋不誤放（列管）；方法論 §10 未提 Kelly 排除 → 已補。
- Codex 第二意見：環境無 CLI，未執行。

## 列管

- 真實資料回測：CEO 本機 `/backtest` 選「五項觀察條件」＋ `uv run python -m app.backtest.event_study 2330`；費率 `verified_on` 待 data-engineer 查證；除權息未同步。
- **下一輪待辦（owner：dev-lead）**：後端 Kelly 匯入端點拒絕 `five_conditions`（qa high／風控 REQ-13）；`FIVE_CONDITIONS_NOTE`「跌破」→ 與 `<=` 對齊用字＋REQ-12「當日 ATR」併送風控。
- REQ-10；`/backtest` 頁首靜態句「以均線交叉（MA Cross）策略回測…」已不符四策略現況（既有字面，另案送風控）。
- 出場規則敏感度分析、2R 停利變體、ATR 定義（面板簡單平均 vs signals 層 Wilder）分歧。
- 既有 SQLite 併發 flake；Codex CLI 缺席政策。

---

# 附：回測頁權益曲線圖（CEO「我要的是像曲線圖那樣的」）

> 企劃與字面：`work/stock-desk-回測權益曲線-企劃.md`（creative-lead，creative-masters 三視角）。

## 實作

- 後端（dev-lead）：`POST /api/backtest` 新增 `curves`（每段 dates／strategy／buy_and_hold／drawdown、split_date、trades 三欄）；`_segment_series`／`_walk_forward_bounds` 抽取供 report 與 curves 共用；`drawdown_series` 與 `max_drawdown` 同源；Kelly 端點不外洩 curves（測試釘住）。`uv run pytest` 2427 passed。
- 前端：`app/lib/backtestCurves.ts`（每段各自指數化 100；`MIN_SEGMENT_POINTS=2`、drawable／absent）、`app/lib/backtestCurveWording.ts`、`app/backtest/EquityCurveChart.tsx`（lightweight-charts 5.2：兩段 × 兩線同色不連線、pane 1 回撤 BaselineSeries、分隔線 overlay、crosshair tooltip、圖例、線尾標籤；關閉平移縮放）；`BacktestReportView` 圖放在常駐免責句、費率警示、notes 之後、費率明細與表之前。
- 色彩：dataviz `validate_palette.js`（`#0a0a0a` 底）通過的類別色——策略藍 `#3987e5` 實線、Buy & Hold 黃 `#c98500` 虛線；sky＋灰組 normal-vision ΔE 14.4 未過 15 門檻故不採。回撤用策略同色相 10%。
- 守門：`backtestCurveWording.test.ts` 逐字釘住；`componentWordingScan.test.ts` 掃兩新檔；`backtestCurves.test.ts`。tsc 通過、vitest 661 passed。Playwright 1280／390 無橫向溢出、tooltip 可用。

## qa-reviewer 第一輪：PASS，BLOCKING_ISSUES=false

- [medium] `buildEquityCurveSeries` 未 memoize（hover 逐次重算）→ 已改 `useMemo`。
- [low] `text-[11px]`／`bg-neutral-100/[0.03]` 任意值 → 改 `text-xs`／`bg-neutral-100/5`；未用 `chartRef` → 移除；`CurveTrade` 三欄前端僅計數（刻意，見企劃「不做什麼」）。
- Codex 第二意見：環境無 CLI，未執行。

## 風控第一輪：NEEDS_CHANGES（非 VETO）

字面與色彩皆過（S1 通過、無目標價／預測線／交易標記）。擋在版位順序與退化狀態：

- REQ-1 圖不得在費率警示與除權息 notes 之前（未還原時被低估的是對照組 B&H，偏誤方向對策略有利）→ 圖移到 notes 之後。
- REQ-2 退化段（單根）指數化後兩線同在 100 會讀成「表現相同」；無樣本外時整圖為擬合區資料卻無標示 → 每段策略與 B&H 皆 ≥ 2 根才繪；未繪的段在圖上方具名說明（筆數不足／不在本次範圍兩句，creative-lead 定稿）。
- REQ-3 平移縮放可把切分線推出框外而說明句仍在 → 關閉 `handleScroll/handleScale`、容器 `overflow-hidden`、`splitX` 夾範圍、分隔線／標籤／底色／說明句／aria 共用同一條件。
- REQ-4 「交易次數：N（詳見下方表格）」N 為兩段合計、表格逐段 → 改「交易次數合計：N（樣本內外加總；逐段筆數見下方表格）」。
- REQ-5 空狀態句指名兩序列但判定只看策略 → 判定改兩序列皆檢查；句改「本次回測樣本內外皆無足夠的每日權益資料，故無法繪製曲線圖。」
- REQ-6 回撤「段內」限定 → 「策略回撤－段內（%）」／tooltip「策略回撤（段內）」。REQ-7 aria 單段變體 → 已加。REQ-8 B&H 缺席 → 該段不繪並具名。REQ-9 黃色 `#c98500` 僅為類別色，不得挪作警示／價值判斷（列管，寫進元件檔頭與企劃）。REQ-10 空狀態 markup 收斂為單一渲染點 → 已改。

## 風控複審：APPROVE

- REQ-1～5 全數以可驗證方式落地（含回頭核對後端 `trades` 口徑：N 恰等於兩表策略欄交易次數之和）；REQ-6～10 已落地。
- 新增 suggested：REQ-11 企劃補黃色語意邊界列管句 → 已補；REQ-12「少於 2 根」綁定 `MIN_SEGMENT_POINTS` 測試＋註明「可繪製點數」口徑 → 已補（(a) 路徑）；REQ-13 兩段皆繪但 `timeToCoordinate` 回 null 的極端態 → 列管。

## qa-reviewer 複審：PASS，BLOCKING_ISSUES=false

- cleanup 內 `setState`：React 19 無 unmount 警告、更新靜默丟棄；deps 變動重跑時清殘影屬必要 → 維持。
- [low] 註解「`curves` is null exactly when `report` is」歧義 → 已改為 `report.curves`／`report.report`。
- [low] 首次 paint 到 effect commit 之間 aria-label 短暫為單段變體（列管，不改 useLayoutEffect）；`indexTo100` 首值無效路徑的句子說「筆數不足」與根因不完全對應（列管，併 REQ-12）。
- Codex 第二意見：環境無 CLI，未執行。

## 列管（本附錄）

- REQ-13 極端態防線；aria 首幀過渡態；首值無效路徑措辭；險牌（交易時間點小刻度）。
- qa-e2e 正式實機驗收（本批以 Playwright 1280／390 自測代行）。
