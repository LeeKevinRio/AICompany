# C8 顯示語意落地 — qa-reviewer 覆審紀錄

- **日期**:2026-08-23
- **審查對象**:commit `6ea1e62`(feat: C8 顯示語意落地——勝率標籤改後端供給、新增完整回合數列),diff 10 檔 +483/−130
- **審查依據**:`work/reviews/2026-08-23-C8-顯示語意-風控批審.md` 落地條件 C8-1~C8-8、逐字定稿清單、零出現要求、列管 L-C8-1~4;`code-review-checklist` + `ui-delivery-checklist`(本批含 UI 變更,兩份都用)
- **結論**:**PASS**,`BLOCKING_ISSUES=false`

> qa 審查途中發現同一份批審文件已被 `93c25c5` 追加風控「四項全准」裁示,已交叉核對——qa 獨立查證的結論與風控裁示**一致**。

## 逐條驗收

1. **C8-1 PASS**:`BacktestReportView.tsx:90` 為 `{ label: labels.win_rate, ... }`,值來自 `metric_labels.win_rate = KELLY_WIN_RATE_ROUND_TRIP_QUALIFIER`(kelly_wording.py:814,字面逐字未動,僅上方 doc comment 改動);同表「勝率」出現次數為 1。
2. **C8-2 PASS**:`ALLOWED_SOURCE_CONTEXTS` 該筆**整筆刪除**(非改寫),比對規則未放寬,未新增第二筆;白名單名額由 1 筆降為 **0 筆**。
3. **C8-3(a) PASS**:前端全檔 grep「勝率」零命中(含英文治理註解);後端 `BacktestMetricLabels` 為單一供給點,並有 `test_no_construction_site_can_mint_its_own_metric_labels` 斷言 `metric_labels=` 從未在任何 construction site 被覆寫。
   **紅燈實測**:把 `kelly_wording.py:814` 常數改一字(回合→回和),`test_each_approved_sentence_ships_character_for_character[task-6-kelly]` 立即轉紅;還原後 working tree 乾淨。
4. **C8-4 PASS**,且「嚴格強於原條文」成立。**三種回退各自獨立植入實測**:①裸標籤 `label: "勝率"` → 6 項轉紅;②舊限定字面 → 5 項轉紅;③重打的**核可**字面(未透過 `labels.win_rate`)→ 6 項轉紅。三種回退全部被擋,且擋法不靠寫出被禁字面(未違反零出現要求)。
5. **C8-5 PASS**:`kelly_wording.py:787-814`、`BacktestReportView.tsx:20-71`、`test_kelly_wording.py:422-429` 三處治理註解皆已更正為「條件 49 之重送」敘述並標註批審檔;`REJECTED_LITERALS` 新增撤銷紀錄、`WIN_RATE_FRONTEND_INVENTORY` 對應筆數刪除。
6. **C8-6 PASS**:label 續引 `KELLY_DETAIL_ROUND_TRIPS_LABEL`,未新增常數定義;值為 `report.py:281` 的 `stats.n`,與同一 `_metrics` 呼叫內的 `win_rate` 讀**同一 `round_trips.stats` 物件**;前端 `num_round_trips` 全 repo 僅出現於型別宣告與 `formatCount(m.num_round_trips)` 兩處——零反推零重算。
7. **C8-7 PASS**,反向斷言確認 **non-vacuous**:`test_a_win_rate_of_none_is_never_paired_with_a_positive_round_trip_count` 明文寫入 `assert any(block.win_rate is None for block in blocks)` 作為自我查核並實跑通過(確有樣本命中該分支);另有 API 層獨立守門。
8. **C8-8 PASS**(`ui-delivery-checklist` 複核無新增違反項):新列與勝率列在 `metricRows()` 陣列中緊鄰(有 `indexOf(round_trips) === indexOf(win_rate)+1` 斷言並實測紅燈),同一 `<table className="… text-sm">`、同一 `<tr>` 渲染路徑,未進 tooltip/折疊/`text-xs` 註腳層;常駐免責句(:185-187)與產出時間戳(:168)皆在 `SegmentTable` **之前**渲染,新增列在其內部,不影響首屏相對位置。無新增 inline color、無新增 spacing 魔術數字。
9. **零出現 PASS**:`apps/` 下僅一處命中 `test_kelly_wording.py:638`(REJECTED_LITERALS 守門元組),與既有約 20 筆同類先例同構。
10. **禁止項未觸碰 PASS**:「獲利因子」列逐字未動;未新增 `round_trip_payoff_ratio`/跨界排除回合數/期末未平倉回合數;`engine.py`/`episodes.py` 的 diff 輸出為**空**。
11. **全套驗證(qa 本機重跑,非採信 dev 回報)**:後端 pytest **2286 passed + 1 deselected**(=2287)、`mypy --strict app` 115 檔綠、ruff 綠;前端 vitest **31 files / 529 passed**、tsc 綠、next build 綠(8 條路由)。`test_store_migration_multiprocess.py` flake 經確認與本批無關(diff 10 檔皆為 backtest/kelly_wording 相關),屬 D9 列管項重現。

## 送裁三項技術判定(qa 獨立驗證,與風控裁示一致)

1. **C8-4② label slot pattern 嚴格強於原條文——確認成立**。原條文有內在矛盾(要點名舊字面就違反自己的零出現要求)。dev 表述以三層覆蓋原三項要防的全部退化路徑,實測六項測試在對應規則轉紅。**②③ 需成對存在**:單留②則變數中轉(`const l = "…"; { label: l }`)可繞過;單留③則失去「標籤槽只能來自 `labels.*`」的結構宣稱。qa 確認 `6ea1e62` 已同時具備②③,符合 C8-11。
2. **守門元組例外與既有慣例一致——確認**。`REJECTED_LITERALS` 已有約 20 筆同類先例;`_shipped_sources()` 只掃 `backend/app/**` 與 `frontend/app/**`,`_APP_ROOT = backend/app` 不含 `backend/tests/`,守門元組所在檔天生在掃描範圍外,屬既有架構性豁免非本次新開例外。
3. **`CONFIRMED_VERBATIM` 逐字釘住處不算第二定義處,且不得改為 import——確認**。改成 import 等於拆掉 qa 在第 3 條紅燈實測所依賴的那個守門機制本身。定義處計數限定**出貨面**;測試樹為逐字比對刻意重打的字面不計入。可作日後同類條文判讀基準。

## Non-blocking(交下一輪修正批)

1. `componentWordingScan.test.ts:157-158` 治理註解引用已刪除的白名單筆目(「line above」懸空)——與 C8-5 同類,需更正指涉。(medium)
2. **C8-13**:`formatCount`(BacktestReportView.tsx:16-18)無任何測試覆蓋,`0`→`"0"` 與 `null`→`"—"` 的渲染層區辨僅由資料層測試間接保障,日後 `?? 0` 式正規化不會被偵測。qa 確認**目前行為正確**(`report.py:318` 對 B&H 傳 `round_trips=None`,「—」為真、「0」會宣稱做過量測而失真)。(medium,測試覆蓋缺口非行為錯誤)
3. **C8-10 qa-e2e 實機驗收尚未執行**——依章程與 C8-9,C8 不得標記 done。

## 第二意見(Codex)

CLI 不可用(`which codex`/`codex --version` 皆 command not found),依慣例降級:以逐檔閱讀 + **三次獨立紅燈實測**(常數漂移、三種標籤回退)+ 全套測試/型別/建置本機實跑取代,並與風控追加裁示的獨立核對結果交叉比對一致。

## 協調人補記

qa 的 non-blocking 1、2 即風控追加裁示的 C8-13 與附帶查獲,已於 qa 覆審完成前派 dev 補齊(與 C8-11 同批)。C8-11 經 qa 確認 `6ea1e62` 已實質具備,dev 該批為明文固化。
