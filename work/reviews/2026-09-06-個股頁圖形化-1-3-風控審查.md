# 個股頁圖形化 1–3 — 風控逐字審＋qa-reviewer 審查紀錄（2026-09-06）

> CEO 指示（2026-09-06）：「好 你直接先做123」——位階量表、停損停利階梯圖、視覺分級。
> 本紀錄彙整 risk-compliance-officer 兩輪審查、creative-lead 起草、qa-reviewer 審查與落地結果。

## 結論

- 風控第一輪：**NEEDS_CHANGES**（R1–R8 required、S-a～S-g suggested）。
- 風控第二輪：**APPROVE**（R1–R8、S-a～S-d 全數達標；7 條 suggested 列管）。
- qa-reviewer：**PASS**，`BLOCKING_ISSUES=false`；Codex 第二意見因環境無 `codex` CLI 未執行（已知缺口）。
- 測試：`npm run typecheck` 通過；`npx vitest run` 36 files / 611 tests 全綠。

## 交付內容

| 項 | 元件 | 純函式 | 說明 |
|---|---|---|---|
| 1 位階量表 | `RangeGauge.tsx` | `keyLevelsVisuals.ts` | 近 252 根區間位置的橫向軌道，三區帶（≤30／30–70／≥70）為 sky 單一色相三階；標記帶「收盤位於 N%」；兩端印出區間最低／最高。 |
| 2 價位階梯 | `PriceLadder.tsx` | `keyLevelsVisuals.ts` | 所有參考價位由高至低排成一張表，每列帶「相對基準價」百分比與中性灰距離條（基準線置中）；「大字所示」標記停損／停利大字採用的那一列。 |
| 3 視覺分級 | `TechnicalIndicatorsPanel.tsx`（指標速覽）、`AdviceCardView.tsx`（方向標籤＋堆疊條） | `indicatorBands.ts`、`ruleDirection.ts` | RSI／K 值／%B／成交量 z 依教科書門檻分低／中／高區帶，chip 用填充度符號 ○◐● ＋ sky 三階；命中規則加「建設性／防禦型／中性」中性 chip，方向權重畫成灰階 100% 堆疊條＋圖例＋限定語。 |

## 風控第一輪 required 與處理

| 項 | 內容 | 處理 |
|---|---|---|
| R1 | △／▽ 為方向符號，規避 S1 | chip 改 ○◐● 填充度；方向 chip 無符號；守門測試禁 `▲▼△▽↑↓↗↘⇧⇩🔺🔻` |
| R2 | 高區帶 amber 與警示語彙同色 | 區帶 chip 改 sky-950/900/800 單一色相；守門禁 amber |
| R3 | 收盤 vs MA 非區帶、above→amber 為價值判斷 | 整個移除該 chip 與 `closeVsMa` |
| R4 | 跨 bars／signals 兩 payload 比較，時間戳不一 | 隨 R3 消解；`page.tsx` 不再傳 `latestClose`；守門禁復活 |
| R5 | 堆疊條占比缺限定語，可讀成機率 | creative-lead 起草三候選，採候選 A `DIRECTION_SHARE_QUALIFIER`；與條同區、text-sm／neutral-400、不摺疊；風控逐字通過 |
| R6 | 「與『拉回觀察』卡片的 MA20 為同一數字」指涉落空 | 採 (a)：拉回卡恢復 MA20／MA60／近 60 日低點三列 |
| R7 | 停利卡「以下數字皆由固定算式自基準價推得」下只剩 MA20 列 | 採 (a)：停利卡恢復 2R／+20% 兩列 |
| R8 | chip 散落各卡、圖例只在速覽 | 採 (a)：chip 只在「指標速覽」渲染；`IndicatorCard` 移除 chip prop |

Suggested 已採納：S-a（量表數值 text-sm）、S-b（「收盤位於」）、S-c（階梯距離條中性灰、移除色點）、S-d（方向 chip／堆疊條全中性；守門區間涵蓋 `DIRECTION_BAR_CLASS`）。
第二輪 suggested 已採納：`fmtSigned` 不印 `-0.0%`；R3/R4 掃描擴及 `indicatorBands.ts`／`page.tsx`；R1 符號集擴充；`RangeGauge` 禁 amber。

## qa-reviewer 發現與處理

- [low] `ladderRungLabel` 缺 exhaustiveness guard → 已加 `never` 分支。
- [nit] `fmt={(n) => fmt(n)}` 多餘包裝 → 直接傳 `fmt`。
- [nit] 量表三區帶 `gap` 造成總寬略超 100% → 改 flex-grow 比例。
- [medium] MA 相對位置借用冷暖色階 → 與風控 R3 同一問題，已移除。
- 唯讀權限判準常數未觸及（`validate_agents.py`、`READONLY_*` 無變更）。

## 本批釘住字面（風控第二輪清單，含標點不得再改）

- `RangeGauge.tsx`：`KEY_LEVELS_GAUGE_LOW_END_LABEL`、`KEY_LEVELS_GAUGE_HIGH_END_LABEL`、`KEY_LEVELS_GAUGE_MARKER_LABEL`（「收盤位於」）、`buildGaugeAriaLabel`。
- `PriceLadder.tsx`：`KEY_LEVELS_LADDER_TITLE`、`KEY_LEVELS_LADDER_INTRO`、`KEY_LEVELS_LADDER_HEADLINE_TAG`、`KEY_LEVELS_LADDER_DISTANCE_HEADER`、`KEY_LEVELS_LADDER_NOTE`。
- `KeyLevelsPanel.tsx`：`KEY_LEVELS_LADDER_RUNG_ANCHOR_COST`、`KEY_LEVELS_LADDER_RUNG_ANCHOR_CLOSE`、`KEY_LEVELS_LADDER_RUNG_CLOSE`。
- `TechnicalIndicatorsPanel.tsx`：`INDICATOR_OVERVIEW_TITLE`、`INDICATOR_OVERVIEW_LEGEND`、`INDICATOR_OVERVIEW_EMPTY`、`RSI_BAND_LABELS`、`KD_BAND_LABELS`、`PERCENT_B_BAND_LABELS`、`VOLUME_Z_BAND_LABELS`、符號集 `○◐●`。
- `AdviceCardView.tsx`：`DIRECTION_BAR_ARIA_LABEL`、`DIRECTION_SHARE_QUALIFIER`。

## 剩餘落地條件與列管

1. 三句限定語（`KEY_LEVELS_LADDER_NOTE`、`INDICATOR_OVERVIEW_LEGEND`、`DIRECTION_SHARE_QUALIFIER`）維持 ≥ text-sm／≥ neutral-400、與圖同區、不摺疊；對應守門斷言不得放寬。
2. 色彩紅線：方向 chip／堆疊條全中性；區帶 chip 單一色相無 amber；階梯／量表無 sky 以外語意色與 amber；三圖形元件無 red/green/rose/emerald。
3. qa-e2e 實機驗收待補：≤400px 與 200% 放大下限定語不截斷、量表標記不溢出、堆疊條灰階與圖例可辨識（本次 frontend-engineer 以 Playwright 自測 390px：頁面無橫向溢出，階梯表內部捲動）。
4. 列管 suggested：「占比／佔比」用字統一（需重送字面）；`KEY_LEVELS_TARGET_STANDING_NOTICE` 範圍句由 creative-lead 收斂；堆疊條百分比四捨五入配平；後端 `_direction_weights` 是否必然涵蓋全部 matched rules（dev-lead 確認）；堆疊條 neutral-700 段對比實機確認；S-e／S-f／S-g 續列。
5. Codex 第二意見缺口續列（環境無 CLI）。

## 實測數字（2330 持倉，桌機 1280px）

- 整頁字數：5,504（批次 3）→ 5,973（本批），**增加約 470 字**——新增的是圖形旁的限定語、圖例與階梯列標籤，圖形沒有取代任何既有揭露句；字數減少不是本批目標。
- 手機 390px：頁面無橫向溢出；價位階梯表在自身容器內橫向捲動。
